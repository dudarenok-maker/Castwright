## Summary

A multi-item backlog round delivered via two non-overlapping streams (server + frontend) plus the fs-25 documentation archive, reconciled on one integration branch and verified once.

**Server (srv-29 + fs-32a/c):**
- **srv-29 (#501)** — generation.ts's inlined chapter encode/persist tail is gone; it now calls the shared `finalizeChapterAudioWrite` with an `onEncoded` callback (its `bumpProgress` seam). Behaviour-neutral; the splice and generation paths can no longer diverge. ~292 lines of duplicated tail removed.
- **fs-32a (#503)** — the splice path now passes the *analytic post-splice* `expectedSec` (prior − replaced spans + new spans) to chapter QA, so a legitimate length-changing re-record no longer false-flags the advisory "suspect" badge while a true runaway/truncation still trips it.
- **fs-32c (#503)** — the non-English designed-voice manifest language re-check is extracted to a shared `clearMismatchedDesignedVoices` helper and now runs on the splice path too (previously generation-only), so a re-record on a non-English book clears a wrong-language designed voice just like generation.

**Frontend (fe-25 + fe-6 + fs-26 + fs-32b + fe-31):**
- **fe-25 (#460)** — the dead mini-player volume button is now a working volume slider, persisted per-device via the `settings` slice (`playerVolume`), applied to the audio element and re-applied across chapter switches.
- **fe-6 (#413)** — the 48 waveform bars are hoisted to a module constant (seeded → identical every mount), dropping the per-instance `useMemo`. (Exploration found no internal interval to lift — the bars are static, animation is prop-driven.)
- **fs-26 (#480)** — the line-level Listen-view per-sentence re-record entry: a re-record marker on the MarkersPanel carries a "Fix this line" action that resolves its timestamp → chapter segment (`lib/resolve-segment-for-sec.ts`) → `{characterId, segmentIndex}` and opens `FixCharacterAudioModal` pre-scoped to that single segment in rerecord mode (`segmentIndices` threaded through `splice-slice`/`splice-runner-middleware` → `api.streamSplice`); a kind toggle lets a note become a re-record marker. Completes fs-26's substantive scope.
- **fs-32b (#503)** — verified the A/B / revision player renders a splice revision with empty `segments[]` without crashing (no real crash path found — added a regression test that locks it).
- **fe-31 (#506)** — a ▶ preview affordance on the manuscript per-quote emotion chip: tag a line and audition that speaker's designed Qwen variant in place (existing `${baseScope}__${emotion}` cache scope via `playSampleWithAutoLoad`, no synth-schema change). No-variant/non-Qwen UX (which doubles as fs-25's 5e manuscript hint): a Qwen speaker without the variant previews the base voice + an inline "renders neutral" note; a non-Qwen speaker disables the preview with an "Emotion only audible on Qwen" tooltip. Plan `180-fe31-emotion-chip-preview.md`.

**Docs / fs-25 archive (#479):**
- fs-25's core (waves 1–3, 4a, 5a–5d, 6a) already shipped via PR #505; this round archives plan 177 as `stable` with Ship notes, moves it under `archive/`, and updates INDEX.
- Deferred sub-waves re-homed as their own backlog items: **4b → fs-33 (#510)**, **5e remainder → fs-34 (#511)**, **6b → fe-32 (#512)**. The 5e manuscript missing-variant hint ships via fe-31 here.
- BACKLOG rows removed for every item this round delivers; the three re-homed rows added.
- fe-31's plan doc was renumbered 179 → **180** because plan 179 is reserved by the in-flight pre-assembly-QA branch (PR #513).

## Test plan

- `npm run verify` (typecheck + lint + all test tiers + e2e + build) — **green locally** before push.
- Server: 1952 + 173 (slow) passed, incl. new `finalize-chapter-write.test.ts` (onEncoded seam), `verify-designed-voice-language.test.ts`, `chapter-splice-rerecord-qa.test.ts`.
- Frontend: 2262 passed + 15 touched e2e, incl. new `waveform.test.tsx`, `resolve-segment-for-sec.test.ts`, `play-emotion-variant.test.ts`, `revision-diff.test.tsx` (empty-segments), mini-player volume, and `manuscript-emotion-preview.spec.ts`.
- **Owed (live GPU, non-blocking, noted in plans):** fs-25/fe-31 variant audible-on-Qwen + byte-identical-on-Kokoro; fs-26 live re-record; fs-32c non-English wrong-language clear.

Closes #501, closes #503, closes #460, closes #413, closes #480, closes #479, closes #506

Refs #510 #511 #512
