import type { SeriesMemoryDetail } from '../lib/types';

// Populated by Task 11. Empty until then — no consumer calls getSeriesMemory in mock mode yet.
export const MOCK_SERIES_MEMORY: Record<string, SeriesMemoryDetail> = {};
