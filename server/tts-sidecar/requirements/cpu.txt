# CPU overlay (no GPU). Vendor-neutral base + the torch engines on a CPU runtime.
# Selected by the accelerator profile resolver when no supported GPU is detected
# (or ACCELERATOR=cpu). Differs from nvidia-cuda.txt only in the ONNX runtime:
# plain `onnxruntime` (CPU EP) instead of the onnxruntime-gpu the [gpu] extra pulls.
-r base.txt

# Coqui XTTS is opt-in on every profile — install it from the Model Manager.
# Qwen is GPU-only (needs an nvidia/amd overlay) — it is not installable on a CPU-only box.

# torch + torchaudio — EXPLICIT, matched, pinned (coqui-tts 0.27.5 dropped the
# transitive torch dep). No index here: PyPI gives a CUDA-bundled wheel on
# win/linux x86_64 that runs fine on a CPU box (it falls back to CPU). For a lean
# CPU-only build the installer pre-installs the cpu-index wheel BEFORE this file
# (install-torch.mjs, cpu profile → https://download.pytorch.org/whl/cpu); the
# ==2.8.0 pins below then stay satisfied.
torch==2.12.0
torchaudio==2.8.0

# Kokoro v1 — plain (no [gpu] extra) so the CPU/CoreML onnxruntime is used; the
# explicit onnxruntime below is the CPU execution provider. Run
# scripts/install-kokoro.{ps1,sh} once to fetch the model + voices manifest.
kokoro-onnx>=0.4.0,<0.5.0
onnxruntime
