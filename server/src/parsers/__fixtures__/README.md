Binary fixture for `epub.test.ts`. Regenerate with:

    node scripts/gen-parser-fixtures.mjs   (from the repo root)

No PDF fixture: `pdf.test.ts` mocks pdf-parse instead. The integration
path is covered by the canonical e2e manuscript run (see CLAUDE.md).
