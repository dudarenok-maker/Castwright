# fs-38 Wave 3c — XTTS voice clone Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let a cloned voice render on Coqui XTTS v2 as well as Qwen — and close the upstream defects that today let a cloned voice render as a stranger, or let a *revoked* person's voice keep playing.

**Architecture:** The clone identity lives in the voice-library entry (`master.wav` + consent). 3c adds a **second derivable artifact** — `voices/xtts/xtts-<uuid>.pt` (gpt conditioning latents + speaker embedding) — produced by `POST /xtts/clone-voice` from the same PCM the Qwen route consumes. Everything downstream becomes **engine-parametric**. Derivation is **lazy**: assignment writes both clone-capable slots, and whichever engine a chapter routes to gets its artifact derived by the 3b2 pre-pass — partitioned by engine so Qwen and Coqui are never forced co-resident.

**Tech Stack:** Python 3.12 sidecar (FastAPI, coqui-tts 0.27.5, torch), Node 20 + TypeScript server (Express, Vitest), React 18 + RTK frontend, Playwright e2e, pytest.

---

## Review provenance

Four independent review passes shaped this plan. Findings are cited inline so any requirement traces back to evidence.

| Pass | Mission | Verdict on the draft it saw |
|---|---|---|
| `[AC]` Opus assumption-checker | Verify every claim against real code | *"Not safe to execute as written"* — 7 Critical |
| `[ADV]` Opus adversarial | Find remaining silent-substitution paths | 4 Critical, 6 High |
| `[EX]` Opus executability | Will cold subagents actually build this? | *"Research excellent, executability is not"* — would refuse 5 tasks |
| `[FAB]` Fable independent correctness | Does it hold both safety properties? | *"Not safe — because of C1"* (consent erasure incomplete) |

**Convergent findings** (found independently by more than one pass, and therefore the highest-confidence items): the mock layer cannot fail `[ADV+AC]`; the latents-cache interleaving `[ADV+AC]`; the audition path disagreeing with the render `[ADV+AC]`; D-E breaking designed voices `[ADV+AC]`; and **Task 13's prescribed fix not fixing its own described race** `[EX+FAB]`.

---

## Global Constraints

Every task's requirements implicitly include this section.

- **`TtsEngine` has no `'xtts'` value.** The engine is `'coqui'` (`server/src/tts/model-keys.ts:18`). `xtts` is **only** the manifest slot key `engines.xtts`, the artifact dir `voices/xtts/`, and the storage prefix `xtts-<uuid>`.
- **Property 1 — never-silent-substitution.** A cloned voice either renders as itself or fails loud. Never substituted — not by `FALLBACK_SPEAKER`, not by `applyQwenFallback`, not by the coqui profile catalog, **and not by having its marker deleted upstream** (Phase 0).
- **Property 2 — erasure is total.** On revoke, every artifact the voice can be *rebuilt or rendered from* is destroyed: disk artifacts, sidecar in-process caches, **and every audition cache regardless of its scope key** `[FAB-C1]`. Already-rendered chapter audio is out of scope by product decision — state that explicitly in plan 269 rather than leaving "total" unqualified `[FAB-M4]`.
- **`provenance` decides the FAILURE POLICY, and it is never inferred from a name prefix.** `qwenStorageKey` returns `qwen-<uuid>` for **designed** voices too (`voice-mapping.ts:21-26`), so a `qwen-` prefix test is *not* a cloned test `[EX-8]`. Both `cloned` and `designed` entries can carry a coqui slot (D-B/D-E); what differs is what happens when the artifact is missing:
  - **`cloned` ⇒ fail loud.** Never substituted, never fallen back, on any engine. This is Property 1.
  - **`designed` ⇒ fail soft.** Fall back to `COQUI_PROFILE_VOICES` exactly as today. Never a new hard failure (§2.3).
  - **`imported`, or `designed` with no `master`** ⇒ no coqui slot is ever written; behaviour is byte-for-byte unchanged.
  Any new branch must state which policy it is on. A branch that treats the two alike is wrong in one direction or the other.
- **Shared vocabulary lives in `server/src/tts/clone-engines.ts`** (Task 1). Never redefine `CloneEngine`, `CLONE_CAPABLE_ENGINES`, `manifestSlotFor`, or `characterHasClonedSlot` locally — four tasks would otherwise write four copies, each passing its own review `[EX-11]`.
- **OpenAPI is the type source of truth.** `VoiceLibraryEngines.xtts` exists at `openapi.yaml:5087-5093` with `coquiVersion` + `modelId`. Any wire change (including Task 24's engine-aware sample request `[FAB-M3]`) means editing `openapi.yaml` then `npm run openapi:types`. **Never hand-edit `src/lib/api-types.ts`.** `server/src/workspace/voice-library.ts:23-33` is a manual mirror — keep names identical.
- **User-facing copy says "Coqui XTTS v2"**; the blessed short label is **`Coqui`** (`src/lib/tts-models.ts:146-152`). Never bare "XTTS" in UI copy, including status chips `[AC-I8]`.
- **No hex colour literals.** **Touch targets ≥44×44** via `min-h-[44px] fine-pointer:min-h-0` (never `sm:min-h-0`).
- Behind `voices.library.enabled` (`server/src/config/registry.ts:644-653`). `XTTS_VOICES_DIR` is a derived spawn env like `QWEN_VOICES_DIR` — no registry knob. Any *other* new env var needs one.
- **Test commands.** Server: `npm --prefix server run test` or `cd server && npx vitest run <path>`. **There is no `src/tts/` at repo root** — root `npm test` is the frontend suite `[EX-2]`. Sidecar: `server/tts-sidecar/.venv/Scripts/python.exe -m pytest server/tts-sidecar/tests/<file> -v`.
- **Sidecar tests run without GPU or weights.** Use `sys.modules` injection (`tests/test_runtime_wiring.py:53-190`) or fixture reuse (`tests/test_qwen_clone_voice.py:1-31`). Use **bare `TestClient(main.app)`**, never `with TestClient(...)` — the context manager fires six startup handlers against the fake torch `[AC-I5]`.
- **`vi.resetModules()` footgun:** never assert `instanceof` on a statically-imported class in a file calling `vi.resetModules()`.
- **Report what you observed.** If a step SKIPs (missing weights, missing browser), say so with the reason. Never report an unobserved pass `[EX-17]`.
- Commit `<type>(<scope>): <subject>`; scopes `server`/`frontend`/`sidecar`/`docs`/`e2e`/`scripts`. Branch `feat/fs38-wave3c-xtts`. PR links `Closes #624` plus Phase-0 bug issues.

---

## Design decisions

| # | Decision | Spec said | Why |
|---|---|---|---|
| **D-A** | **No engine picker in the clone wizard.** Clone derives Qwen first; XTTS derives lazily when a chapter routes to coqui. | §6: "Phase 2 (3b1, **+engine choice 3c**)". | §4.2 already says `deriveEngineArtifact` "serves engine-switch + orphan self-heal". A picker asks the user to commit to an engine they cannot evaluate yet. **Verified sound by all four passes.** |
| **D-B** *(reversed — user call)* | **Designed-voice XTTS eligibility IS in scope**, with a **fail-soft** policy that is the exact opposite of the cloned one. | Wave table: "spike-gated"; §2.3 calls synthetic-clip→latents "quality-unvalidated". | Delivered in-wave at the user's direction. The quality question is real but the **downside is bounded**: a designed voice on a Coqui book renders an *unrelated stock catalogue speaker* today, so even an imperfect derive from its own calibration clip is closer to the intended identity than the status quo. See **D-F** for the policy that keeps it safe. |
| **D-F** *(new — required by D-B)* | **Failure policy differs by provenance.** Cloned → fail loud, always. Designed → **fall back to the catalogue**, never hard-fail. A designed entry with no retained clip gets **no coqui slot** and keeps today's behaviour byte-for-byte. | §2.3: the designed clip-persist work "never introduces a new hard failure". | Without this, D-B re-creates exactly the Critical that review caught in D-E's first form. A designed voice is not a real person's identity; substituting it is the *current, acceptable* behaviour. A cloned voice is, and must not be. |
| **D-G** *(new — required by D-F, from the delta review)* | **The validate set must be a superset of the resolve set.** Anything that can resolve to `xtts-<uuid>` must first have been through a pre-pass that either derived the artifact or removed the slot. | — | `[DELTA-C2]` found three reachable paths where a designed coqui slot reaches `pickVoiceForEngine` without any derive — fs-60's Qwen→Coqui fallback, `qwenUnavailable`, and `clearMismatchedDesignedVoices` deleting only the qwen slot. Each turns a chapter that renders today into a 409. **This is the single constraint that makes D-B safe**, and it is why Task 20a selects on *"has an xtts slot"* rather than on the routed engine. |
| **D-C** | **Hand-rolled latents** (`get_conditioning_latents` + `_atomic_torch_save`), not `CloningMixin`. | Not addressed. | Verified: the built-in slugifies and writes `<voice_dir>/<slug>.pth` via plain `torch.save` (`TTS/utils/voices.py:100-113`) — we would not control the path (so purge could not reliably erase) and the write is not atomic. |
| **D-D** | **Sidecar substitution of a cloned key is FATAL Node-side** — gated on `xtts-` only, plus provenance. | Not addressed. | `sidecar.ts:188-195` only warns. Gating on `qwen-` too would make *designed* substitution fatal, which the Global Constraint forbids `[EX-8]`. |
| **D-E** *(amended twice)* | **Assign writes both clone-capable slots when the entry can actually render on both** — `provenance === 'cloned'`, **or** `provenance === 'designed'` **and a retained designed clip exists on disk**. Never otherwise. | Not addressed. | A qwen-only slot means an engine switch drops to `COQUI_PROFILE_VOICES` and silently replaces a real person. **The unconditional form was Critical** `[AC-C5][ADV-H3]`. ⚠ **`entry.master` is the WRONG test** `[DELTA-C1]`: it is written in exactly one place — the `/clone` promote at `voice-library.ts:745-757`, which hardcodes `provenance:'cloned'` at `:749` — so **no designed entry can ever have it**, and the first draft of this condition was unsatisfiable dead code. The designed clip is a *file*: `qwenVoiceWavPath(\`qwen-${uuid}__master\`)`, written only by the sidecar's `design_voice` (`main.py:3862-3866`) and read at `synthesise-chapter.ts:997`. `purge-clone-artifacts.ts:63-65` documents the split. |

---

# Phase 0 — Foundations and marker preservation

**Tasks 2-6 fix defects in shipped code.** Each deletes/launders the clone marker or leaks a revoked voice *before* any resolver runs, so every guard 3b2 shipped and 3c adds is inert against them. Each files a `bug` issue and ships a regression test that **fails before the fix**.

## Task 1 — `clone-engines.ts` shared vocabulary `[EX-11]`

**Files:** Create `server/src/tts/clone-engines.ts`; Test `server/src/tts/clone-engines.test.ts`

**Why first:** six later tasks reference this vocabulary. Without it, cold implementers each invent a local copy.

**Produces** (every later task imports from here, never redefines):
```ts
export type CloneEngine = 'qwen' | 'coqui';
export const CLONE_CAPABLE_ENGINES: ReadonlySet<TtsEngine>;      // {'qwen','coqui'}
export function isCloneEngine(e: TtsEngine): e is CloneEngine;
export function manifestSlotFor(e: CloneEngine): 'qwen' | 'xtts'; // coqui -> 'xtts'
export function cloneStorageKey(e: CloneEngine, uuid: string): string; // qwen-<u> | xtts-<u>
export function characterHasClonedSlot(c: { overrideTtsVoices?: … }): boolean;
export function clonedSlotForEngine(c, e: TtsEngine): { libraryUuid: string } | undefined;
```

- [ ] **Step 1: Write the failing tests** — `manifestSlotFor('coqui') === 'xtts'`; `cloneStorageKey` prefixes; `characterHasClonedSlot` true for a cloned slot on *either* engine and **false** for a `designed` slot carrying a `libraryUuid`; `clonedSlotForEngine` returns undefined for a non-cloned slot.
- [ ] **Step 2: Run to verify they fail** (module absent).
- [ ] **Step 3: Implement.** Pure functions, no I/O, no imports beyond `TtsEngine`.
- [ ] **Step 4: Run** `cd server && npx vitest run src/tts/clone-engines.test.ts` → PASS. **Commit** — `feat(server): shared clone-engine vocabulary`

## Task 2 — Consent gate + total audition erasure `[FAB-C1]`

**Files:** Modify `server/src/routes/voice-sample.ts:105-133`, `server/src/workspace/purge-clone-artifacts.ts`; Test `server/src/routes/voice-sample.test.ts`, `purge-clone-artifacts.test.ts`

**The bug, verified.** The cast-view sample route caches under `cacheScope = voiceId` — **the character's id** (`voice-sample.ts:124`) — and serves it via an `existsSync` short-circuit with **no consent check anywhere in the route** (`:131-133`). `purgeVoiceSamples` is strictly prefix-scoped (`server/src/tts/voice-sample-cache.ts:75-87`), so a `char-<id>-…` file is unreachable by any purge. The raw branch (`:112`) can also synthesize an arbitrary named key with no gate.

**Repro:** audition a cloned-assigned character from the Cast view → revoke consent. `master.wav`, both engines' artifacts, the library-scoped cache and sidecar RAM are all erased — **and the Play button still plays the revoked person, permanently.** That audio is itself re-ingestable as a fresh clone source. Property 2 does not hold.

- [ ] **Step 1: Write the failing tests** — (a) after revoke, the cast-view sample route **refuses** for a character whose slot carries `provenance:'cloned'`, *including when a cached file exists* (the gate must precede the `existsSync` short-circuit); (b) `purgeCloneArtifacts` erases sample files rendered from `qwen-<uuid>`/`xtts-<uuid>` **regardless of cache scope**; (c) the raw branch refuses a cloned storage key; (d) a non-cloned character's audition and cache behaviour are **unchanged**.
- [ ] **Step 2: Run to verify they fail** — the revoked voice still plays from cache.
- [ ] **Step 3: Implement.** Two independent halves, both required: a provenance-gated consent check mirroring `routes/voice-library.ts:428`, placed **before** the cache short-circuit; and purge matching on the embedded `voiceName` (it is hashed into the filename by `voiceSampleFileName`) rather than only on scope. The consent gate is the robust half — the cache sweep is defence in depth.
- [ ] **Step 4: Run** `npm --prefix server run test -- voice-sample purge-clone` → PASS. **Commit** — `fix(server): gate cloned auditions on consent and erase them on revoke`

## Task 3 — `clearMismatchedDesignedVoices` must skip cloned voices `[ADV-C1]`

**Files:** Modify `server/src/tts/verify-designed-voice-language.ts:41-48`; Test `verify-designed-voice-language.test.ts`

**The bug, verified.** No provenance check, and the manifest is resolved via `qwenStorageKey(c, c.id)` → `qwen-<c.voiceUuid ?? c.voiceId ?? c.id>`, while a cloned artifact lives at `qwen-<libraryUuid>` (`voice-mapping.ts:341`) and the assign route never sets `c.voiceUuid` (`routes/voice-library.ts:817-819, 919-930`). The read **always** misses → `delete c.overrideTtsVoices.qwen`. Callers: `generation.ts:810`, `chapter-splice.ts:266`, both under `if (nonEnglishBook)`.

**Repro:** clone → assign in a Russian book → Generate. Marker deleted; `pickVoiceForEngine('qwen')` returns `''`; `needsFallback` fires; `forbidKokoroFallback` + `coquiEligible` routes to `pickVoiceForEngine('coqui')` = **a stock catalog voice.** Silent.

- [ ] **Step 1: Write the failing test** — a cloned slot with no `voiceUuid` and no manifest on disk **survives**, and the character is absent from `cleared[]`.
- [ ] **Step 2: Run to verify it fails.**
- [ ] **Step 3: Implement** — first line of the loop body, using `Task 1`'s helper:
```ts
    /* fs-38 Wave 3c — a CLONED voice's language is the speaker's own, not a
       baked design language, and its manifest lives under qwen-<libraryUuid>,
       which qwenStorageKey() does not produce. Both facts made this loop
       delete the clone marker on every non-English book, after which
       applyQwenFallback saw an ordinary undesigned character and fs-60's
       Qwen->Coqui branch rendered a catalog speaker for a real person. */
    if (c.overrideTtsVoices?.qwen?.provenance === 'cloned') continue;
```
- [ ] **Step 4: Run** → PASS, designed-voice cases green. **Commit** — `fix(server): never language-clear a cloned voice slot`

## Task 4 — Series rebaseline must not launder provenance — set **and** clear paths `[ADV-C2][ADV-M5][FAB-I4]`

**Files:** Modify `server/src/routes/voice-override-linked.ts:186-190`, `server/src/routes/voices.ts` (clear branch), `src/store/voices-slice.ts:73-80`; Test all three.

**Two branches, both laundering:**
- **Set:** `:190` replaces the slot with a bare `{name}` (dropping `libraryUuid`/`provenance`) and `:186` rewrites `voiceUuid`, across every book in the group (`:118-125`). `routes/voices.ts:781` is the well-behaved spread. On qwen the `voiceUuid` rewrite makes `pickVoiceForEngine` return a *different existing* storage key → **a different voice renders, no error at all.**
- **Clear `[FAB-I4]`:** `override === null` does `delete next.overrideTtsVoices` (`:187-188`); `voices-slice.ts:73-76` clears every slot client-side. A linked "clear override" on a group containing a cloned sibling **in other books** silently reverts a real person to a catalog voice.

- [ ] **Step 1: Write the failing tests** — set path preserves `libraryUuid`/`provenance`; **both** set and clear are **refused** when the existing slot is `provenance:'cloned'`; the redux reducer preserves other slot fields. Note the client applies optimistically before the refusal lands — assert the 409 result is surfaced `[FAB-M5]`.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — spread as `voices.ts:781` does; refuse **any** group write that would remove or replace a cloned slot (a rebaseline is a designed-voice operation; silently repointing a consented clone is never correct). Mirror the spread in `voices-slice.ts`.
- [ ] **Step 4: Run** both suites → PASS. **Commit** — `fix(server,frontend): preserve cloned provenance through series rebaseline`

## Task 5 — Reuse hydration must not reroute or launder `[ADV-C3][EX-10]`

**Files:** Modify `server/src/tts/hydrate-reused-voice.ts:28,56-58,107,126-144`, `server/src/workspace/series-reuse-link.ts:82`, `server/src/routes/cast-link-prior.ts:51`; Test `hydrate-reused-voice.test.ts`

**The bug.** `hasOwnQwenVoice(c)` = `!!c.overrideTtsVoices?.qwen?.name` is the sole gate; own slots win the override merge but **`ttsEngine` does not** (`:140`). Called on the hot path (`generation.ts:775`) before the engine force and the pre-pass. A coqui-cloned character can be rerouted to qwen and pick up **the source book's** qwen slot → another character's designed voice. `OverrideSlot` (`:28`) omits `libraryUuid`/`provenance`, so the compiler can't flag it.

**Second route to the same bug `[EX-10]`:** `:107` does `ttsEngine: source.ttsEngine ?? 'qwen'` — a hardcoded qwen default that Step 3's wording would not obviously reach. Fix it explicitly.

- [ ] **Step 1: Write the failing tests** — a coqui-cloned character with a `matchedFrom` pointing at a qwen-designed source and no explicit `ttsEngine`: `ttsEngine` is **not** rewritten to `'qwen'` (cover **both** `:107` and `:140`), and no foreign qwen slot is merged in.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — widen `OverrideSlot` to carry `libraryUuid?`/`provenance?` (and in the two sibling files, so the lie doesn't persist); rename the gate to `hasOwnVoice` (any slot with a `name` **or** a `libraryUuid`); never inherit or default `ttsEngine` when the character carries a cloned slot on another engine.
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): reuse hydration preserves a cloned character's engine and slot`

## Task 6 — The force-to-Qwen loop must **retarget**, not skip `[ADV-H4][FAB-I3]`

**Files:** Modify `server/src/routes/generation.ts:795-798`, `chapter-splice.ts:258-261`, `chapter-qa-repair.ts:308-313`; Test each.

**The bug.** `if (c.ttsEngine && eligibleEngines.includes(c.ttsEngine)) continue; c.ttsEngine = 'qwen';` force-moves a coqui-cloned character riding the book default onto qwen on any non-English book — even though coqui **is** eligible for ru/es/fr/de/zh/ja.

**Why "skip" is the wrong fix `[FAB-I3]`.** After Task 21 a cloned assignment writes **both** slots, so today's force actually renders correctly (as the qwen clone). Merely skipping leaves `ttsEngine` unset, so the character rides the **request's** default — which on a non-English book can be Kokoro/Gemini (this loop is fs-2's enforcement precisely because the account default can be anything). The pre-pass then fails the chapter `wrong-engine` where today it renders. Loud, never a substitution — but a real regression.

- [ ] **Step 1: Write the failing tests** (one per entry point) — a character whose cloned slot is on `coqui`, es book: `ttsEngine` ends as `'coqui'` (**not** unset, **not** `'qwen'`); with cloned slots on both and qwen the default, it stays `'qwen'`.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — **set** `ttsEngine` to the eligible clone-capable engine carrying the cloned slot, preferring the current default when both qualify. Extract the shared predicate into `clone-engines.ts` (Task 1) rather than triplicating. Note the splice/repair `clearMismatchedDesignedVoices` asymmetry in a comment; **do not** change it here — file a follow-up.
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): retarget a cloned coqui assignment instead of forcing Qwen`

---

# Phase 1 — Sidecar XTTS clone

## Task 7 — XTTS voices dir + latents paths

**Files:** Modify `server/tts-sidecar/main.py` (`CoquiEngine.__init__`), `server/src/tts/spawn-sidecar.ts:451`; Test `server/tts-sidecar/tests/test_xtts_clone_voice.py` (create), `spawn-sidecar.test.ts`
**Produces:** `CoquiEngine._voices_dir`, `_voice_paths(voice_id) -> (pt, json)`, `XTTS_KEY_PREFIX = "xtts-"`.

- [ ] **Step 1: Write the failing tests** — env honoured; `xtts-abc` → `xtts-abc.pt`/`.json`; lossy sanitisation stays **injective** (two different non-ASCII ids must not collide).
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — in `__init__` (the method begins at `main.py:1046`; the qwen analogue it mirrors is inside `QwenEngine.__init__`, which begins at `:2788`, at lines `:2877-2884`):
```python
        # Cloned-voice latents (fs-38 Wave 3c). Node points XTTS_VOICES_DIR at
        # <workspace>/voices/xtts; the local default keeps a bare sidecar run
        # working. Sibling convention to QWEN_VOICES_DIR.
        self._voices_dir = os.environ.get("XTTS_VOICES_DIR") or os.path.join(
            os.path.dirname(__file__), "voices", "xtts"
        )
```
Add `XTTS_KEY_PREFIX = "xtts-"` beside `FALLBACK_SPEAKER`; copy `QwenEngine._voice_paths` (`:3489-3503`) verbatim in behaviour (`re.sub` sanitise + sha1 suffix when lossy).
- [ ] **Step 4: Wire the spawn env** — `spawn-sidecar.ts:451`, after `QWEN_VOICES_DIR`: `XTTS_VOICES_DIR: join(WORKSPACE_ROOT, 'voices', 'xtts'),`; assert it beside `spawn-sidecar.test.ts:557`.
- [ ] **Step 5: Run** both → PASS. **Commit** — `feat(sidecar,server): xtts cloned-voice artifact dir + paths`

## Task 8 — `CoquiEngine._synth_lock` `[AC-I4][EX-5]`

**Files:** Modify `server/tts-sidecar/main.py` (`CoquiEngine`); Test `tests/test_concurrent_synthesis.py`

**Why before any clone code.** `CoquiEngine` is the only engine with no forward-serialization lock. `QwenEngine._synth_lock` is **created at `main.py:2821`** and used at `:3799-3800`; the non-obvious invariant — state is re-validated *inside* the lock because unloaders take it too — lives at the definition. 3c adds a second concurrent entry point into the same `self._tts`, and `derive-engine-artifact.ts:52-56` issues a bare `fetch` **outside** `sidecar.ts`'s per-engine `CountSemaphore(1)` (`:55-62,168,236`), so Node doesn't serialize it either.

**Scope is `_ensure_loaded` + `synthesize` only.** The derive-vs-synth interleaving test belongs to Task 9 — `clone_voice` does not exist yet, and a test that cannot be written at its own position is a test that gets faked `[EX-5]`.

- [ ] **Step 1: Write the failing test** — parallel coqui `/synthesize` calls each return their own PCM (existing contract), and a load racing a synth never observes a half-loaded model. Use `test_concurrent_synthesis.py`'s existing fake-engine harness — no model required.
- [ ] **Step 2: Run to verify it fails / is unguarded.**
- [ ] **Step 3: Implement** — `self._synth_lock = threading.Lock()`; hold across the model-mutating regions of `_ensure_loaded` and the synth forward. **Do not** hold it across Task 10's multi-second `torch.load`; that uses its own `_latents_lock` plus an epoch guard.
- [ ] **Step 4: Run** `pytest tests/ -k "concurrent or coqui"` → PASS. **Commit** — `fix(sidecar): serialize CoquiEngine model load and forward`

## Task 9 — `CoquiEngine.clone_voice` (config-faithful latents derive) `[AC-C3][AC-I1][AC-I3]`

**Files:** Modify `server/tts-sidecar/main.py`; Test `tests/test_xtts_clone_voice.py`
**Consumes:** Task 7's paths, `_atomic_torch_save` (`:202`), `_atomic_wav_save` (`:227`), `_float_audio_to_int16_le` (`:1638`).
**Produces:** `clone_voice(voice_id, ref_audio, ref_sr, audition_text, language=None, model="xtts_v2", device=None) -> SynthResult`.

**Two verified traps:**
1. **Call `get_conditioning_latents` with the model's own config settings.** `Xtts.synthesize` (`xtts.py:425-438`) builds `{key: kwargs.pop(key, self.config[key]) for key in ["gpt_cond_len","gpt_cond_chunk_len","max_ref_len","sound_norm_refs"]}`. Bare call ⇒ `gpt_cond_len=6` (`xtts.py:334`) ⇒ **lower-fidelity latents than the engine's own path.**
2. **It takes an audio PATH, not an array** (`xtts.py:331`). Write the normalised PCM to a temp WAV beside the artifact via `_atomic_wav_save`, derive, delete it.

- [ ] **Step 1: Write the failing tests** — `.pt` exists with both keys; audition returns non-empty 24 kHz PCM; temp WAV cleaned up **including on a derive exception**; metadata `.json` records `voiceId`/`clone`/`coquiVersion`/`modelId`; `get_conditioning_latents` receives **config-derived** kwargs; **and the derive-vs-synth interleaving** deferred from Task 8.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement.** Use a **function-local `import torch`** — never `self._torch`, which `unload()` nulls at `main.py:1235` `[AC-I3]`. Build settings from `tts_model.config`, falling back to signature defaults only when a key is absent. Persist `{"gpt_cond_latent", "speaker_embedding"}` via `_atomic_torch_save`, write the `.json`, produce the audition via Task 10's shared inference helper. **Do not seed `_latents_cache`** — Task 10 owns the epoch contract `[AC-M6]`.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(sidecar): derive XTTS conditioning latents at engine-config fidelity`

## Task 10 — Latents synth branch: fail loud, config-faithful, epoch-safe `[AC-C3][AC-C7][AC-I1][ADV-M1]`

**Files:** Modify `server/tts-sidecar/main.py` (`CoquiEngine.synthesize`, 1262-1314); Test `tests/test_xtts_clone_voice.py`
**Produces:** `_latents_cache`, `_latents_lock`, `_evict_epoch`, `_bump_evict_epoch(voice_id)` — **Task 11 wires the HTTP route to `_bump_evict_epoch`; this task's tests drive it directly** `[EX-6]`.

**Three requirements:**

**(a) Placement.** The branch **must** precede the `self._speakers` membership check (`:1275-1287`) — `xtts-<uuid>` is never in the manifest, so falling through substitutes `FALLBACK_SPEAKER`. `/synthesize` already maps `VoiceNotDesignedError` → 409 engine-agnostically (`:7306-7320`), so raising needs no route change. Batch synthesis is Qwen-only (`synthesise-chapter.ts:1774`), so this single branch covers the entire coqui render path — no bypass.

**(b) Config fidelity + text splitting `[AC-C3]`.** Bare `inference()` hardcodes `temperature=0.75`, `repetition_penalty=10.0`, `top_p=0.85` and defaults `enable_text_splitting=False` (`xtts.py:448-462`), while the shipped `tts()` path splits by default (`api.py:300`). `inference` then asserts `text_tokens.shape[-1] < gpt_max_text_tokens` (`xtts.py:516`, ~400) — **so a long sentence that renders fine on a stock coqui voice hard-crashes on a cloned one.** Mirror `Xtts.synthesize`'s `inference_settings`, pass `enable_text_splitting=True`, and mirror its `language in self.config.languages` check (`xtts.py:423`), which the low-level path skips.

**(c) Cache epoch `[AC-C7][ADV-M1]`.** `purgeCloneArtifacts` is files-first / evict-last (`purge-clone-artifacts.ts:8-11`). Naive `read cache → isfile → torch.load (seconds) → write cache` lets a load already past `isfile` repopulate **after** the evict landed — a revoked person renderable from RAM until restart. Capture `_evict_epoch[voice]` **before** `isfile`, re-compare under `_latents_lock` immediately **before** installing; refuse if it moved.

- [ ] **Step 1: Write the failing tests** — cloned key renders via `inference` with `substituted_from is None`; missing latents raises `VoiceNotDesignedError`; **placebo-proof:** on a miss, spy on **`_tts.tts`** (the real fallback path — asserting on `tts_model.*` can never observe a fallback `[AC-M1]`) and assert it was never called; a baked catalog voice still substitutes (regression); `inference` receives config settings and `enable_text_splitting=True`; **a long sentence renders** (the crash case); **interleaving:** bump the epoch mid-`torch.load` and assert nothing is cached and the next synth raises.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** `_load_voice_latents` + the branch. Function-local `import torch`; `torch.load(pt_path, map_location='cpu', weights_only=False)` — **`map_location` is required**: latents are saved as CUDA tensors on the deriving device (`xtts.py:361-370`), and placement can derive on cuda:1 and synth on cuda:0. `inference` moves both to `self.device` anyway (`xtts.py:505-506`), so `'cpu'` is free `[AC-I1]`.
- [ ] **Step 4: Run** `pytest tests/ -k "xtts or coqui"` → PASS (pre-existing `test_coqui_*` green). **Commit** — `feat(sidecar): render cloned XTTS voices from latents, fail loud on miss`

## Task 11 — `POST /xtts/clone-voice` + `POST /xtts/evict-voice` `[AC-C1][AC-C2][AC-I2][AC-I5][EX-6]`

**Files:** Modify `server/tts-sidecar/main.py` (beside `/qwen/clone-voice`, 6944-7040); Test `tests/test_xtts_clone_voice.py`

Merged because both are small routes in one file sharing one test file and one mental context `[EX]`.

**Clone contract:** raw s16le mono PCM body; headers `X-Sample-Rate` (required), `X-Voice-Id` (required, `xtts-<uuid>`), `X-Audition-Text` (base64, optional), `X-Language` (optional). Response: PCM, `X-Sample-Rate`, `X-Coqui-Version`, `X-Model-Id`. **No `X-Ref-Text`** — `get_conditioning_latents` is purely acoustic and takes no transcript.

**Three verified errors — do not reintroduce:**
- The poison helper is **`_mark_cuda_poisoned(reason)`** (`main.py:754`), not `_mark_poisoned`. The wrong name raises `NameError` *inside the except handler*, so a real CUDA OOM never marks the process poisoned `[AC-C1]`.
- **`import base64 as _b64` is function-local** in `qwen_clone_voice` (`:6953`); no module-scope `_b64` exists. A bare `_b64.b64decode` inside `try/except Exception` swallows the `NameError` and silently discards **every** audition text. Add the local import **and a test that `X-Audition-Text` reaches `clone_voice`** — without it this ships green `[AC-C2]`.
- Use **bare `TestClient(main.app)`** `[AC-I5]`.

**Footprint decision — settled here, not left to the implementer `[EX-12][AC-I2]`.** `FootprintTable._key` (`:2038-2051`) special-cases only qwen-1.7b, so a seeded `"coqui.clone"` is dead code. Extending `_key` to honour an `op` tag would immediately re-key **existing qwen clone reservations** to `qwen.clone`, absent from the seed table — a VRAM-admission behaviour change to shipped code. **Decision: do NOT extend `_key`; do NOT add the seed entry.** A coqui clone reserves against the plain `"coqui"` footprint, which is the right order of magnitude. Note it in plan 269 as a known simplification.

- [ ] **Step 1: Write the failing tests** — clone happy path (200 + headers + PCM); missing `X-Voice-Id` → 400; empty body → 400; **`X-Audition-Text` honoured**; a CUDA-poison-shaped exception → 503 `poisoned:true` (this is what catches the helper-name bug); evict drops a warm cache so the next synth raises. `server/tts-sidecar/conftest.py:26` sets `SEG_CAPACITY_ADMISSION=0`, so tests take the non-admission branch — **exercise the admission branch in at least one case**, and if `_capacity_admission_enabled()` reads env at import rather than call time, reload the module in that test rather than silently skipping it `[EX-12]`.
- [ ] **Step 2: Run to verify they fail** (404).
- [ ] **Step 3: Implement** — copy `/qwen/clone-voice`'s poison/restart fences, `_inflight_synth` accounting and `_placement.reservation` shape verbatim, changing only engine, header parsing and response headers. Evict pops `_latents_cache` **and** calls `_bump_evict_epoch` under `_latents_lock`. Note `unload()` early-returns when `self._tts is None` (`:1231`), so clear the cache **before** that guard `[AC-M4]`.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(sidecar): POST /xtts/clone-voice and /xtts/evict-voice`

---

# Phase 2 — Node artifact lifecycle

## Task 12 — xtts workspace paths

**Files:** Modify `server/src/workspace/paths.ts` (beside `qwenVoicesDir`, 289-325); Test `paths.test.ts`
**Produces:** `xttsVoicesDir()`, `xttsVoiceLatentsPath(name)`, `xttsVoiceSidecarPath(name)`.

- [ ] **Step 1: Write the failing tests** — the three resolve under `voices/xtts`; a traversal argument throws.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — copy the three qwen helpers substituting `'xtts'`; keep `sanitizeIdSegment(safeSegment(name))` + `assertContained` on every path.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): xtts voice artifact path helpers`

## Task 13 — `purgeCloneArtifacts` erases xtts artifacts and samples `[ADV-M2][FAB-M2]`

**Files:** Modify `server/src/workspace/purge-clone-artifacts.ts` (the `TODO(3c)` at :76); Test `purge-clone-artifacts.test.ts`
**Consumes:** Task 2's name-based sample sweep. **Convention (settled, so Task 24 matches): a coqui audition caches under scope `xtts-<uuid>`** `[FAB-M2]`.

Ordered before anything that creates an xtts artifact in anger, so no window exists where a revoked person's latents survive a purge.

- [ ] **Step 1: Write the failing tests** — latents + sidecar json erased; the `xtts-<uuid>` sample scope erased; an unlink failure is **reported in `failed[]`**, not swallowed.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — push `xttsVoiceLatentsPath`/`xttsVoiceSidecarPath` into the same `files` array (sharing `unlinkTracked`'s accounting), add `purgeVoiceSamples(\`xtts-${uuid}\`)`, extend the evict loop to `/xtts/evict-voice`. **State explicitly** that evict is best-effort while file erasure must be reliable — an unreachable sidecar leaves latents resident for the process lifetime, and unlike the qwen prompt cache there is no TTL reclaim.
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): erase xtts clone artifacts and samples on revoke and delete`

## Task 14 — Serialize the voice-library **read-modify-write**, not just the write `[AC-C6][EX-1][FAB-I1]`

**Files:** Modify `server/src/workspace/voice-library.ts`; **convert all five call sites**: `server/src/tts/clone-voice-resolver.ts:263-337`, `:353-360`, **`:509-517` (the designed self-heal — it writes `engines.xtts` after D-B, so it is now part of the very cross-slot race this task exists to close `[DELTA-concurrency]`)**, the revoke route's read→stamp→write in `server/src/routes/voice-library.ts`, and `purge-clone-artifacts.ts:93-96` (`master: undefined`). Test `voice-library.test.ts`.

**Why required, not deferred.** In 3b2 two concurrent resolvers wrote the **same** `engines.qwen` slot, so a lost update was idempotent — which is why #1826 was acceptable to defer. 3c writes a **second, independent slot**, and concurrent multi-book generation is a first-class invariant. Interleaving: A and B both read `{qwen:stale}`; A writes `{qwen:stale, xtts:ready}`; B writes `{qwen:ready}` from **its** stale snapshot → **the xtts slot is silently erased.**

**The critical section is read-modify-write as a unit.** Two reviewers independently found that a mutex around `writeEntry` alone only *orders* the writes — B still persists its stale snapshot and still erases the slot `[EX-1][FAB-I1]`. Provide `updateEntry(uuid, mutate)` holding a per-uuid lock across **fresh read + mutate + write**, and convert every call site above. Done properly this also closes the millisecond revoke-flag window documented at `clone-voice-resolver.ts:246-256`.

- [ ] **Step 1: Write the failing test** — two interleaved read→modify→write cycles touching **different** engine slots; assert both survive. (This test passes against a write-only mutex **only if** production routes through the locked helper — hence converting the call sites is part of the task, not a follow-up.)
- [ ] **Step 2: Run to verify it fails** (one slot lost).
- [ ] **Step 3: Implement.** Cross-process (two servers, one workspace) stays out of scope — say so and reference #1826.
- [ ] **Step 4: Run** `npm --prefix server run test -- voice-library clone-voice-resolver` → PASS. **Commit** — `fix(server): serialize voice-library read-modify-write across engine slots`

## Task 15 — Engine-parametric `deriveEngineArtifact`

**Files:** Modify `server/src/tts/derive-engine-artifact.ts` (the qwen-only throw at :35-38); Test `derive-engine-artifact.test.ts`
**Consumes:** `CloneEngine` from `tts/clone-engines.ts` (Task 1).
**Produces:**
```ts
export interface DeriveArtifactResult {
  previewPcm: Buffer; sampleRate: number;
  baseModel?: string;                        // qwen only
  coquiVersion?: string; modelId?: string;   // coqui only
}
export async function deriveEngineArtifact(
  voiceUuid: string, engine: CloneEngine, input: DeriveArtifactInput,
  opts?: { signal?: AbortSignal; sidecarUrl?: string },
): Promise<DeriveArtifactResult>;
```
`input.refText` becomes optional; validate inside the qwen branch only.

**⚠ Making `baseModel` optional breaks two live consumers `[DELTA-I4]`.** It is required today (`:26`) and read unguarded at `clone-voice-resolver.ts:487` and `:515`. Left alone, `:515` writes `baseModel: undefined` onto `engines.qwen`, which `withComputedStaleness` (`routes/voice-library.ts:325`) then reads as "never stale" — a silent regression to a shipped self-heal. Guard both call sites in this task, and pin the qwen stamp with a test.

**Also `[DELTA-M1]`:** `readDesignedMasterPcmDefault` returns `null` when the sidecar manifest has no `refText` (`synthesise-chapter.ts:995-996`). A coqui derive needs no transcript, so that gate must not block the coqui arm — split it.

- [ ] **Step 1: Write the failing tests** — posts `/xtts/clone-voice` with `X-Voice-Id: xtts-u1` and **no** `X-Ref-Text`; maps `coquiVersion`/`modelId`, leaves `baseModel` undefined; **reserves capacity against `engine: 'coqui'`** (the literal `'qwen'` at `:55` would reserve the wrong footprint and mis-place on a multi-GPU box); qwen unchanged; a non-clone-capable engine → 400.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — one shared POST/response helper; vary target path, key prefix (via `cloneStorageKey`), `X-Ref-Text` presence, the capacity tag, and the response-header mapping.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): derive XTTS clone artifacts via deriveEngineArtifact`

---

# Phase 3 — Routing and resolution

## Task 16 — `pickVoiceForEngine` resolves a cloned coqui voice `[ADV-H3]`

**Files:** Modify `server/src/tts/voice-mapping.ts:317-366`; Test `voice-mapping.test.ts`

**Policy note (D-B/D-F).** Both a `cloned` and a `designed` coqui slot resolve to `xtts-<uuid>` — the resolution is identical. The provenance difference is enforced **downstream**, in the pre-pass (Task 22a), which is what decides whether a missing artifact fails the chapter or quietly reverts the group to the catalogue. Do **not** encode the failure policy here: this function is pure and synchronous and cannot know whether the artifact exists.

**⚠ This makes a coqui `libraryUuid` sticky `[DELTA-I5]`.** `applyOverrideToCastFiles` spreads the slot and replaces only `name` (`routes/voices.ts:781`). That is harmless today because `pickVoiceForEngine` returns `slotName` — but once the libraryUuid branch is inserted *before* that lookup, a user who assigns a library voice on coqui and then picks a stock Coqui voice from the cast picker **still renders `xtts-<uuid>`; their pick is silently ignored.** Task 4 deliberately left `:781` alone as "the well-behaved spread"; it is no longer well-behaved. Clear `libraryUuid`/`provenance` when an explicit override names a catalogue voice, and test it.

- [ ] **Step 1: Write the failing tests** — a **cloned** coqui slot → `xtts-u1`; a **designed** coqui slot carrying a `libraryUuid` → `xtts-u1` as well; neither returns a `COQUI_PROFILE_VOICES` member; a slot with a `name` but **no** `libraryUuid` is unchanged; an `imported` slot is unchanged; **a slot with `libraryUuid` but no `provenance` is unchanged** (legacy drift `[DELTA-M2]`); **an explicit catalogue override after a library assign wins** (`[DELTA-I5]`).
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — insert before the generic `slotName` lookup at `:352` (beating it and the legacy singular fallback at `:354-360`), gated on `libraryUuid` present **and** provenance in `{cloned, designed}` via `clonedSlotForEngine`.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): resolve cloned voices on the coqui engine`

## Task 17 — A substituted cloned voice is fatal `[EX-8]`

**Files:** Modify `server/src/tts/sidecar.ts:188-195`; Test `sidecar.test.ts`

- [ ] **Step 1: Write the failing tests** — substitution of an **`xtts-`** prefixed voice throws; substitution of a catalog name still only warns; **substitution of a `qwen-` prefixed DESIGNED voice still only warns** (gating on `qwen-` would change designed-voice behaviour, which the Global Constraint forbids).
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — gate on the `xtts-` prefix only. Comment honestly as defence-in-depth: Task 10 makes it unreachable from the current sidecar; it exists so a future regression cannot silently reintroduce substitution.
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): treat substitution of a cloned coqui voice as fatal`

## Task 18 — Engine-parametric resolver `[AC-I13][EX-4]`

**Files:** Modify `server/src/tts/clone-voice-resolver.ts`, **`server/src/routes/voice-library.ts:323-327`** (`withComputedStaleness` — note **routes/**, not workspace/); Test `clone-voice-resolver.test.ts`, `voice-library.test.ts`

- `ClassifyInput`/`ClonedVoiceRequest` gain `engine: CloneEngine`; the classifier reads `entry.engines[manifestSlotFor(engine)]`.
- **Rule order unchanged and load-bearing:** revoked → wrongEngine → engineUnavailable → persisted `failed` → needs-derive → healthy.
- **Staleness differs per engine:** qwen compares `baseModel`, coqui `coquiVersion`. Rename the comparand `currentBaseModel` → `currentArtifactVersion`.
- **`withComputedStaleness` must recompute the xtts slot** — it compares only `engines.qwen.baseModel` today, so a `coqui-tts` bump leaves the card reading "ready" forever while only the pre-pass notices `[AC-I13]`.

- [ ] **Step 1: Write the failing tests** — the classifier table with a coqui column; **sibling preservation both ways** (the existing test at `:322-332` pins only one direction); a `coquiVersion` mismatch → repairable; placebo-proof: **Broken ⇒ raises AND no derive invoked** on the coqui path.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — thread `engine`; per-engine storage key (`cloneStorageKey`) and existence check; pass `engine` to `deriveEngineArtifact`; write the matching slot via Task 14's `updateEntry`. `guardPostDeriveWrite` and `isTransientDeriveFailure` are already engine-agnostic — **do not touch them**. Make `UnresolvableClonedVoiceError`'s message engine-aware.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): classify and repair cloned voices per engine`

## Task 19 — A Coqui availability signal `[AC-I11][EX-3]`

**Files:** Modify `server/src/workspace/user-settings.ts` (beside `getLastKnownQwenInstallState`, :554-568); update the five `qwenUnavailable` call sites (`synthesise-chapter.ts:525,1045`; `chapter-splice.ts:283`; `chapter-qa-repair.ts:315`; `generation.ts:858`). Test both sides.

**The value does not exist** — there is no Coqui counterpart, and Task 20 hard-depends on it.

**Source decision, settled here `[EX-3]`:** mirror the qwen pattern — the singleton is fed by the disk probe (`server/src/index.ts:274`) and the health poll (`server/src/routes/sidecar-health.ts:262`). Use **`server/src/tts/coqui-install-detect.ts`'s `CoquiInstallState`** as the probe source and mirror it into the same per-engine store, so both engines share one shape and one refresh path. Do **not** reach into `models-inventory.ts`'s `deriveEngineHealth` (a route-scoped view with a different lifecycle).

- [ ] **Step 1: Write the failing test** — a coqui-uninstalled state is observable through the same shape as the qwen one; the qwen call sites behave identically to before.
- [ ] **Step 2: Run to verify it fails.**
- [ ] **Step 3: Implement** — generalise to a per-engine lookup rather than duplicating a bespoke second function.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): per-engine install-state signal for the clone resolver`

## Task 20 — Pre-pass: union filter over every clone-capable slot `[ADV-C1]`

**Files:** Modify `server/src/tts/synthesise-chapter.ts:1205-1245`; Test `synthesise-chapter-cloned-resolver.test.ts`

**The single load-bearing line.** The adversarial pass's closing verdict is that the whole invariant rests on this filter being a **union**, not "the routed engine's slot is cloned":
```ts
const isCloned = characterHasClonedSlot(c);   // tts/clone-engines.ts
```
Emit one request per character for the **routed** engine, setting `wrongEngine` when the routed engine has no cloned slot and `engineUnavailable` from **that engine's** signal (Task 19), not `qwenUnavailable` unconditionally. Keep the `inChapterCharacterIds`/`rendersNarrator` gate exactly as-is — it was a 3b2 review finding.

- [ ] **Step 1: Write the failing tests** — a cloned coqui slot is validated; a coqui-routed clone with no coqui slot fails loud (not a catalog voice); **one test per Phase-0 upstream mutator** (Tasks 3/4/5/6) proving the marker survives to the pre-pass; a chapter with no clones runs the pre-pass zero times.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.**
- [ ] **Step 4: Run** `cd server && npx vitest run src/tts/` → PASS. **Commit** — `feat(server): pre-pass validates cloned voices on every clone-capable engine`

## Task 20a — Designed voices derive on coqui, and fail SOFT `[D-B][D-F]`

**Files:** Modify `server/src/tts/clone-voice-resolver.ts` (`resolveDesignedVoicesForChapter`, 439-542), `server/src/tts/synthesise-chapter.ts` (the designed pre-pass call site); Test `clone-voice-resolver.test.ts`, `synthesise-chapter-cloned-resolver.test.ts`

**Why this is its own task.** `resolveDesignedVoicesForChapter` is a separate orchestrator from the cloned one and is qwen-hardcoded in the same three places (`:453` `ptExists('qwen-…')`, `:461-471` literal `'qwen'`, `:513-516` the `engines.qwen` write). D-B needs it engine-parametric — but with the **opposite failure policy** from Task 20, which is precisely why it must not be folded into that task. One reviewer looking at one diff must be able to see one policy.

**Selection set — D-G, and the part most likely to be got wrong.** The existing designed filter is `.filter((c) => !qwenUnavailable && routeFor(c).engine === 'qwen')` (`synthesise-chapter.ts:1260`). **Both clauses must go for the coqui arm.** Select on *"the character has an xtts slot"*, independent of routed engine and of `qwenUnavailable` — otherwise `[DELTA-C2]`'s three vectors each produce a hard 409 where the chapter renders today:
1. **fs-60's Qwen→Coqui fallback** (`:1175-1185`) reroutes a *qwen-routed* character to coqui and calls `pickVoiceForEngine('coqui', …)` at `:1180`. Routed engine is qwen, so a `routeFor`-based filter never validates it.
2. **`qwenUnavailable`** skips the designed pre-pass entirely via the `!qwenUnavailable` clause — so with Qwen uninstalled, *every* designed character with an xtts slot fails.
3. **`clearMismatchedDesignedVoices`** deletes only `overrideTtsVoices.qwen` (`verify-designed-voice-language.ts:48`), leaving the xtts slot behind — which is exactly how `voiceName` goes empty and vector 1 fires.

**The contract:**
- Designed + has an xtts slot + retained clip on disk + artifact missing ⇒ **derive**.
- Designed + derive **fails**, *or* **no retained clip** ⇒ **do not raise. Remove the coqui slot** for this chapter so `pickVoiceForEngine` falls through to `COQUI_PROFILE_VOICES`. (The earlier "never touch the slot" wording was the specific sentence that made `[DELTA-C2]` unfixable — a slot left in place with no artifact behind it *is* the 409.)
- `UnresolvableClonedVoiceError` must **never** be thrown from this path.
- **Re-check provenance inside the resolver** (it reads the entry anyway at `:509`): `entry.provenance !== 'designed' ⇒ skip`. Today a cloned voice cannot reach the soft path, but only because its uuid yields no `__master.wav` — a filename convention two modules away. Make the invariant explicit rather than incidental `[DELTA-verified]`.
- **Staleness:** presence-only for qwen — `if (ptExists) continue;` (`:454-455`) is deliberate and documented at `:375-383` ("auto-re-deriving on every base-model bump has drift/quality implications nobody has designed"). Add `coquiVersion` staleness **on the coqui arm only**; do not let a shared path start re-deriving qwen designed voices `[DELTA-I3]`.
- **Stamp:** a designed coqui derive writes `engines.xtts` with `coquiVersion`/`modelId` — never `baseModel` `[DELTA-I4]`.

**Mechanism (specified, not left to invention `[DELTA-I1]`).** `DesignedVoiceRequest` is `{characterName, libraryUuid}` (`clone-voice-resolver.ts:399-402`) and the resolver holds no `CastCharacter`. **Return `Promise<{ softFailedUuids: string[] }>` and have the call site — which owns `cast` — delete the slots.** Keep mutation out of a module built on injected deps.

**Scope the drop to the chapter, not the run `[DELTA-I2]`.** `cast.characters` is read once (`generation.ts:757`) and reused for every chapter (`:1584`, `:1825`). Dropping in place means one transient failure in chapter 1 silently downgrades that voice for the **whole book** — and a resumed run re-reads cast.json and likely succeeds, so the user hears the voice change across the resume boundary. Apply the deletion to a per-chapter copy.

- [ ] **Step 1: Write the failing tests**
  - designed + clip + missing latents ⇒ derives, renders as `xtts-<uuid>`
  - **designed + derive throws ⇒ chapter completes, group renders a `COQUI_PROFILE_VOICES` name, no error surfaces** (the load-bearing case)
  - **one test per `[DELTA-C2]` vector** — fs-60 reroute, `qwenUnavailable`, post-`clearMismatchedDesignedVoices` — each asserting the chapter completes on a catalogue voice rather than 409ing
  - designed + no retained clip ⇒ no derive attempted, **slot removed**, renders exactly as on `main`
  - **placebo-proof:** `UnresolvableClonedVoiceError` is never constructed on this path, **and** a *cloned* voice in the same chapter still fails loud — the two policies coexisting is what can silently regress
  - a soft failure in chapter 1 does **not** persist into chapter 2
  - qwen designed voices are **not** re-derived on a base-model bump (pins `[DELTA-I3]`)
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — thread `engine` through reusing `cloneStorageKey`/`manifestSlotFor`/`updateEntry`; wrap the derive in the fail-soft handler. Do **not** share a code path with Task 20's fail-loud handler — the duplication is deliberate and load-bearing. **Add this function's write (`:509-517`) to Task 14's serialized call sites** — it now writes `engines.xtts` alongside the cloned path's `engines.qwen`, which is precisely Task 14's cross-slot lost update `[DELTA-concurrency]`.
- [ ] **Step 4: Run** `cd server && npx vitest run src/tts/` → PASS. **Commit** — `feat(server): derive designed voices on coqui with catalogue fallback`

## Task 21 — `applyQwenFallback` becomes engine-neutral

**Files:** Modify `server/src/tts/synthesise-chapter.ts:1125-1192`; Test `synthesise-chapter-cloned-exemption.test.ts`

Gated on `route.engine === 'qwen'` (`:1157`), so a coqui-routed clone gets **no exemption at all**. A cloned character must never be rerouted on any engine — including by fs-60's Qwen→Coqui branch, and including when coqui itself is unavailable. Preserve the existing "unreachable backstop" contract that file documents.

- [ ] **Step 1: Write the failing tests** — coqui unavailable + cloned → raises, never reroutes; fs-60's Qwen→Coqui branch still skips cloned characters; the existing qwen-side exemption tests stay green.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.**
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): never reroute a cloned character on any engine`

## Task 22 — Engine-partitioned derive with a stated end state `[AC-C4][FAB-I2]`

**Files:** Modify `server/src/tts/synthesise-chapter.ts` (pre-pass + `synthGroupsSerialized` 1923-1945 and its call sites 1950/2016/2125); Test as above.

**The hazard.** `evictQwenForCoquiPhase` (`:880-890`) is reachable **only** from `synthGroupsSerialized` — strictly *after* the pre-pass. `generation.ts:1538` warms Qwen before `synthesiseChapter`, so a coqui derive in the pre-pass calls `_ensure_loaded` with **no cross-engine admission** — the 8 GB co-residency fs-60 exists to prevent (rationale at `:868-879`).

**And the mirror `[FAB-I2]`:** "qwen derives → evict qwen → coqui derives" leaves XTTS (~3.5 GB) resident when the chapter's **qwen render phase** starts and reloads Qwen — the same defect mirrored.

**⚠ Ordering alone does not fix it `[DELTA-C3]`.** Qwen is **already resident** when the pre-pass starts: `generation.ts:1538` awaits `ensureReadyOrPause(engine, …)` before `synthesiseChapter` is called at `:1582`. So the first coqui derive still loads XTTS on top of a warm Qwen. Ordering fixes only the pre-pass's **end** state — which is what the previous draft claimed and tested — while leaving the window it *opens* with untouched. That is the same shape as the `[EX+FAB]` finding about Task 13's non-fix.

**D-B multiplies the exposure.** Designed voices are the common case: a chapter with 8–15 in-chapter designed speakers now issues 8–15 serial coqui derives where it previously issued zero, each a bare `fetch` outside `sidecar.ts`'s per-engine semaphore. Worst case on the 8 GB card is Qwen-1.7B-tier + XTTS co-resident — the plan-108 OOM class.

**Decision:** **evict Qwen BEFORE the coqui derive block**, then derive coqui, then evict coqui, then derive qwen. Let `generation.ts`'s warm reload pay for itself once. The invariant to state in the comment and pin in a test is: *"the pre-pass ends with only Qwen resident, **and never has both resident at any point**."*

**Gate the trailing evict `[DELTA-I7]`.** `synthGroupsSerialized` short-circuits to `synthGroupsBatched` unless the chapter genuinely mixes qwen and coqui (`:1928`). On a Coqui book "all characters on coqui" is the normal shape — evicting XTTS after the derives would unload it immediately before every group reloads it, and Task 11 clears `_latents_cache` on unload so every `.pt` is re-read too. Only evict coqui when the chapter actually has qwen groups.

**Not closed by this task:** two books both in a coqui-derive pre-pass compound on the same card. Node serializes nothing across books; the sidecar's placement controller is the only backstop. Record it in plan 269.

- [ ] **Step 1: Write the failing tests** — a mixed chapter evicts qwen **before** the first coqui derive; **both engines are never simultaneously resident** at any point in the pre-pass (assert on the load/evict call sequence, not just the end state); a coqui-only chapter does **not** evict coqui afterwards; a qwen-only chapter issues no coqui load at all.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.**
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): partition cloned-voice derives by engine to preserve VRAM serialization`

## Task 23 — The narrator stub must resolve its real cast row `[ADV-H5]`

**Files:** Modify `server/src/tts/synthesise-chapter.ts:1285-1286`, `generation.ts:1599`, `chapter-splice.ts:323`, `chapter-qa-repair.ts:450`; Test as above.

`castById.get(narratorCharacterId) ?? { id, name: 'Narrator' }` with `narratorCharacterId` hardcoded `'narrator'` at all three entry points, while `voice-mapping.ts:411-414` recognises `'char-narrator'` as a real narrator id. On such a book the title beat and the orphaned-`characterId` net resolve from a stub with **no overrides** → catalog voice, and `cast.filter` can't match it so no validation fires.

- [ ] **Step 1: Write the failing test** — a `char-narrator` book resolves the narrator's real overrides and validates a cloned narrator.
- [ ] **Step 2: Run to verify it fails.** **Step 3: Implement.**
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(server): resolve the narrator's real cast row when one exists`

## Task 24 — Assign to a clone-capable engine, both slots, provenance-gated `[AC-C5][AC-I7]`

**Files:** Modify `server/src/routes/voice-library.ts` (guard 884-906; slot write 917-930); Test **`server/src/routes/voice-library.test.ts`** — extend the existing describe blocks at `:612`, `:794` (readiness gate), `:836` (wrong-engine), `:893`. **Do not create a new test file** `[EX-9]`.

- [ ] **Step 1: Write the failing tests** — a cloned voice assigns to a coqui-routed character (200) and **both** slots are written with engine-correct names; a **designed entry WITH a `master`** also writes both slots (D-B); a **designed entry with NO `master`** writes **only** the qwen slot (D-E/D-F — this is the guard that keeps legacy entries byte-for-byte unchanged); an `imported` entry writes only the qwen slot; kokoro still 409s; the 409 names both engines *and* keeps the character-caused-vs-book-caused distinction; `variants` cleared on both slots. **New fixtures must set `engines.qwen.status='ready'`** or the readiness guard at `:834` 409s them for the wrong reason and the test reads as a passing "still 409s" case `[AC-I7]`.
- [ ] **Step 2: Run to verify they fail.**
- [ ] **Step 3: Implement** — widen the guard to `CLONE_CAPABLE_ENGINES.has(routedEngine)`; gate the both-slots write on `provenance === 'cloned' || (provenance === 'designed' && await hasRetainedDesignedClip(voiceUuid))`, where that helper stats `qwenVoiceWavPath(\`qwen-${voiceUuid}__master\`)`. **Not `entry.master`** — no designed entry ever has it `[DELTA-C1]`. This makes `/assign` do async I/O it does not do today; that is intended and should be noted in the route comment. The check is **point-in-time**: a later re-design or purge can invalidate it, which is exactly why Task 20a must remove an unbacked slot at render time rather than trusting this gate `[DELTA-C2]`. **The wrong-engine 409 stays scoped to `cloned`** — a designed voice has always been free to route anywhere.
- [ ] **Step 4: Run** → PASS. **Commit** — `feat(server): assign cloned voices to Qwen or Coqui XTTS v2`

---

# Phase 4 — Surfaces

## Task 25 — Client picker + cast view audition `[ADV-H1][AC-I6]`

**Files:** Modify `src/lib/tts-voice-mapping.ts:319-345`, `src/views/cast.tsx:546-556` **and `:514-520`** (a second qwen-only gate `[EX-14]`); Test alongside.

`resolveTtsVoiceForCharacter` does **no** `overrideTtsVoices` read for any non-qwen engine — it goes straight to `options[stableHash(id) % options.length]`. `cast.tsx` injects overrides only when `effectiveEngine === 'qwen'`. So the cast row shows, and Play sounds, a stock speaker for a cloned-on-coqui character — while the chapter renders the clone. The audition is the one surface a user uses to verify identity.

- [ ] **Step 1: Write the failing tests** — the client picker returns `xtts-<uuid>` for a cloned coqui slot; `playSampleFor` passes `overrideTtsVoices` for **every** engine (both gates).
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement** — mirror Task 16's branch client-side.
- [ ] **Step 4: Run** `npm run test` (full frontend — shared components reach distant view tests) → PASS. **Commit** — `fix(frontend): audition a cloned voice on the engine it will render on`

## Task 26 — Profile drawer optimistic state `[AC-I12][EX-14]`

**Files:** Modify `src/modals/profile-drawer.tsx:391-394`, `src/store/cast-slice.ts:130`; Test alongside.

Hardcodes `` `qwen-${entry.voiceUuid}` `` and dispatches `setQwenOverrideName`, whose reducer is hardwired `otv.qwen ??= …` — it **cannot** be reused for xtts. Generalise the reducer (or add an engine-parameterised sibling); do not leave the drawer writing the wrong slot.

- [ ] **Step 1: Write the failing tests** — the optimistic write targets the routed engine's slot; the reducer handles both.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.** **Step 4: Run** → PASS. **Commit** — `fix(frontend): profile drawer assigns to the routed engine's slot`

## Task 27 — Engine-aware library sample route `[ADV-H2][FAB-M3]`

**Files:** Modify `openapi.yaml` (the sample request — **OpenAPI-first, then `npm run openapi:types`**), `server/src/routes/voice-library.ts:428-439`; Test `voice-library.test.ts`.

`:433-434` hardcodes `voiceName='qwen-<uuid>'`, `modelKey='qwen3-tts-0.6b'`, and `:439` hardcodes `cacheScope = \`qwen-${voiceUuid}\`` — so Task 28's "Coqui ✓" chip would sit on a card whose Play button auditions the **Qwen** artifact, and 409s if qwen is stale while xtts is ready. `:439` is also why Task 13's `xtts-<uuid>` sample purge would otherwise be dead code `[EX-7]`. Keep the existing consent check at `:428`.

- [ ] **Step 1: Write the failing tests** — the route auditions the requested engine; **`cacheScope` becomes the engine's storage key** (`xtts-<uuid>`, matching Task 13's convention); with lazy derive, an un-derived engine returns a clear "not prepared yet" rather than an opaque 409.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.**
- [ ] **Step 4: Run** `npm --prefix server run test -- voice-library` → PASS. **Commit** — `feat(server): audition a library voice on a chosen engine`

## Task 28 — Per-engine cloned-voice state `[AC-I8][AC-I9][EX-16]`

**Files:** Modify `src/components/voices/voice-library-card.tsx:31-43,50-60,152-158`; Test `voice-library-card.clone.test.tsx`

- **Labels use `Coqui`, not `XTTS`** `[AC-I8]`. `ENGINE_STATUS_LABEL`/`ENGINE_STATUS_COLOR` (`:31-43`) are `Record<QwenStatus, …>` with literal `'Qwen ✓'` values — **they must become per-engine maps**, which the previous draft failed to say `[EX-16]`.
- The testid pattern `voice-library-engine-qwen-${uuid}` **already exists at `:154`** — extend it for the second engine; do **not** rename existing working code `[EX-16]`.
- `deriveClonedVoiceState` (`:52-59`) reads `engines.qwen` **and** gates on `consent.revokedAt` and `!master` — preserve both. Worst-state-wins across engines is **new** behaviour `[AC-I9]`. Handle the consequence: a transient coqui failure persisting `engines.xtts.status='failed'` must **not** brand the card permanently broken on a Qwen-only book.

**⚠ D-B widens this beyond cloned cards `[DELTA-I6]`.** `deriveClonedVoiceState` is called only inside the `provenance === 'cloned'` branch (`voice-library-card.tsx:308`, guarded at `:235`/`:297`), but designed cards now carry `engines.xtts` too. **Do not reuse `deriveClonedVoiceState` for designed cards** — its `!entry.master ⇒ 'broken'` rule at `:53` keys on the very field `[DELTA-C1]` shows designed entries never have, so every designed card would read broken. Designed cards get engine chips only, with no broken/repairable verdict (their failure mode is a silent catalogue fallback, not a defect). Tasks 29 and 30 likewise only exercise the cloned assign — extend their fixtures to cover a designed entry with an xtts slot.

- [ ] **Step 1: Write the failing tests** — a chip per populated engine with the right labels; only populated engines shown; a failed xtts does not brand a qwen-only book's card broken; revoked/missing-master still read broken **for cloned cards**; **a designed card with an xtts slot shows both chips and no broken verdict**.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement.**
- [ ] **Step 4: Run** the full frontend suite → PASS. **Commit** — `feat(frontend): per-engine readiness chips on cloned voice cards`

## Task 29 — Make the mock layer able to fail `[ADV-C4][AC-I10][EX-15]`

**Files:** Modify `src/lib/api.ts:9740-9748` (`mockAssignLibraryVoice`), `src/mocks/voice-library.ts:67-119`, **`src/modals/profile-drawer.tsx`** (surface the rejection); Test alongside.

`mockAssignLibraryVoice` returns `{ updated: 1 }` — no slot write, no `libraryUuid`/`provenance`, and **none** of the real route's three 409s (revoked `:827`, not-ready `:834`, wrong-engine `:884`). Both cloned fixtures carry `engines: {}`, which the real route rejects at `:834`. So Task 30's e2e would assert "the assignment succeeds" against a stub that cannot say no — it passes today for kokoro too.

**And the drawer mutates client-side regardless of the call's outcome (`profile-drawer.tsx:391-394`)** — so making the mock throw is not enough; the UI must surface the rejection or Task 30's negative case has no path to pass `[EX-15]`.

- [ ] **Step 1: Write the failing tests** — the mock 409s a revoked entry, a not-ready entry, and a non-clone-capable engine; a successful assign writes both slots; **the drawer surfaces a rejection instead of optimistically showing success**.
- [ ] **Step 2: Run to verify they fail.** **Step 3: Implement** — mirror the real route's guards and slot write; add an `engines.xtts` fixture; give the cloned fixtures a `ready` qwen slot.
- [ ] **Step 4: Run** → PASS. **Commit** — `fix(frontend): mock assign mirrors the real voice-library guards`

## Task 30 — e2e: cloned voice on Coqui XTTS v2

**Files:** `e2e/voice-library.spec.ts`

The spec **already has** an assign-a-cloned-voice step at `:228-281` (`selectOption('qwen')` at `:275`) `[EX-15]`. Add the coqui case **after** it, at the very end of the serial scenario, so the four hardcoded `[data-testid^="voice-library-card-"]` counts (6/7/8/9 at `:99/:122/:225/:254`) don't shift.

- [ ] **Step 1: Add the positive case** (assign on coqui succeeds, both engine chips shown) **and the negative** (a non-clone-capable engine is rejected and the UI says so) — the latter is only testable because Task 29 landed.
- [ ] **Step 2: Run** `npx playwright test e2e/voice-library.spec.ts --project=chromium` → PASS. **Actually run it; report SKIP/failure honestly.**
- [ ] **Step 3: Commit** — `test(e2e): cloned voice assigns on Coqui XTTS v2`

---

# Phase 5 — Gates and docs

## Task 31 — A golden gate that can actually run `[ADV-M3][EX-17]`

**Files:** Modify `server/tts-sidecar/run-golden-tests.ps1`, `tests/golden/test_cross_engine_sanity.py`

The gate probes Kokoro by `Test-Path` on weight files and Qwen by `import qwen_tts, torch`, then exits 0 — so on a Coqui-only box `test_coqui_sanity` never runs even with `GOLDEN_COQUI=1`.

- [ ] **Step 1: Add a `$coquiPresent` probe** — **method: `import TTS`** (matching the Qwen import-probe style; the XTTS weights are fetched lazily by the library on first load, so a weights-path test would be wrong) `[EX-17]`.
- [ ] **Step 2: Add `test_xtts_clone_sanity`** gated behind `GOLDEN_XTTS_CLONE=<voiceUuid>` (mirroring `GOLDEN_QWEN_VOICE`), following `_assert_sane`'s **loose-check** pattern — format/RMS/duration. Clone synthesis is stochastic; no exact-length baseline. `_assert_sane` already asserts `substituted_from is None` — don't duplicate. **Include a long-sentence case** — that is what would have caught the `enable_text_splitting` crash.
- [ ] **Step 2b: Add `test_xtts_designed_sanity` `[D-B]`** — the same loose checks against latents derived from a **designed** voice's synthetic Qwen calibration clip, gated behind `GOLDEN_XTTS_DESIGNED=<voiceUuid>`. **This is the check the spec called for when it labelled synthetic-clip→latents "quality-unvalidated" (§2.3), and it is now a delivery gate rather than a deferral rationale.** Format-level assertions can only prove it is not broken, not that it sounds right — so Step 3 records the numbers and the on-box acceptance sheet carries the listening test that actually settles it.
- [ ] **Step 3: Run** `npm run test:golden-audio:sidecar` and **record whether it ran or SKIPped, with the reason.**
- [ ] **Step 4: Commit** — `test(sidecar): golden gate covers Coqui, adds an XTTS clone sanity check`

## Task 32 — Docs, release notes, follow-ups `[EX-19]`

**Files:** Create `docs/features/269-fs38-wave3c-xtts.md` (confirm 269 is still free — rescan in-flight worktrees); modify `docs/features/INDEX.md`, `267-*.md`, `268-*.md`, `docs/testing/fs38-wave3-onbox-acceptance.md`, `docs/release-notes-next.md` (before the `---` at `:111`), `RELEASE_NOTES.md` (before `# Castwright 1.14.0` at `:42`), `docs/BACKLOG.md`

- [ ] **Step 1: Write plan 269** — status `active` (live-GPU acceptance owed). Invariants led by never-substitute-on-any-engine, **marker-survives-every-upstream-mutator**, and **the two failure policies (D-F): cloned fails loud, designed falls back**. Known limitations **must** include: **the synthetic-clip→latents quality claim is delivered but validated only at format level in CI — the listening test is on-box (D-B)**; **already-rendered chapter audio is not erased on revoke** — a deliberate product decision, so "erasure is total" is qualified `[FAB-M4]`; cross-process entry writes still unserialized (#1826); the coqui clone reserving against the plain `coqui` footprint (Task 11's settled simplification); the splice/repair `clearMismatchedDesignedVoices` asymmetry; `voiceSubstitutedFrom` not carried through re-record merges `[ADV-H6]`.
- [ ] **Step 2: Acceptance sheet** — **insert Section E before `## 6 Known limitations` (`:2066`), not appended after Section D** `[EX-19]`; update the §7.1 totals and the title line. **Starred must-pass:** clone → cast in a Russian book → generate (Task 3's repro); **audition then revoke, confirm Play refuses** (Task 2's repro); revoke during an in-flight coqui derive; a long sentence on a cloned coqui voice; audition matches render; **a designed voice on a Coqui book — listen and judge whether the synthetic-clip derive beats the stock catalogue voice it replaces (D-B's open question, and the only place it can actually be answered)**; **a designed voice whose derive is forced to fail still renders the chapter** (D-F).
- [ ] **Step 3: File follow-ups with full detail** (must survive cold pickup) + thin `docs/BACKLOG.md` rows: the manual-link consent bypass `[ADV-M4]` (`cast-link-prior.ts:204-207` and `series-reuse-link.ts:390` bypass `library-cast-scan.ts:81-83`, so a cloned slot can be copied into a book that never had consent); re-record substitution-stamp loss `[ADV-H6]`; the splice/repair asymmetry.
- [ ] **Step 4: Commit** — `docs(docs): fs-38 Wave 3c regression plan, release notes, acceptance section`

---

## Self-Review

**Spec coverage.** §3.2 → Tasks 9 (latents persist), 10 (low-level `inference` + fail loud), 16 (wire contract). §5.3 sidecar → Task 10; Node → Tasks 17/20/21. §5.6 erasure → Tasks 2/11/13. §2.3's designed clip-persist payoff ("one derive path") → Tasks 20a/24, with its "never introduces a new hard failure" clause enforced by D-F. §8's 3c list → 9/10 (latents stable, no bleed), 10 (fails loud), 16 (resolves not `FALLBACK_SPEAKER`), 31 (golden, now covering **both** the cloned and the designed/synthetic derive). §6's "+engine choice" deliberately not implemented (D-A).

**The D-B delta is the youngest part of this plan** — Tasks 15, 16, 20a, 22, 24, 28-30, 31 and 32 changed after the four main passes ran. It has had **one** focused review, which returned three Criticals: the condition was unsatisfiable dead code `[DELTA-C1]`, the resolve set was wider than the validate set on three reachable paths `[DELTA-C2]`, and the VRAM ordering fixed only the end state `[DELTA-C3]`. All three are folded above, but this scope remains the least-reviewed in the wave. **Treat Task 20a's fail-soft handler, Task 22's residency invariant, and Task 24's clip-existence gate as the highest-risk diffs**, and give them the whole-branch reviewer's closest attention.

**What D-B actually cost.** It is not "one more provenance". It reaches into fs-60's fallback branch, the cross-language re-design guard, the cast picker's override precedence, VRAM residency (8-15× more derives per chapter), the card component, the mocks and the e2e. That is the honest price of the scope call, recorded here so it is visible at review time rather than discovered at merge.

**Ordering.** Task 20a lands after Task 20 so the fail-loud policy exists before the fail-soft one is written beside it — a reviewer sees them in contrast. Task 1 first (shared vocabulary six tasks import). Phase 0 before all XTTS work — while the marker can be deleted upstream, every downstream guard is inert. Task 2 before anything that widens the audition surface. Task 13 (erasure) before Task 15 (first path creating an xtts artifact in anger). Task 8 (`_synth_lock`) before the clone code adding a second concurrent entry point. Task 14 (read-modify-write serialization) before Task 18 (writes the second slot). Task 19 before Task 20 (consumes the signal). Task 27 before Task 28 (the chip must reflect an auditable engine). Task 29 before Task 30, or the e2e proves nothing.

**Task sizing.** The previous draft's Task 19 was four units and Task 21 three; they are split here into 20/21/22/23 and 25/26/27. Tasks 9+10 of the previous draft merged into Task 11 (one file, one test file, one context).

**Known residual risk.** Cross-process voice-library writes unserialized (#1826). `conftest.py:26` defaults `SEG_CAPACITY_ADMISSION=0`, so the admission branch is exercised in exactly one deliberate test (Task 11). Rendered chapter audio survives revocation by design.
