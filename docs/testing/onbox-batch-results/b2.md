# B2 · An unset book hits the analysis language gate — evidence

Register row B2 (`docs/testing/onbox-acceptance-register.md`, issue #2246).
Run against the isolated worktree workspace
`C:\Claude\Projects\wt-analyzer-render-batch\castwright-workspace` (this
worktree's own `server/.env` `WORKSPACE_DIR`), never the primary checkout's
`C:\AudiobookWorkspace`. Real local Ollama analyzer (`qwen3.5:4b`), no mocks.

Book under test: imported from `server/src/__fixtures__/the-coalfall-commission.ru.md`
(Author "Castwright Fixture", title "Дело о Коалфолле — B2 Gate Test"),
`bookId = castwright-fixture__standalones__дело-о-коалфолле-b2-gate-test`,
`manuscriptId = mns_IC4cNQVCHA`.

## Step 1 — book with `language: null` on disk

`POST /api/import` detected the fixture confidently as Russian
(`language: "ru", languageSupported: true, languageFallback: false`), so the
confirm-screen "Decide later" option (only offered when detection
*surrenders*, `languageFallback: true`) never appears for this text — the
detector is too confident. Reached the same on-disk state through the
route named in the brief's own "Read first" list as where language is set
*after* import — the Book settings row (`edit-book-meta.tsx` →
`PUT /api/books/:bookId/state` with `{ slice: 'state', patch: { language: null } }`,
the same call `EditBookMetaPatch.language` documents as "a deliberate
'stated absence'"):

```
PUT /api/books/castwright-fixture__standalones__%D0%B4.../state
{"slice":"state","patch":{"language":null}}
→ 204
```

Confirmed on disk (`.audiobook/state.json`): `"language": null`.

## Step 2 — analysis refused pre-detach

```
POST /api/manuscripts/mns_IC4cNQVCHA/analysis
{}

HTTP/1.1 409 Conflict
Content-Type: application/json; charset=utf-8

{"error":"language_unset"}
```

Matches `server/src/routes/analysis.ts:3085-3105`'s pre-flight gate exactly —
refused before the SSE headers opened, no job was started.

## Step 3 — resolve the language

Same Book-settings-row mechanism, this time setting it:

```
PUT /api/books/castwright-fixture__standalones__%D0%B4.../state
{"slice":"state","patch":{"language":"ru"}}
→ 204
```

Confirmed on disk: `"language": "ru"`.

## Step 4 — re-started analysis, real local Ollama, no mocks

```
POST /api/manuscripts/mns_IC4cNQVCHA/analysis
{}

HTTP/1.1 200 OK
Content-Type: text/event-stream
```

Job ran to completion against the worktree's real Ollama instance
(`qwen3.5:4b`, `engine: "local"` in `/api/books/:id/analysis/state`), no
`VITE_USE_MOCKS`. Server log (`[analysis] mns=mns_IC4cNQVCHA ...`):

```
phase=0 Detecting cast chapter-by-chapter across 2 chapters via Ollama (qwen3.5:4b)…
phase=0 Chapter 1/2 cast done — 1 character in 12s
phase=0 Chapter 2/2 cast done — 5 characters in 27s
phase=0 Detected 5 characters: Рассказчик, Рен, Мэйрин, Одуван, Коалфолл
phase=1 Attributing 2 chapters with Ollama (qwen3.5:4b)…
phase=1 Chapter 2/2 — narrated-speech check: 1/3 spoken lines attributed to
  the narrator (33.3%) — attributed population below the 20-line floor, too
  small to judge; source has 0 dash-opening speech lines.
phase=1 Attributed 55 sentences across 2 chapters
phase=1 Рассказчик: 25 lines · Коалфолл: 11 · Одуван: 8 · Мэйрин: 7
```

Job outcome: **completed**, 5-character cast detected, 55 sentences
attributed across the 2 chapters, no error.

## Step 5 — dash-opened dialogue attribution: confirmed after fixture extension

**Original fixture gap.** `the-coalfall-commission.ru.md` as scoped to this
row contained zero bare-dash-opened dialogue paragraphs — every line of
dialogue was wrapped in guillemets (`«…»`), with the dash used only *inside*
the quotes as a mid-sentence attribution tag, never as the sole marker
opening an unquoted paragraph. Confirmed via the analyzer's own
narrated-speech-check log ("source has 0 dash-opening speech lines") and
`grep -n "^—"` returning no matches. Posted as `AGENT BLOCKED` on
Castwright#2984 with three resolution options.

**Operator decision** (`AGENT HUMAN ANSWERED`, 2026-09-06): extend the
fixture with a minimal bare-dash passage, then re-run step 5.

**Fixture extension.** Added one short scene (6 lines: 2 narration, 4
bare-dash dialogue exchanges between the dragon and Одуван, no guillemets)
to the end of the existing chapter in
`server/src/__fixtures__/the-coalfall-commission.ru.md`, consistent with the
existing chapter's voice and characters — no restructuring or removal of
existing content:

```
Наутро колокол ещё не был готов, а мастер Одуван уже сидел у горна, кроша хлеб в кружку с чаем.

— Спал? — спросил дракон, не открывая глаз.

— Мало, — ответил Одуван. — Бронза не прощает сна.

— Тогда прости мне моё присутствие. Я стерегу огонь, пока ты дремлешь.

— Стереги. Только не дыши на угли, не то сожжёшь мне тигель раньше времени.

Дракон издал звук, отдалённо похожий на смех.
```

Synced the extended fixture into the isolated workspace book's
`manuscript.md` copy and re-ran analysis with `{"fresh":true}` (discarding
the disk analysis cache — required because the in-memory manuscript store is
keyed by manuscriptId per server process and the on-disk analysis cache
under `server/handoff/cache/` is keyed the same way; a non-fresh re-run
against an already-hydrated manuscript replays cached sentences and never
re-parses the new text) against this worktree's real local Ollama
(`qwen3.5:4b`, no mocks).

**Result: all 4 dash-opened lines correctly attributed as dialogue, none
fell to the narrator.** Server log:

```
phase=1 Chapter 2/2 — narrated-speech check: 1/7 spoken lines attributed to
  the narrator (14.3%) — attributed population below the 20-line floor, too
  small to judge; source has 4 dash-opening speech lines.
```

Attributed sentences (from the `result` event's `sentences` array), all four
new bare-dash lines split at id 57/58/60/61, none attributed to `narrator`:

| id | text | attributed to | confidence |
|---|---|---|---|
| 56 | `Наутро колокол ещё не был готов…` (narration) | narrator | 0.95 |
| 57 | `— Спал? — спросил дракон, не открывая глаз.` | **dragon** | 0.65 |
| 58 | `— Мало, — ответил Одуван.` | **oduvan** | 0.65 |
| 59 | `Бронза не прощает сна.` | **oduvan** | 0.90 |
| 60 | `— Тогда прости мне моё присутствие. Я стерегу огонь, пока ты дремлешь.` | **dragon** | 0.65 |
| 61 | `— Стереги. Только не дыши на угли, не то сожжёшь мне тигель раньше времени.` | **oduvan** | 0.65 |
| 62 | `Дракон издал звук…` (narration) | narrator | 0.95 |

Every dash-opened line (57, 58/59, 60, 61) is correctly attributed to the
speaking character (dragon or Одуван) rather than defaulting to the
narrator — the failure mode this row's acceptance criterion guards against
did not occur. This satisfies row B2 in full, including step 5, on the
operator-approved extended fixture.
