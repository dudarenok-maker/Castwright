# Feature regression plans

Living specs for every feature shipped in v1. Each plan combines **invariants to preserve** (structural rules a refactor must not break) and an **acceptance walkthrough** (manual click-through with expected URL hashes, redux state, and network calls). Partial features are documented as-is with a `KNOWN: scaffolded` banner so reviewers do not mistake "documented current behavior" for "fully working."

PRs that change behavior cited in a plan MUST update the relevant plan in the same diff — that is the regression discipline the plans buy.

**For outstanding work** see [`../BACKLOG.md`](../BACKLOG.md) — the MoSCoW-bucketed list of every follow-up, scaffolded item, and untested seam pulled from these plans. Future planned rounds of work pull from there.

## Writing a new plan

1. Copy [TEMPLATE.md](TEMPLATE.md) — required sections are **Benefit / Rationale**, **Architectural impact**, and **Test plan** (automated + manual).
2. Add YAML frontmatter (`status`, `shipped`, `owner`). New plans start `status: draft` until they ship.
3. Link the plan from the matching area section below.
4. Land paired automated tests in the same PR — see CLAUDE.md "Testing discipline".

## Plan lifecycle

Each plan's frontmatter `status:` is one of:

- **draft** — written, not yet implementing.
- **active** — implementation in progress on the main branch.
- **stable** — feature is end-to-end functional, behavior locked by automated tests.
- **deferred** — explicitly parked; see body for "wake when" condition.
- **scaffolded** — UI/contract in place, parts mocked or partial (matches the `KNOWN: scaffolded` body banner).

When a plan reaches **stable** AND has a filled **Ship notes** section, move it to [`archive/`](archive/README.md) in the same PR — the top-level index is the working set, not a changelog. See `archive/README.md` for the move checklist.

## How to run a plan

1. Pick a plan from the index.
2. Read the **Invariants** section first; if any cited file/line has moved, the plan is stale — fix it before validating behavior.
3. Walk the **Acceptance** steps against a running app (`npm run dev`, plus `cd server && npm run dev` and the TTS sidecar if the plan covers audio).
4. Any drift → update the plan in the same PR as the code change.

## Plans by area

This section lists only the **in-flight working set** — plans whose `status:` is `active` or `deferred`. Every shipped (`stable`) plan now lives under [`archive/`](archive/README.md) and is indexed in [Shipped (archive)](#shipped-archive) below.

### D. Voice matching & cast

- [131 — Cast status filter](131-cast-status-filter.md) — `active`. A row of toggle chips under the cast search box filters the table by voice-matching status (multi-select, OR) so default/undesigned voices don't get lost as a cast grows. Each chip shows a live count and only appears for statuses present in the cast. New `statusFilterKeys` helper in `voice-status.ts` wraps `resolveVoiceStatus`, so the chip counts and the per-row Status pills share one source of truth; the filter operates on the post-search copy inside `CastView` (same discipline as the plan-121 sort) and applies to both the desktop grid and mobile cards. Replaces the previously non-functional Filter button.
- [140 — Cross-book cast cleanup (fe-8/9/16, fs-11)](140-cross-book-cast-cleanup.md) — `active`. Four plan-101 follow-ups making cross-book duplicate cleanup low-friction: a "Possible duplicate of …" chip in the profile drawer (fe-8) opening the existing `DuplicateReviewModal` pre-populated; a "Review all duplicates in <Series>" bulk walkthrough (`bulk-duplicate-review.tsx`, fe-9); a reversible "different on purpose" — a `DELETE …/not-linked-to` route + an "Ignored duplicate suggestions" toggle on `#/voices` with Unmark (fs-11); and a "Fallback (Kokoro)" cast/drawer Status pill (fe-16) fed by a new `collectRenderedFallbackEngines` aggregator surfaced on book-state GET + threaded into `resolveVoiceStatus`.
- [165 — Revision A/B player e2e + library/cast language UX (fe-15 + fe-16)](165-fe-15-16-language-and-revision-e2e.md) — `active`. Two deferred backlog items shipped together. **fe-15** closes the browser-level gap on the plan-114 profile-regen preview gate + the review-mode A/B player: the BACKLOG premise (mock "hydration throws") was stale — chapters already hydrate, so the real fix was giving `cc` (Carrick's Compass) `CC_CHAPTERS` + a `chapterCharacters` map so it has BOTH a cast AND chapters its cast speaks in (the preview flow's prerequisite). New `e2e/revision-diff.spec.ts` click-through (review mode) + `e2e/profile-regen-preview.spec.ts` (preview→Approve fans the rest + logs a regenerate; preview→Reject reverts), with the preview chapter fast-forwarded via `window.__store__`. **fe-16** adds (a) a library En/Русский filter pill set (`filterBooks` gains `activeLanguages`, `selectPresentLanguages`/`LANGUAGE_LABELS`; rendered only when >1 language present, ANDs with search+tags) and (b) a cast-view banner + one-shot Qwen auto-load on entry for non-English books (`CastView` gains `bookLanguage` threaded from the library entry). `ts` is the lone Russian mock fixture; `cc` stays English so the preview e2e never trips the cast Qwen probe. Vitest (library-slice / library-chrome / cast) + the two e2e specs landed; live GPU Qwen auto-load acceptance owed.

- [161 — A/B "current vs proposed" voice audition in the Qwen design flow](161-voice-design-compare.md) — `active`. The Profile drawer's Qwen "Design & preview" becomes "Design & compare": it stages a PREVIEW design and opens a two-pane A/B (Side A current voice, Side B proposed — editable persona + Re-design inline + Auto A→B), then "Use proposed voice" promotes + stages it while the drawer's Save persists as before (Cancel discards). Extracts CompareCastModal's two-pane shell + playback into a reusable `AbCompareShell` + `useAbAudition` (CompareCastModal refactored onto them, no behaviour change). Non-destructive: a new server `preview:true` stages under `qwen-<id>-preview`, `promote-voice` moves it onto the stable id + evicts the sidecar prompt cache, `discard-voice` drops it on Cancel (new sidecar `POST /qwen/evict-voice`) — so re-designing an existing Qwen voice never overwrites it until approved. Successor surface to 108; helps audition the 160 persona format.

### E. Manuscript editing

- [70c — Restructure rebuilds the analysis cache](70c-merge-cache-rebuild.md) — Merge / split / reorder now rebuild `server/handoff/cache/{manuscriptId}.json` from `manuscript-edits.json` instead of deleting it. Generation auto-heals on the next POST if the cache is empty. Resolves post-merge halts citing "No analysed sentences cached for this book."
- [92 — Manuscript view virtualisation](92-manuscript-virtualisation.md) — `useWindowVirtualizer` from `@tanstack/react-virtual` wraps the segment list above a 60-segment threshold; below that the flat-render path stays for short chapters where windowing is overhead. Boundary drag, `data-sentence-id`/`data-sentence-idx` attributes, and the J/K low-confidence jumper all preserved (jump routes via `virtualizer.scrollToIndex` first). DOM node count for a 300-segment chapter drops from ~300 to ~20–40 (visible + overscan window).
- [93 — Confirm-cast + listen-chapter list virtualisation](93-list-virtualisation.md) — Same `@tanstack/react-virtual` dep as plan 92, two shapes: `useWindowVirtualizer` for the page-scrolling confirm-cast picker (`src/views/confirm-cast.tsx`); `useVirtualizer` with `getScrollElement` for the listen view's chapter list that scrolls inside its own `max-h-[560px]` container (`src/components/listen/listen-player-region.tsx`). Both gated at 40 rows so short books stay on the flat path. No new dep cost beyond plan 92.

### F. TTS

- [200 — Bulk voice-design recycle resilience](200-bulk-design-recycle-resilience.md) — `active`. Fixes "Design full cast always fails after the first voice." Root cause: the failing runs ran against a sidecar started **without this server's `.env`** → it used the AUTO committed-RAM ceiling (~14 GB) instead of the configured 48.5 GB and self-recycled (code 43) after ~3 designs; the recycle then broke the run because `start.ps1` only relaunched on poison-code 42 (not recycle-code 43) and `cast-design.ts` treated the respawn-gap "unreachable" as a fatal halt. Three fixes: (1) the bulk job **rides out a recycle** — waits for the supervisor respawn (`ensureSidecarEngineReady`) and retries the character up to `MAX_RECYCLE_RIDEOUTS`, halting only after the budget; (2) `start.ps1` restarts on **both 42 and 43** (decision extracted to a unit-tested `sidecar-restart-policy.ps1`); (3) a **config-consistency handshake** — `/health` exposes `mem_restart_mb`/`vram_restart_mb` and the spawn-gate replaces (A1) a sidecar whose effective ceilings disagree with the server config (closes the "dev sidecar adopted by prod" trigger). Reserved VRAM was never the bottleneck (flat ~5.8 GB; a correctly-configured sidecar designs 8/8). Paired vitest + Pester + pytest green; live-GPU acceptance owed.
- [195 — Design full cast (bulk Qwen voice design)](195-design-full-cast.md) — `active`. A **Design full cast** button on the Cast screen designs a bespoke Qwen voice for every "Needs voice" character in one click, as a **server-owned SSE job** surfaced via a **third top-bar status pill** (beside Analysis + Generation) that survives navigation and a browser reload (an in-memory per-book job + a cold-boot re-subscribe probe — the resilience analysis lacks). The per-character work reuses an extracted `designQwenVoiceForCharacter` core (sidecar design + audition cache) under a per-book **design mutex** + GPU semaphore, persists the override via the drawer's exact `applyOverrideToCastFiles` path (series scope, standalone-safe), Gemini persona fallback, ~6s heartbeats, **freshness-skip** (never clobber a manual design), and per-character failures that don't halt the run. Hardens a pre-existing `cast.json` write-race with a `bookDir`-keyed busy registry making bulk-design ↔ re-analysis **mutually exclusive** + the single-design route mutex-shared (`server/src/tts/design-lock.ts`). Slice + middleware + server-route (real-ffmpeg) + pill + button + e2e tests green; live-GPU acceptance owed.
- [201 — Per-book bulk emotion-variant design + cast-table variant glyphs (fe-32)](201-cast-bulk-emotion-variants.md) — `active`. Extends plan 195's "Design full cast" button with a **scope picker** (Base voices / Emotion variants / Both) and designs every *demanded-but-missing* Qwen emotion variant for the book's cast in one action. A demand-driven **glyph strip** on the second line of the cast row's Status column (one SVG glyph per in-use emotion, green ✓ = designed / amber ! = needed) supersedes the old variant count badge + "N tags need a variant" hint. `persistEmotionVariant` extracted from the single-design route as a shared helper; new `variant_designed` SSE event mirrors live flips into the glyph strip. A designed variant **travels with the linked cast** — its `cast.json` slot propagates to every linked character (same `voiceId`) across the series via the shared `forEachMatchingCastCharacter` walk (standalones excluded), and `applyOverrideToCastFiles` was fixed to preserve `variants` when re-assigning the base name (`srv-37`, [#688](https://github.com/dudarenok-maker/AudioBook-Generator/issues/688)). Closes backlog `fe-32` ([#512](https://github.com/dudarenok-maker/AudioBook-Generator/issues/512)); builds on plan 195 (job/pill/slice) and plan 177 (fs-25 per-quote emotion). Full test matrix: server (qwen-voice + cast-design), API client, Redux (slice + middleware), frontend (variant-tasks, scope picker, glyph strip, cast view), e2e (cast-variant-design + design-full-cast updated); live-GPU acceptance owed.
- [194 — Voice cloning (your own / family voice) + cloned-vs-designed library split](194-voice-cloning.md) — `draft`. The **next big release** (`fs-38`, [#624](https://github.com/dudarenok-maker/AudioBook-Generator/issues/624)): clone a real person from a short in-app sample (XTTS reference first, then Qwen design-to-target) and cast it consistently across a book/series, with explicit consent on the record, a separate cloned-voices section in `#/voices` excluded from the cross-book reuse matcher, and local-only storage. Pays off the _"even in your own voice"_ brand promise; parent epic [#622](https://github.com/dudarenok-maker/AudioBook-Generator/issues/622). The **next big release** (`fs-38`, [#624](https://github.com/dudarenok-maker/AudioBook-Generator/issues/624)): clone a real person from a short in-app sample (XTTS reference first, then Qwen design-to-target) and cast it consistently across a book/series, with explicit consent on the record, a separate cloned-voices section in `#/voices` excluded from the cross-book reuse matcher, and local-only storage. Pays off the _"even in your own voice"_ brand promise; parent epic [#622](https://github.com/dudarenok-maker/AudioBook-Generator/issues/622).
- [70d — Per-sentence synth + audio-tag stripping](70d-per-sentence-synth-and-tag-strip.md) — `buildSentenceGroups` emits one group per sentence (was: fold consecutive same-speaker), and `normaliseForTts` strips the closed `[empathic]` / `[whispers]` / etc. vocabulary at the TTS boundary. Fixes long all-narrator chapters that ran past the 30 s stall watchdog, audio tags being read aloud, and same-speaker voice drift at large context sizes.
- [108 — Qwen3-TTS coexistence + per-character engine/voice + series rebaseline](108-qwen-coexistence.md) — `active`. Adds Qwen3-TTS 0.6B as a coexisting sidecar engine alongside Kokoro; makes TTS engine a per-character (`Character.ttsEngine`) decision threaded through generation + drift; a cast-view picker to change a character's engine + base voice with series-wide propagation; a "Rebaseline the series" modal that designs bespoke Qwen voices for the principal cast (~80% of non-narrator lines) with current-vs-proposed audition and writes changes as drift; a VRAM-weighted GPU semaphore (`GPU_VRAM_BUDGET`); a deliberate `dualModelEnabled` account flag; queue badges naming each chapter's required engines. Delivers the engine half of backlog `fs-2` (language half deferred). All five implementation waves shipped (Wave 5 = the rebaseline modal); GPU-bound manual acceptance remains.
- [162 — Multi-language support (Russian first): the fs-2 language half](162-fs2-multilanguage.md) — `active`. The language half of `fs-2` on top of plan 108's Qwen engine: a BCP-47 `language` field end-to-end (additive, no schema bump), Cyrillic auto-detection + a confirm-metadata selector, the Qwen design proxy threading `book.language` so designed voices speak the book's language, Cyrillic-aware analyzer token estimate + a per-language attribution preamble (Gemini + Ollama), a Russian preview pangram, a Listen language badge, and — the load-bearing piece — the **never-cross-language invariant** enforced at two layers: the cast view hard-locks non-English books to Qwen (Kokoro is English-only), and the generation route force-routes every character (incl. narrator) to Qwen, blocks the silent Kokoro fallback (`MissingDesignedVoiceError`), treats a cross-language-reused voice as undesigned, and makes an unavailable Qwen fatal. English books are byte-identical to pre-fs-2. Library En/Ru filter pill + cast-wide banner deferred (`fe-16`); Russian UI strings are a separate epic (`fs-14`). Code + paired tests (server + frontend + e2e) landed; GPU-bound Russian-synthesis acceptance remains.
- [113 — Qwen3-TTS true batching](113-qwen-true-batching.md) — `active`. Packs every Qwen sentence in a chapter into `QWEN_BATCH_SIZE`-capped batches synthesised in ONE `generate_voice_clone(text=[…])` call (one batched forward per decode step instead of N), via a new Qwen-only sidecar `POST /synthesize-batch` (length-prefixed binary frame) and an OPTIONAL `TtsProvider.synthesizeBatch` seam. Whole-chapter scatter/gather: batches mix narrator + dialogue voices (per-element prompts) and scatter each chunk back to its own sentence index, so the index-order reassembly + plan-107 determinism are reused verbatim. Independent-sequence batching (not plan-70d string folding) means no shared decode context → no voice drift; the stall heartbeat now covers long batches; `=1` is the per-call kill-switch. Coqui/Kokoro/Gemini untouched. Code + automated tests landed; real-GPU speedup/drift acceptance pending.
- [141 — Sidecar host-memory leak guard](141-sidecar-memory-leak.md) — `active`. Fixes the 2026-05-30 incident where a long-lived sidecar grew to ~54 GB committed-private host RAM and the OS killed the Node server mid-run. Root cause: model-unload dropped the ref + `empty_cache()` (VRAM only), but PyTorch `nn.Module` graphs hold reference cycles so the ~3.4 GB host copy waited on a lagging cyclic GC — across repeated VoiceDesign cycles the corpses piled up (~15 × 3.4 GB ≈ 54 GB). Fix: `gc.collect()` in every `unload*` path before `empty_cache()`; a host-RSS watchdog (`SIDECAR_RSS_WARN_MB`, default 8192) that logs the curve and force-reclaims past threshold (in-process — no self-exit, since `srv-15`'s respawn gap means a kill would stall the run); a `GET /debug/memory` readout (RSS/private + GC stats + per-engine model/cache + CUDA alloc); `psutil` pinned explicitly. Code + `test_memory.py` landed; instrumented live-run curve-confirmation pending before → stable.
- [143 — Sidecar process-recycle for the variable-input-shape host leak](143-sidecar-process-recycle.md) — `active`. **Corrects plan 141's root-cause scope.** A live run reproduced the host-RAM climb during generation with ZERO designs; a controlled experiment settled it: fixed-shape batches hold the floor flat, variable-shape real generation climbs unbounded (~+0.3 GB/min), CUDA flat throughout — i.e. native per-input-shape workspace accumulation (pytorch #32596 / the user-supplied Qwen-leak report), which `gc`/`empty_cache` can't reclaim. Fix: the memory watchdog gains a HARD ceiling at which the sidecar self-exits (code 43) so the srv-15 supervisor respawns a fresh process — the report's "process recycling"; srv-16's skip-completed-on-resume means only the in-flight chapter re-renders. Soft `gc+empty_cache` ceiling kept (helps the plan-141 design-cycle half). **Post-ship correction (2026-05-30): the ceiling is keyed on COMMITTED-PRIVATE, not RSS** — live data showed private ≈ 1.7–1.9× RSS, so an RSS ceiling maps to private > RAM and would fire only after the OOM cliff; env knob is now `SIDECAR_RESTART_MB` (default 70% of RAM ≈ 45 GB on 64 GB) reading `_process_commit_mb()`. Eliminating the leak itself (MKLDNN-disable / shape-bucketing / upstream) is `side-11`. Validated live: a recycle fired cleanly (self-exit → respawn → resume) during the test.
- [144 — Sidecar CUDA-fragmentation OOM guard (`expandable_segments`)](144-sidecar-cuda-frag-oom.md) — `active`. A SEPARATE memory issue from 143 (VRAM, not host RAM): ~40 min into a clean run a 32-item Qwen batch 500'd with `CUDA error: out of memory` on the 8 GB GPU despite modest total VRAM use — CUDA allocator fragmentation from variable-length batches churning fixed `cudaMalloc` blocks. It auto-recovered (sidecar poisoned → code-42 self-exit → srv-15 respawn → resume; 2 chapters failed), and the host-RSS recycle (143) doesn't prevent it (VRAM OOM hits earlier, at low host RAM). Fix: default `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` into the sidecar spawn env (`spawn-sidecar.ts`, after `...process.env` so an explicit override wins) — CUDA VMM grows/shrinks virtual segments so fragmented free space stays reusable; must be set pre-torch-import, hence the spawn-env default. Report's recommendation #4. Code + spawn-sidecar tests landed; live no-OOM acceptance pending.
- [180 — Preview an emotion from the manuscript quote chip (fe-31)](180-fe31-emotion-chip-preview.md) — `active`. Adds a ▶ preview affordance to the fs-25 per-quote emotion chip (`SentenceEmotionControl`): tag a line and audition the speaking character's designed Qwen variant in place, via the existing variant cache scope (`${baseScope}__${emotion}`) through `playSampleWithAutoLoad` — a cache hit on a warm sidecar, no synth-schema change. Also serves fs-25's 5e missing-variant UX: a Qwen speaker WITHOUT the variant previews the BASE voice + an inline "renders neutral" note; a non-Qwen (Kokoro/XTTS) speaker DISABLES the preview with an "Emotion only audible on Qwen" tooltip (engine read off `character.ttsEngine`, the same signal the generation route uses). Variant-vs-base resolution factored into the pure `lib/play-emotion-variant.ts`; `SentenceEmotionControl`'s API extended additively (new `character` prop) so the existing tag/clear behaviour is untouched. Unit + e2e landed; live-GPU audible-variant acceptance owed (CI has no sidecar venv). Issue #506.

### G. Generation

- [198 — "Needs regeneration" indicator after reassigning sentences (Bug 2)](198-stale-chapter-reassign-indicator.md) — `active`. A `done` chapter whose sentence→speaker assignments were changed after it rendered now shows an amber **"⚠ Sentences reassigned · regenerate to refresh"** caption on its Generate row. **Durable, no new stored field** — derived from the change-log `boundary_move` events (one per reassignment) vs the chapter's `audioRenderedAt` render stamp (pure helpers `latestReassignAt` / `isChapterStaleFromReassign`). Computed at the view level and passed to each `ChapterRow`; the caption wins over the informational mixed-engine / engine-drift "done" captions. Closes the one reassignment path that didn't emit a `boundary_move` (the `reattribute-lines` modal). v1 is time-based/optimistic (reassign-then-undo still reads stale); the precise per-sentence net-diff is a filed follow-up.
- [197 — "Not queued" held chapter state (Bug 1)](197-not-queued-held-chapter.md) — `active`. Deleting a chapter from the generation queue now records the user's intent as a durable per-chapter `held` flag (server `state.json` + additive openapi `Chapter.held` + `POST .../chapters/:id/held`, cloned from the exclude handler minus audio cleanup). The Generate row flips to a neutral **"Not queued"** instead of a stale "Queued", and the auto-work `enqueueOnWork` / "Resume generation" no longer silently re-adds it — every "is this queued work" read site became `state === 'queued' && !held`. The queue-modal delete wires the hold in `cancelQueueEntry` (this-scope, loaded book, only a `queued` chapter — never a done regenerate ticket); "Generate this chapter" clears it. Held chapters stay in `activeChapters` so the book reads "not fully complete" while any are held. Re-analysis carries `held` forward (not re-derivable, unlike audio metadata); reparse drops it. Slice + thunk + middleware + server-route + render + e2e tests green.
- [107 — Within-chapter sentence parallelism](107-within-chapter-parallelism.md) — Bounded-concurrency dispatch of a chapter's sentence groups in `synthesiseChapter`, width = `gpuSemaphore.maxConcurrency` (default 1 ⇒ byte-identical to the old serial loop). Results collected by `group.index` and concatenated in narrative order; sample-rate anchor fixed before dispatch (lowest-index group / title beat), not first-to-complete; `onGroupStart` heartbeats still reset the 30 s stall watchdog. Stacks on plan 87, references plan 70d.
- [110 — Queue modal reflects active generation + GPU badge disambiguation](110-queue-active-generation-honesty.md) — `active`. The PRIMARY generation path (first-run + resume) opens its SSE via the `generation-stream-middleware` reconcile and writes NO workspace-queue entry, so the queue modal + "Queue · N" chip read "Empty"/0 while a book is visibly generating. Two new pure selectors (`selectActiveGenerationView`, `selectGenerationActivityCount`) let the modal/chip overlay the live `chapters.activeStream` run when the workspace queue is empty (real entries still win). Top-bar GPU-contention badge renamed `"Queued (N ahead) ·"` → `"GPU busy · N waiting ·"` to stop colliding with the generation queue. Part B (make the queue authoritative — N configurable workers) is plan 111.
- [127 — Generation RTF telemetry](127-generation-rtf-telemetry.md) — `active`. The fast batched Qwen path (`/synthesize-batch`) now logs `qwen batch synth: … rtf=` (was silent — only the slow ~8 audition path logged rtf). `generation.ts` logs a per-chapter rollup (rtf / Nx realtime / chapters-hr) fed by a rolling accumulator (`server/src/tts/generation-stats.ts`), surfaced over `GET /api/generation/stats` and shown live in the top-bar Admin pill so speed is self-monitorable without grepping logs.
- [172 — Admin watch console (fs-18 diagnostics health board)](172-admin-watch-console.md) — `active`. Folds backlog `fs-18` into the former dev-only Worktrees console, making it an all-users **Admin** watch console (stage `worktrees`→`admin`, `#/admin`). A new `GET /api/diagnostics` aggregator reuses the in-process probe helpers extracted from the sidecar/ollama/gpu health routes (no HTTP self-calls) plus two net-new probes (`server/src/diagnostics/{ffmpeg,disk}.ts`) and returns a green/amber/red board (GPU/VRAM, sidecar + resident models, analyzer connectivity, ffmpeg, free disk); each check is isolated so one failing probe yields a `fail` row, never a 500. The view stacks Health board → generation throughput (both all-users) → the git-worktree list (still gated behind `import.meta.env.DEV` + the route's prod 404). The top-bar `AdminPill` is always rendered now and carries a health status dot off the board's `overall`, keeping the live RTF readout. Closes #468; server + frontend + e2e tests landed, live multi-process acceptance pending.
- [130 — Qwen3-TTS as the default engine](130-qwen-default-engine.md) — `active`. Makes Qwen the default for new books WHEN it's installed (else Kokoro), resolved live via `getResolvedTtsModelKey` off a cached install-state (`/health` `qwen_install_state` + a Node-side boot probe) — no per-book engine field. A per-character render fallback (`applyQwenFallback` in `synthesise-chapter.ts`) renders an undesigned-voice or engine-unavailable Qwen character in Kokoro instead of failing, stamped `renderedFallbackEngine` on the segment + snapshot and surfaced as a "Fallback (Kokoro)" status. A one-click in-app installer (`QwenInstallBootstrap` + `/api/qwen/*`, `<QwenInstall/>`) replaces the copy-paste CLI card; a `QwenStatusNotice` warns + promotes Qwen on the cast screens when it's not installed. `PRELOAD_QWEN=1`/`PRELOAD_KOKORO=0` when Qwen is the default (Kokoro lazy, warmed on demand). Successor to 108.
- [137 — Sidecar synth fetch timeout headroom](137-sidecar-fetch-timeout.md) — `active`. The server→sidecar synth POST used Node's global `fetch` with undici's default **300 s `headersTimeout`**; a wide Qwen batch (plan 136, cap 64) takes 400–454 s > 300 s, so the fetch aborted (`fetch failed`) → the provider's `transient` "not reachable" → the retry wrapper re-synthesized the same batch → loop → fatal "Local TTS sidecar not running" while the sidecar kept producing orphaned audio (473 items for a 217-line chapter; detached UX). Fix: `post()` uses undici's OWN `fetch` + an `Agent({ headersTimeout: 0, bodyTimeout: 0, connectTimeout: 10_000 })` so a long-but-alive synth isn't aborted (cancellation stays via `AbortSignal`); `undici` added as an explicit server dep. Real-`node:http` regression tests (CONTROL short-timeout aborts / FIX tolerates slow / AbortSignal honoured) + the classification suite switched to `vi.mock('undici')`. Unblocks larger plan-136 caps.
- [139 — Queue failure circuit-breaker + SSE-disconnect orphan recovery](139-queue-failure-circuit-breaker.md) — `active`. Two queue-robustness restores. (srv-11) The dispatcher tracks consecutive **identical** `errorReason`s per book and after 3 auto-pauses the queue with a toast naming the repeated error (in-memory streak, reset on success or a differing reason), restoring the fail-fast the removed within-book cascade-kill gave for free. (srv-12) The generation route resets an orphaned `in_progress` entry → `queued` (new `resetEntryToQueued` mutator) and aborts the unwatched synth when the LAST SSE subscriber disconnects without completion, so a mid-generation browser refresh self-heals — load-bearing detail: the starter close handler moved `req.on('close')` → `res.on('close')`, the handler that actually fires on client disconnect.
- [142 — Generation recovery: sidecar respawn + server-authoritative queue completion](142-generation-recovery.md) — `active`. Two reliability gaps the 2026-05-30 OOM incident exposed. (srv-15) Plan 43 moved sidecar ownership to Node but `spawn-sidecar.ts` only LOGGED the child's exit and `start-app.ps1` stopped supervising — so a crash / OOM-kill / poison self-exit (code 42) stalled generation forever. New `sidecar-supervisor.ts` (`createSidecarSupervisor` → start/stop/current) owns the handle and respawns on unexpected exit with backoff `[2s,5s,15s]` + a crash-loop cap, via a new `onExit` seam on `SpawnSidecarOpts`; `index.ts` swaps the one-shot spawn for the supervisor (stop() before reap so shutdown can't race a respawn). (srv-16) Queue completion was frontend-only (dispatcher POSTs `/complete` on stream close), which a hard kill / closed tab bypasses → rendered chapters stuck `in_progress`. The server now completes the entry itself: Hook 1 right after the `chapter_complete` broadcast (rendered + persisted), Hook 2 when a non-force single-chapter POST finds the chapter already on disk (restart-after-crash), both through a `serializeQueueMutation` chain (also wraps the srv-12 reset) so concurrent workers can't lose a removal; idempotent, with the frontend `/complete` as backstop. Code + tests landed; kill-mid-run acceptance pending.
- [145 — Server crash diagnostics + unhandled-rejection resilience](145-server-crash-diagnostics.md) — `active`. The generation server's `:8080` child died SILENTLY twice on 2026-05-30 (no stack trace, no heap-OOM, RAM fine the 2nd time), stalling the run — because it had no `uncaughtException`/`unhandledRejection` handlers, so Node's default crash output never reached the captured stderr. New `crash-logging.ts` (`installCrashHandlers`, wired in `index.ts` right after `installTimestamps()`): `uncaughtException` → log the stack + `exit(1)` (state is corrupt); `unhandledRejection` → log the reason + SURVIVE (overrides Node's terminate-on-rejection default so a transient async error — prime suspect: a sidecar-fetch rejection during the ~20 s startup race — can't kill an unattended run). Makes the next crash diagnosable (or, if still no log, narrows it to native/external). Root-causing whatever it captures is `srv-17`. Tests via an injected EventEmitter.
- [146 — Per-chapter loud Qwen→Kokoro fallback gate + resident-model visibility](146-loud-fallback-gate.md) — `active`. Closes the 2026-05-30 "why is Kokoro loaded on my all-Qwen book and I can't see/kill it?" incident. Three coupled fixes. (1) **Stop the unconditional Kokoro pre-warm:** `generation.ts` warmed Kokoro for EVERY Qwen run (`if (qwenInUse)`) as a defensive fallback target — even a fully-designed all-Qwen book, wasting ~1 GB VRAM and oversubscribing the 8 GB card (driver spilled to shared host RAM → RTF collapse). Now warms it ONLY when a fallback will actually render. (2) **Per-chapter loud gate:** a chapter that would silently render an undesigned Qwen voice in Kokoro now PARKS (new queue status `awaiting_confirm` + `fallbackCharacters`/`fallbackConfirmed`, new `computeQwenKokoroFallbackSet` predicate), emits a `chapter_awaiting_fallback_confirm` SSE tick → warn toast, and waits for per-chapter Confirm (`POST /confirm-fallback` → queued+confirmed) or Skip (`POST /skip-fallback` → removed); other chapters keep flowing. `completeEntry` no-ops on a parked entry so neither the dispatcher reconcile nor srv-16's done-flip can clobber it. Distinct from + mutually exclusive with plan 135's all-cast `qwen_unavailable_kokoro_fallback` (engine-unavailable). (3) **Pill shows all resident models:** `layout.tsx` `enginesToShow` now includes any engine the sidecar actually has loaded (`/health` state ready/streaming/loading), not just the current book's cast — so a pre-warmed Kokoro is always visible with a Stop control. Full server + frontend test coverage; live acceptance pending → stable.
- [148 — Recycle no longer drops the single in-flight chapter (drain + in-worker recovery, srv-17c)](148-recycle-inflight-recovery.md) — `active`. Closes the gap plan 142 scoped out: srv-17b's readiness gate protects only the NEXT chapter, so the one already mid-synth when a recycle (143) makes the sidecar self-exit dies on the connection drop, is classified fatal ("sidecar not reachable"), and flips to `failed` (the ch36/ch46 drops). Two halves. (A, server backstop — covers recycle AND crash/OOM) `processOneChapter` wraps the synth in a bounded recovery loop keyed on `isTransient(e)` (both a connection drop and the drain-503 are transient-annotated; CUDA-poison is `transient:false` so it still surfaces immediately): on a transient throw it rides out the supervisor respawn via `ensureSidecarEngineReady` (srv-17b, 120 s) and re-renders on the same worker, up to `MAX_RECYCLE_RECOVERIES` (2) before falling through to the unchanged `chapter_failed`/cascade/fatal path — no new SSE event, no frontend change. (B, sidecar polish) the watchdog DRAINS in-flight synth before the code-43 exit: an `_inflight_synth` counter (inc/dec around each `to_thread` offload in `/synthesize` + `/synthesize-batch`) is polled to 0 by `_drain_then_restart` (bounded by `SIDECAR_DRAIN_GRACE_MS`, default 180000; `0` = immediate exit), and `_restart_pending` makes new synth fast-fail a NON-poisoned 503 so no fresh chapter enters the dying process. Server + sidecar tests landed; live recycle-mid-chapter acceptance pending.
- [152 — Recycle-drain readiness: `/load` honors the drain fence (no queued-chapter cascade)](152-recycle-drain-readiness.md) — `active`. Closes the gap plan 148 (srv-17c) scoped out: a recycle protects the in-flight chapter, but **queued** chapters a free worker starts during the ~2-min drain window were dropped — the 2026-05-31 cascade failed **12 chapters (17–28)** with `503 "recycling"`. Cause: `/synthesize` honored the `_restart_pending` drain fence but **`/load` did not** (it checked only "model resident"), so the readiness gate `ensureSidecarEngineReady` saw `ready` instantly and marched queued chapters into a 503, burning their 2-attempt ride-out in ~5 s. Fix: `/load` returns the same non-poisoned recycling 503 while draining so the gate POLLS through the drain+respawn (the preload gate then blocks the worker until truly ready); `READINESS_TIMEOUT_MS` 120 s → 210 s to span a full 180 s drain grace in one wait. Plus two data bugs from the same run: empty-text sentences are dropped before batching (the ch14 `400 "text is required"`), and a `MAX_TEXT_LENGTH` cap (`side-13`) fails an over-length item fast with a 400 + logs the offending sentence on a synth timeout (the ch29 600 s hang). Eliminating the leak that forces the recycle is `side-11` (separate). Server + sidecar tests landed; live recycle-drill acceptance pending.
- [153 — Variable-shape host leak: candidate-1 (MKLDNN-disable) probe + leak-slope harness](153-sidecar-variable-shape-host-leak.md) — `stable`. Attacked the leak that _forces_ the recycle (plan 143/152): Qwen climbs committed-private host RAM monotonically because every sentence is a different length → a native per-shape workspace never freed (flat on fixed shapes, CUDA flat — pytorch/pytorch #32596). Shipped candidate 1 (env-gated `SIDECAR_DISABLE_MKLDNN`, default OFF, in `_apply_torch_perf_flags`), the `committed_mb` `/debug/memory` field, and the leak-slope instrument `bench-tts.py --mem-sample`. **2026-06-01 live A/B REJECTED candidate 1 on throughput:** disabling MKLDNN cripples Qwen synth ~15×+ (one 16-sentence batch didn't finish in 31 min vs ~120 s; ~7–8 CPU cores pegged, GPU idle) — MKLDNN is load-bearing for the Qwen CPU audio path, so it's non-viable regardless of leak effect. Flag stays OFF (diagnostic toggle only). OFF baseline reconfirmed the leak (~1,150 MB/batch committed, CUDA spill to 16 GB by ~batch 23). The leak itself stays open under `side-11` (candidate 2 — fixed-shape padding), bounded meanwhile by the plan-143 recycle + plan-155 load-reclaim. Bench summary print also de-glyphed (`Δ`/`≈` → ASCII) for Windows cp1252.
- [158 — Soft recycle at the chapter boundary (side-11 item 2)](158-sidecar-soft-recycle-boundary.md) — `active`. Blast-radius mitigation for the variable-shape host leak (plan 153 / side-11) while the leak itself stays open. The HARD watchdog (`SIDECAR_RESTART_MB` → code-43 self-exit) fires LATE — after RTF has degraded as committed climbs 5→45 GB — and can drain mid-chapter. New: a SOFT threshold `SIDECAR_RECYCLE_SOFT_MB` (default 0/OFF) below the hard ceiling sets `recycle_pending` in `/health` WITHOUT exiting; the generation worker reads it at each chapter boundary and, for a sidecar engine, POSTs a new sidecar `POST /recycle` (reuses `_schedule_restart_exit`'s drain→exit→respawn verbatim, idempotent) so the recycle lands BETWEEN chapters and EARLIER — sustained RTF, no mid-chapter cut, zero dropped chapters. `/health` also gains `committed_mb`; the boundary check is sidecar-engines-only, abort-guarded, and best-effort (a flaky probe never blocks generation). Clean at the operative single-worker config; the hard watchdog stays the untouched backstop. Sidecar pytest + `sidecar-health` + `generation-boundary-recycle` vitest landed; full-book live-GPU acceptance pending → stable.
- [161 — Sidecar VRAM-keyed recycle + non-leaky Qwen reload](161-sidecar-vram-reload-recycle.md) — `active`. Closes the 2026-06-01 stall: a long Qwen run that used to cap ~6.5 GB VRAM and run for hours (until the _host_-RAM recycle fired) instead crept past the 8 GB card and STALLED — GPU pinned at 100% util but crawling. Root cause: `QwenEngine.unload()` (unlike `unload_design`) did NOT take `_synth_lock`, so an `/unload` landing mid-synth nulled `_base` under a running forward → its VRAM couldn't be reclaimed → the idempotent `/load` stacked a SECOND copy (measured 7.4→10.8 GB `allocated` across one reload); crossing the 8 GB card then hit the NVIDIA Windows **sysmem fallback** (overflow mapped into host RAM, GPU thrashes over PCIe instead of OOM-ing), and the leak watchdog keyed ONLY on host committed RAM so nothing recycled it. Three levers: (C) `unload()` now takes `_synth_lock` so a reload can't stack a copy, + logs the reserved-VRAM delta; (B) the watchdog gains a reserved-VRAM tier (soft → the SAME `recycle_pending` flag, so [[158-sidecar-soft-recycle-boundary]]'s boundary recycle drives it unchanged; hard → self-exit), keyed on `memory_reserved` vs device total, env `SIDECAR_VRAM_RECYCLE_SOFT_MB`/`SIDECAR_VRAM_RESTART_MB` (default 90%/98%); `/health`+`/debug/memory` expose VRAM headroom; (A) a loud `VRAM SPILL` warning + a Windows-effective `PYTORCH_CUDA_ALLOC_CONF` (`garbage_collection_threshold`+`max_split_size_mb`) + README NVCP "disable sysmem fallback" step. **Corrects [[144-sidecar-cuda-frag-oom]]** — its `expandable_segments:True` default is a silent no-op on Windows; the spawn default now also carries the cross-platform knobs. Distinct from the host variable-shape leak (`side-11` / [[153-sidecar-variable-shape-host-leak]]) and the load-failure orphan ([[155-qwen-load-failure-reclaim]]). Sidecar pytest + `sidecar-health` + `spawn-sidecar` vitest landed; live-GPU acceptance pending → stable.
- [169 — Defense-in-depth for silent generation stalls + leak-saturated sidecar adoption](169-generation-stall-defense-in-depth.md) — `active`. Closes the 2026-06-02 _stellarlune_ ch52 incident where a real story chapter silently stalled mid-render (no `generationError`, no crash, no watchdog) and a restart then ADOPTED the leaked orphan sidecar (~26 GB committed vs a ~10 GB fresh load) and wedged. Three independent layers. **(1)** A whole-chapter no-progress watchdog in `processOneChapter`: a chapter-scoped `AbortController` chained to the job controller, raced against a stall guard, bumped on real completions (`onGroupComplete`/`onBatchComplete`/`onTitleStart` + assembly milestones — NOT `onGroupStart`, which `withHeartbeat` re-fires every ~10s and would mask a wedge) — on no progress for `CHAPTER_NO_PROGRESS_MS` (default 12 min) it aborts and records a `generationError`, covering the previously-uncovered post-synth assembly phase too, so the chapter fails loudly and rehydrates as Failed not the misleading Queued. **(2)** Adopt-fitness gating: `spawnSidecar` reads `committed_mb`/`recycle_pending` off `/health` and refuses to adopt a leak-saturated (`≥ SIDECAR_ADOPT_MAX_COMMITTED_MB`, default 20 GB) or recycling sidecar — killing + respawning a clean one via the stale-sidecar path; the supervisor's adopted-sidecar watchdog now `/health`-polls for fitness (`SIDECAR_ADOPTED_HEALTH_POLL_MS`, default 20s) on top of the cheap TCP disappearance probe, so an alive-but-saturated orphan is replaced, not just one that exits. **(3)** The sidecar memory watchdog samples committed every 15s (not 60s) so a transient per-batch spike trips the soft recycle promptly (the run grazed a 40500 MB ceiling at a ~39.6 GB peak and never recycled); the periodic log reports the rolling peak; `.env.example` recommends setting `SIDECAR_RECYCLE_SOFT_MB` with headroom below the per-batch peak. Server + sidecar tests landed (`generation-stall-watchdog`, `spawn-sidecar`, `sidecar-supervisor`, `test_memory`); live GPU acceptance pending. Contains the variable-shape leak (`side-11`) rather than eliminating it.
- [147 — Sidecar-readiness gate: ride out a respawn instead of failing chapters (srv-17b)](147-sidecar-readiness-gate.md) — `active`. Fixes the 2026-05-30 "sidecar failed → queue failed too" cascade caught DURING the first long fixed-build run. When the host-RAM recycle (plan 143) made the sidecar self-exit and the srv-15 supervisor respawned it (~10–30 s to be HTTP-ready), the preload gate `ensureSidecarEngineReady` gave up on the first connection-refused and let the worker fire synth into the gap — so a BURST of "sidecar not reachable" failures (ch28/29/30) tripped the srv-11 consecutive-failure breaker and paused the whole queue. Fix: the gate now POLLS through a respawn (sidecar unreachable OR model still loading) up to a 120 s budget covering the supervisor backoff `[2s,5s,15s]` + a fresh model load, so the worker WAITS for the fresh sidecar and never dispatches into the gap — no burst, no breaker trip, run continues unattended. Best-effort fallback-to-lazy retained (now reached only after the full budget); breaker untouched (removes the spurious failures at the source). Optional `opts:{timeoutMs,pollIntervalMs}` for test injection; 2-arg call sites unchanged. Addresses the readiness-gate half of `srv-17` (the silent-server-death root-cause stays open). `ensure-sidecar-loaded.test.ts` pins poll-through-respawn + give-up-after-budget + abort-mid-poll. Live acceptance (cross a recycle without a pause) pending → stable.
- [148 — Don't queue (and never hang on) non-narration chapters](148-skip-nonnarration-chapters.md) — `active`. Fixes the 2026-05-31 KOTLC stall (queue hung ~1 h, 4 chapters from done). Two coupled defects around EPUB **back-matter** the parser admits as chapters: (1) the front/back-matter detector `isLikelyFrontMatterTitle` was wired only into the PDF outline reader, so EPUB/text back-matter (Acknowledgments / Copyright / CONTENTS / next-book teaser) was queued like any chapter; (2) a degenerate chapter (a table-of-contents page) made Qwen's open-ended decode run away and **hang the whole queue** with no chapter ever reaching `failed`. **Layer A (prevention):** apply the heuristic at the two state-creation sites (`import.ts`, `manuscripts.ts` re-parse) so detected back-matter defaults to `excluded` — the user's explicit choices still win. **Layer B (the net):** a per-call synth timeout in `synthesise-chapter.ts` (`SYNTH_CALL_TIMEOUT_MS`, env `SIDECAR_CALL_TIMEOUT_MS`, default 600 s, `0` disables) — `withCallTimeout` races each provider call against a timer, aborts a derived child signal on timeout (cancelling the fetch), and throws a non-transient `ChapterSynthTimeoutError` so a runaway chapter fails and the queue **advances** instead of hanging. Layer A is best-effort title matching; Layer B is the format-agnostic safety net. Tests: import auto-exclude w/o excludedSlugs, synth timeout fires + aborts + doesn't fire on a fast call. Live acceptance (re-import KOTLC → 52–59 pre-excluded) pending → stable.
- [149 — Qwen persona display fallback + voiceStyle backfill](149-qwen-persona-display-backfill.md) — `active`. Fixes the 2026-05-31 "voice persona missing" on a reused/origin Qwen character whose voice is correctly designed (the drawer's "Voice persona" textarea read blank, and a re-design 400'd). Cause: the persona was persisted only on the voice sidecar `voices/qwen/<name>.json` `instruct`, never mirrored onto `character.voiceStyle` (reuse copies the override, not the persona; even origin books lacked it), and the drawer + design route both read `character.voiceStyle`. Two layers mirroring plan 138's read-time-resolve + denormalize: (1) **runtime fallback** — new `GET /api/books/:bookId/cast/:characterId/designed-persona` returns the sidecar `instruct` (200 `{instruct:''}` when absent, 404 only for unknown book/char; resolves the override name else derived `qwen-<voiceId>`), and the drawer lazily seeds the textarea from it when `voiceStyle` is empty (never clobbers user edits), unblocking re-design; (2) **backfill** — `scripts/backfill-qwen-voicestyle.mjs` (dry-run default, `--apply`, `.bak`, `BASE` env) writes each designed voice's `instruct` onto the empty `voiceStyle`, idempotent (ran live on `C:\AudiobookWorkspace`: 5 files, 105 personas, 0 unresolved). New `qwenVoiceSidecarPath`/`qwenVoicesDir` path helpers centralise the `voices/qwen` dir. The separately-reported stale `Fallback (Kokoro)` pill on the same characters is data (a pre-backfill render), not code — it clears on regeneration. Server + drawer tests; live acceptance pending → stable. Follow-up landed as plan 150 (denormalise `voiceStyle` at the auto-match write site, sibling of srv-14).
- [150 — Denormalise Qwen persona (`voiceStyle`) at the auto-match write site](150-srv18-voicestyle-denormalise-write.md) — `active`. Closes the write-time gap plan 149 deferred (`srv-18`): plan 149 healed existing data once + resolves the persona at read-time, but a freshly designed-then-reused Qwen character still landed without `voiceStyle` on disk until the next backfill. Now the three reuse write sites copy the source character's `voiceStyle` alongside the bespoke voice they already denormalise — `cast-link-prior.ts` (manual link, inline block), `series-reuse-link.ts` (analysis-time series auto-match), and `book-state.ts` (`PUT /state` cast funnel). The core change is in the shared resolver `hydrate-reused-voice.ts` (`ReuseHydratable`/`ResolvedReusedVoice` gain `voiceStyle`; `hydrateCharacterVoice` merges `character.voiceStyle ?? resolved.voiceStyle` so own-wins, never clobbers), which auto-fixes the latter two; the inline `cast-link-prior` block adds the persona copy inside its existing `shouldDenormaliseVoice` gate. Persona denormalisation rides the SAME gate as the voice override (fires only for a reuse lacking its own qwen voice); the narrow residual stays covered by 149's read-time fallback. Sibling of srv-14. Server tests across all four files; live acceptance pending → stable.
- [160 — Align the Qwen voice-design persona prompt with the official VoiceDesign format](160-voicedesign-persona-format.md) — `active`. Checks our `buildVoiceStylePrompt` (`server/src/analyzer/voice-style.ts`) — the Gemini call that turns a character profile into the natural-language `instruct` fed to `Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign` — against Qwen's official VoiceDesign guidance and closes two gaps on the length/structure/inclusions axes: the persona was a bare ~30-word comma fragment with **no purpose/scenario clause** and **no pitch dimension**, and drifted into psychology ("a hint of anxiety") against the model's objectivity principle. New prompt requests a 15–40-word descriptive sentence covering gender/age/**pitch**/pace/timbre/emotion that **ends with a purpose clause** ("…for audiobook narration" / "for expressive character dialogue"), encoding Qwen's five principles (specificity, multidimensionality, objectivity, originality, conciseness). Profile block + evidence quotes + `cleanPersona` + the per-character one-call discipline unchanged; English-only `instruct` preserved. New/re-generated personas only — no migration of existing books' personas or already-designed voices. Server test pins the format contract; GPU audition validation owed.
- [153 — Durable per-chapter failure status + queued-row "Generate this chapter" + generated-time](153-stuck-queued-failure-status.md) — `active`. Closes the 2026-05-31 "Ch14 stuck on Queued when it actually failed" report. A failed chapter wrote no audio, so it was absent from `completedSlugs` and re-hydrated as the neutral **Queued** once its (clearable) `.queue.json` entry was gone — and a queued row offered only Rename/Exclude, so it was unactionable. Three coupled fixes: (1) **durable failure status** — new optional `generationState:'failed'` + `generationError` on `state.json` chapters (no schema bump), written by the generation route's failure `catch` and cleared on a successful render, so `hydrateFromBookState` shows **"Failed · reason"** with **Retry** after a reload/queue-clear (`done`-on-disk still wins over a stale flag); (2) **escape hatch** — a per-row **"Generate this chapter"** action on queued rows enqueues just that chapter (mirrors the drift-bulk enqueue → dispatcher → stream), unblocking any pre-existing stuck chapter; (3) **generated-time** — done rows show `Generated <relativeTime(audioRenderedAt)>` with the absolute date/time on hover. Server + slice + view + e2e tests; live acceptance pending → stable.
- [154 — A local Qwen timeout no longer reports "Gemini TTS rate-limited" (and no longer stops the run)](154-false-gemini-rate-limit-misclassify.md) — `active`. Closes the 2026-05-31 KOTLC _Stellarlune_ CH24 incident: a 100%-local Qwen run (every chapter `audioModelKey: qwen3-tts-0.6b`) flipped to **"Synthesis failed — Gemini TTS rate-limited — stopped run"** and paused the whole book — with **no Gemini request ever made** (Gemini is analysis-only here; Gemini TTS has no free tier). Two root causes. (A) `describeSynthesisError`'s quota regex `/429|quota|rate/i` matched the bare token **`rate`** inside **"dege·nerate"** — the word in the `ChapterSynthTimeoutError` message thrown when a post-recycle synth stalled into the 600 s ceiling ([[148-skip-nonnarration-chapters]] layer B) — so a local timeout was tagged Gemini-quota **and** `fatal:true` (the timeout was _meant_ to be non-fatal/skip). Fix: tighten the match to a real `429` or an unambiguous phrase (`rate[-\s]?limit`/`quota`/`too many requests`/`resource…exhausted`), add `describeSynthesisError(err, engine?)` engine-gating so a local engine is never blamed on Gemini, and classify `ChapterSynthTimeoutError` explicitly as non-fatal FIRST. (B) that timeout is non-transient, so it bypassed the srv-17c recovery loop ([[148-recycle-inflight-recovery]]); now it's recoverable there too — a stall waits on the readiness gate and re-renders, bounded by `MAX_RECYCLE_RECOVERIES`, then skips non-fatally. So one stalled chapter never halts the book again. The Qwen reload VRAM leak that _forces_ the recycles is `side-11` (separate). Server tests landed; live acceptance pending.
- [155 — Qwen load-failure VRAM reclaim (stop failed reloads orphaning CUDA tensors)](155-qwen-load-failure-reclaim.md) — `active`. Root-causes the orphaned-VRAM leak surfaced during the plan-154 CH24 hunt: live `/debug/memory` showed ~9.9 GB CUDA **allocated with zero models loaded**, which oversubscribed the 8 GB card and forced the recycle that broke CH24. Cause: `QwenEngine._load_qwen_model` had **no reclaim on its failure path** — a load that materialised weights then raised on `inner.to(device)` (CUDA OOM partway through the move under existing pressure) left a partial `nn.Module` whose reference cycles keep its tensors alive past the failing frame, and `_ensure_*_loaded` never assigned it, so repeated failed reloads accumulated VRAM. Fix mirrors `unload()`: wrap the load so any failure drops the partial + runs `_reclaim_host_and_vram()` (gc + empty_cache) before re-raising. Paired `test_qwen_load_reclaim.py` pins the reclaim-on-failure contract (CI has no GPU). Distinct from the committed-host variable-shape leak that _forces_ the recycle (`side-11` / plan 153 MKLDNN A/B) and recycle-at-chapter-boundary (`side-11` next round). Sidecar tests green; live acceptance pending.
- [173 — Structured failure taxonomy + plain-language remediation (fs-19)](173-failure-taxonomy.md) — `active`. One ordered first-match-wins signature table (`failure-taxonomy.ts` → `classifyFailure`) maps recurring failure modes (sidecar-unreachable, vram-spill, oom, disk-full, model-not-loaded, analyzer-rate-limit, synth-timeout, xtts-speaker-desync, cuda-poisoned, auth) to `{ code, userMessage, remediation, fatal }`. `describeSynthesisError` now delegates to it while keeping its legacy `{errorReason,fatal}` contract (incident regexes ported verbatim — must not regress plan 154's narrow-quota fix); `generation.ts` persists `generationErrorCode`/`generationRemediation` + carries them on the `chapter_failed` tick; the Generate view + toast show the friendly message + a "What to do:" line. Unknown errors fall back to the trimmed raw string, never fatal. Closes #469; server + frontend tests green, live multi-failure acceptance owed. Analysis-path classification deferred.
- [174 — Post-synthesis audio QA gate (advisory) (srv-27)](174-audio-qa-gate.md) — `active`. Before a chapter completes, `audio-qa.ts` `evaluateChapterQa` runs cheap validators reusing the loudnorm first-pass stats (`i` LUFS / `tp` true-peak) + duration-vs-expected (chars ÷ ~14 cps): near-silent / clipped / truncated / runaway → a `ChapterQaVerdict { status:'ok'|'suspect', reasons[] }` persisted as `audioQa` on the chapter (conservative env-tunable thresholds). **Advisory** — the chapter still flips `done`; an amber "Suspect" badge (reason tooltip) shows in the Generate + Listen rows. No second audio pass. Closes #465; server + frontend tests green, live acceptance owed.
- [175 — Per-run resource telemetry + admin trend panel (fs-20)](175-resource-telemetry.md) — `active`. A rolling capped JSONL (`resource-telemetry.ts`, `<WORKSPACE_ROOT>/.telemetry/`, cap 2000, best-effort fire-and-forget append after `recordChapterThroughput`) records per-chapter RTF / VRAM reserved+total / committed host RAM / wall-time; `GET /api/generation/telemetry` serves it; a new **Resource trends** section in the Admin watch console (`src/views/admin.tsx`, after Generation throughput, before the DEV worktrees list) tabulates it with a hand-rolled inline-SVG RTF sparkline (no charting dep). Telemetry never blocks the generation hot path. Closes #470; server + frontend tests green, live acceptance owed.
- [176 — Per-character re-record / splice (fs-26)](176-character-splice.md) — `active`. Surgically re-mix or re-record ONE character's segments in a rendered chapter instead of regenerating the whole chapter. A pure byte-range splice engine (`splice-chapter.ts`) decodes the chapter PCM, substitutes the target segments (a `volume` gain for **remix** — the fix for "too quiet", no GPU; or fresh synthesis for **rerecord**), copies head/gaps/title-silence/tail verbatim, re-times every segment (`round`-not-floor sec↔byte, `runningBytes===len` invariant), then re-encodes via the shared `finalize-chapter-write.ts` tail (loudnorm re-pass + peaks/lufs + `.previous.*` preservation + atomic write + state.json). A relative gain boost survives the whole-chapter loudnorm because it targets integrated loudness. `POST /chapters/:id/splice` (SSE); `chapter-job-coordination.ts` mutually displaces a splice and a full regen of the same chapter. UI: a "Fix audio" button on the cast profile drawer opens a modal (Loudness slider / Re-record toggle + a checkbox list of the rendered chapters the character appears in) that runs one splice per chapter and drops a pending A/B revision each. `buildCharacterSnapshots` extracted + shared so drift snapshots match a regen. The line-level Listen-view per-sentence re-record entry + `srv-29` generation-tail convergence landed 2026-06-05. Closes #480; server + frontend + e2e tests green, live-GPU re-record acceptance owed.
- [184 — Meaningful progress for single-chapter re-analysis](184-reanalyse-progress-indicator.md) — `active`. The per-chapter **Re-analyse** row (183) + un-exclude flow reused the whole-book progress `0.02 + 0.93·(done/total)`, so for one chapter it **pinned at 2%** the whole stage-1 call then jumped — users thought it hung (observed live on Stellarlune ch16). New pure `lib/reanalyse-progress.ts` `computeReanalyseProgress` maps the two real LLM steps onto phase **bands** (detect `[2%,40%]`, attribution `[40%,97%]`) and fills each via an asymptotic ease on the **heartbeat's per-call elapsed** (`1−e^(−t/τ)`), snapping to the band top when the server reports the phase done; monotonic by construction. `generation.tsx` now wires the already-supported `onHeartbeat` (subset client parsed it; the row ignored it) through a new `applySubsetTick`, and the row shows a live `· 0:08 · 512 chars/s` readout alongside the existing "waiting on rate limit" note. Frontend-only — no server/analyzer change, no slowdown. Whole-book bar / sticky pill mapping left as a follow-up. Unit + component tests green.
- [151 — Per-book `state.json` auto-backup (srv-2)](archive/151-state-json-auto-backup.md) — `stable` (archived). Scheduled background sweep snapshots every book's `state.json` on a configurable cadence (daily/weekly), keeps the newest N (retention default 14), and exposes manual list + restore — disaster recovery for the highest-value per-book file (chapter list, metadata, cover, tags, format/parser stamps). Settings toggle + cadence/retention + per-book Restore picker in the Account view. New `auto-backup.ts` (sweep/list/restore + `setInterval` scheduler) and `/api/books/:bookId/backups` (list / now / restore) routes; server + frontend tests. Shipped 2026-05-31 in the ops-3/ops-6/fe-17/side-5 integration round (commit `1feccba`); renumbered from 150 (collided with the merged srv-18 voiceStyle-denormalise plan). Close-out round (2026-06-03) added the route-contract test (`server/src/routes/backup.test.ts`) + the restore-success library refresh (#424) and archived the plan.

- [193 — In-app Model Manager (fs-23, +srv-21, +ops-7)](193-fs23-model-manager.md) — `stable`. Consolidates every model install / inventory / residency control out of the Account view into one **Model Manager** (`#/models`, launched from Admin). New `GET /api/models/inventory` (Node sizes the weight dirs directly + folds in sidecar `/health` residency + Ollama tags — works with the sidecar down) lists each model with present? · size · disk path · live residency; rows Load/Unload via the existing `ModelControlPill`, Install/Update via the existing installers, and **Remove** via `POST /api/models/:id/remove` guarded by `evaluateRemoval` (409 for loaded / current-default / fallback-Kokoro; absent = idempotent; Windows EBUSY → `files-locked`). The model-flavored settings (defaults, analyzer split, TTS-sidecar, server config minus workspace-dir) move into a `ModelSettingsForm` that Saves a partial `UserSettingsPatch` so it coexists with Account's own Save; shared form primitives extracted to `account-forms.tsx`. Folds in **srv-21** (validate the outbound `sidecarUrl` at the `getResolvedSidecarUrl` chokepoint — http(s) loopback/private only, else fall back to the local default; closes the SSRF) and **ops-7** (pin `model-hashes.json`; `install-kokoro.ps1` Get-FileHash-verifies each download and `install-qwen3.mjs` SHA256-verifies the FA2 wheel before install, refuse+delete on mismatch; a lightweight size-match integrity badge in the inventory). fe-22 subsumed. Closes #476/#426/#430. Server + frontend + e2e; live-GPU acceptance owed.

### H. Listen & playback

- [159 — Listen-row affordances gated on generated audio](159-listen-ungenerated-chapter-affordances.md) — `active`. Listen-view chapter rows for chapters that haven't generated audio yet (`state` of `queued`/`in_progress`/`failed`) no longer masquerade as playable: the Play + Share-clip buttons are `disabled`, and a state-aware label (`Queued`/`Generating…`/`Failed`) replaces the misleading "0:00" duration. Reuses the existing `state === 'done'` "has audio" predicate; Regenerate stays active (it can start/retry generation). Single-component change in `listen-player-region.tsx` with paired unit coverage.

### K. Cross-cutting invariants

- [199 — Advanced Settings configuration surface](199-advanced-settings.md) — `active`. A `#/advanced` view (reached from Admin + Account) exposing ~70 model/generation/QA knobs via a collapsible accordion; each knob is an `OverrideRow` (edit → override, revert → shipped default, env-sourced knobs read-only "locked by .env"), analyzer prompts forkable to local `.md` files via `PromptRow`, and a "Restart sidecar" amber banner for `restart-sidecar` knobs. Backed by a server-side `config.json` registry with `GET /api/config` + `PUT /api/config` + reset routes; `KnobDescriptor.apply` drives banner logic automatically for any future knob. Paired Vitest unit + 6 Playwright e2e specs + responsive coverage.
- [171 — In-app upgrade pathway (fs-1, versioned-directory)](171-in-app-upgrade.md) — `active` (implemented on `feat/server-in-app-upgrade`, pending the v1.6.0 cut). Turns cross-version upgrades into a one-click **Account → Application updates** flow for hand-delivered alpha bundles, on a new **versioned-directory** install layout (`releases/vX.Y.Z/` + stable `launch.mjs` + shared `workspace/`/`venv/`/`models/` siblings) so the running release is never touched and rollback is just not flipping the `.current-version` pointer. Six phases: env-knob path config (`APP_LOG_DIR`/`APP_RUN_DIR`/`SIDECAR_VENV_DIR`), the launcher + one-time `setup-versioned-install.mjs` + `restart-after-upgrade.mjs`, a boot coordinator that backs up every workspace JSON to `.upgrade-backups/from-<old>-to-<new>-<iso>/` before migrating (generic `schema-migrate.ts` seam, all v1/identity today) + additive `lastSeenAppVersion`/`showWhatsNew` on user-settings, the `/api/upgrade/{stage,apply,abort,state}` endpoints (multer + yauzl, **409** busy / **412** downgrade, extract-to-sibling → npm ci → conditional pip → atomic pointer flip → detached restarter → SIGTERM self), `/api/info` + sidecar `version.py`/`__version__` + `bump-version.mjs` three-way lockstep + `RELEASE_NOTES.md` baked into the zip, and the Account `UpgradeCard` + `UpgradingScreen` + top-bar version pill + what's-new banner + `upgrade-slice`. Seed-release honesty: the jump INTO 1.6.0 is manual; the first self-upgrade is 1.6.0→1.7.0. Closes #395.
- [166 — GitHub Issues for backlog detail + bug tracking](166-github-issues-backlog-integration.md) — `active`. Moves the canonical _detail_ of every backlog item into a 1:1 GitHub issue (`<prefix>-<n> — <what>` title; the `<prefix>-<n>` ID stays the durable cross-reference, `#NN` is the auto-close hook) and makes bugs `bug`-labelled issues that stay **off** `docs/BACKLOG.md`, which becomes the thin prioritized MoSCoW planning view. Ships three dry-run-by-default Node scripts — `gh-labels.mjs` (the 14-label taxonomy as code, upserted via `gh label create --force`), `migrate-backlog-to-issues.mjs` (idempotent bulk-file keyed on the ID token parsed from each title; exports the pure `parseBacklogItems`/`issueIdFromTitle`/`issuePayload` helpers, node:test-covered via `test:hooks`; emits `docs/backlog-issue-map.json`), and `thin-backlog.mjs` (one-shot BACKLOG.md rewrite from the map, reversible via git) — plus YAML issue forms (`backlog-item`/`bug`/`config` with `blank_issues_enabled:false`) and the CONTRIBUTING.md §Issues + CLAUDE.md §backlog conventions (`Closes #NN`/`Refs #NN` linking, plan-vs-no-plan threshold). Struck-through/RESOLVED entries (srv-17) are reported, not filed. Tooling+docs in PR 1; the live migration + thinning run as PRs 2–3.
- [163 — Protected-branch pre-push guard](163-protected-push-guard.md) — `active`. GitHub's "main isn't protected" nudge points at branch protection / rulesets, both **403** on this free private repo ("Upgrade to GitHub Pro or make this repository public"). Maintainer stays free + private, so the chosen rule (block force-push + deletion of `main`) is enforced at the only point we control: a new `pre-push` guard. `scripts/guard-protected-push.mjs` exports a pure `evaluatePush(stdinText, { isAncestor })` (mirrors the `validate-commit-msg.mjs` validator pattern — DI'd ancestry so it's unit-testable with no real git) that blocks a deletion (local sha all-zero) or non-fast-forward force-push to `refs/heads/main`, allowing ref creation + every feature-branch push. `.husky/pre-push` runs it first (`… || exit 1`) so a force/delete is refused before the ~15-min verify. Bypassable on purpose with `git push --no-verify`. The ready-to-run server-side ruleset command for the day the repo goes Pro/public lives in the plan + backlog `ops-9`. node:test coverage under `npm run test:hooks`.
- [164 — Dependency & CI-action hygiene (ops-8 · srv-4 · safe bumps)](164-deps-ci-hygiene.md) — `active`. Bumps every GitHub Action off the deprecated Node-20 runtime ahead of the 2026-06-16 force / 2026-09-16 removal deadline (`checkout@v4→v6`, `setup-node@v4→v6`, `cache@v4→v5`, `upload-artifact@v4→v7` across all 5 workflows + the shared `setup` composite); re-audits the `srv-4` deprecation chains (genai `^2.0.1→^2.7.0`, `node-domexception` chain re-confirmed upstream-blocked, no `glob` sub-chain); lands the safe minor bumps (TypeScript → latest 5.x, root `engines.node` floor + `.nvmrc`=24); and files every framework major now behind (React 19, Vite 7/Vitest 3, Tailwind 4, Express 5, Zod 4, react-router-dom 7, TypeScript 6, pdfjs-dist 5) as research-complete BACKLOG entries. No runtime-code delta; ops-8 acceptance is the PR's annotation-free CI run.
- [167 — Frontend React-cluster + TypeScript major upgrade (fe-19 · ops-10 · fe-18 · fe-21)](167-fe-react-cluster-upgrade.md) — `stable` (shipped 2026-06-02, PR #454). Executes the plan-164-deferred dependency-major round as one branch, four stages: **fe-19** `vite ^5→^8` (Rolldown + Oxc + Lightning CSS) + `vitest ^2→^4` (root + server) + `@vitejs/plugin-react ^4→^6`; **ops-10** `typescript ^5.9→^6` (typescript-eslint ^8 already supports it); **fe-18 + fe-21** `react/react-dom ^18→^19` + `react-router-dom ^6→^7`, shipped as ONE atomic commit because react-router 6.26 is **not React-19-compatible** (`navigate()` from the Layout stage→URL effect is dropped under React 19 — `generation-resume.spec.ts`; bisect: React 18 + Vite 8 passes, React 19 fails; router 7 fixes it with zero code changes). Targets moved past 164's research — latest majors at execution were Vite 8 (Rolldown) / Vitest 4 / plugin-react 6, taken over the conservative Vite 7 / Vitest 3. Notable non-version work: function-form `manualChunks` → `advancedChunks.groups` (Rolldown), Vitest-4 `poolOptions`→`maxWorkers` across all three vitest configs + `vi.fn<Sig>()` typing fixes + a `routes/prefetch` mock in `layout.test.tsx` (Vitest 4 now fails on post-teardown unhandled rejections), an npm `overrides` pinning `openapi-typescript`'s TS-6 peer, and — caught only by the e2e app-boot — switching `redux-persist` storage `lib/storage`→`es/storage` because Rolldown leaves the CJS default wrapped (`storage.getItem` undefined → rehydrate threw). No behaviour change → no new behavioural test (existing battery is the regression net); the stale `library` visual baseline (predated the 2026-06-01 language-UX feature) re-baked. Closes #403/#404/#407/#408.
- [170 — Dependency major upgrades round 2 (srv-25 · srv-24 · srv-26 · fe-20)](170-deps-majors-zod-express-pdfjs-tailwind.md) — `stable` (shipped 2026-06-02, PR #489, merge `ffc3016`). The last four plan-164-deferred framework majors, one branch / four commits. **srv-25** `zod ^3→^4` + **deletes `zod-to-json-schema`** (Zod 4's native `z.toJSONSchema({ target:'draft-07', reused:'inline' })` rebuilds the Ollama constrained-decoding schema; no `.email/.uuid` validators in the tree so the codemod was a no-op; `handoff/schemas.test.ts` pins the `additionalProperties:false`/`minItems:1`/no-`$ref` contract). **srv-24** `express ^4→^5` + `@types/express ^5` — the type widening of `req.params` to `string | string[]` (127 errors) handled by a `server/src/http.ts` re-export narrowing `Request` params back to `Record<string,string>` (every route imports `Request` from there, not `'express'`); Express-5 `res.sendFile` now defaults `dotfiles:'ignore'` so `cover`/`share` (both serve `.audiobook/…`) needed `dotfiles:'allow'`; plus a new last-mounted `error-handler.ts` for Express-5 async-rejection propagation. **srv-26** `pdfjs-dist ^4→^5` — legacy ESM import retained, dropped v5-removed `isEvalSupported`, added a committed fixture + un-mocked `pdf-outline.real.test.ts` (the existing `pdf.test.ts` mocks pdfjs). **fe-20** `tailwindcss ^3→^4` via `@tailwindcss/upgrade` (`@import 'tailwindcss'` + `@theme`, `tailwind.config.ts` deleted, autoprefixer dropped, 62-file utility renames `outline-none→outline-hidden`/`shadow-sm→shadow-xs`); hand-re-added the `coarse-pointer`/`fine-pointer` `@custom-variant`s the codemod dropped + fixed a z-index regex; only `library-dark` win32 baseline crossed the 0.05 drift (re-baked), Linux baselines regenerated post-merge via `regen-visual-baselines.yml`. Closes #405/#406/#410/#409.
- [202 — Dependency major upgrades round 3 (srv-38 · srv-39 · ops-13 · ops-14 deferred)](202-deps-majors-round-3.md) — `active` (branch `worktree-deps-round-3`, pending merge). The majors a fresh `npm outdated` surfaced after plan 170, one branch / four commits. **srv-38** `pdf-parse ^1→^2` — class-API rewrite (`new PDFParse({data}).getText()/getInfo()/destroy()`), self-typed so `@types/pdf-parse` dropped; `getText({pageJoiner:''})` suppresses v2's default `-- N of M --` per-page marker; `getInfo().info` keeps the v1 `Title`/`Author` keys. **srv-39** drops the direct `pdfjs-dist` dep entirely (the spec's "bump to 6" pivoted): pdf-parse 2 bundles its own pdfjs, and a second top-level copy collides (`API vN != Worker vM`) under tsx/`npm run dev`; the outline now reads from `getInfo().outline` via the pure `extractOutlineTitles`, so one pdfjs / one doc load. The renamed `pdf-real.test.ts` drives the real v2 path and is routed to the serial `vitest.config.slow.ts` (it crashes the parallel fork pool, like `generation-boundary-recycle`). **ops-13** `concurrently ^8→^10` + `jest-axe ^9→^10` + `@types/supertest ^6→^7` + `@types/yauzl ^2→^3`; plus a `npm update` tail sweep (react-router 7.17, typescript-eslint 8.61, prettier 3.8.4, @google/genai 2.8, undici 8.4, yauzl 3.4). **ops-14** `eslint ^9→^10` DEFERRED — upstream-blocked (latest `eslint-plugin-react`/`-jsx-a11y` still cap eslint peer at `^9`). `@types/node` held to the Node-20 line. No behaviour change; the battery + the new real-path PDF test are the net. Closes srv-38/srv-39/ops-13; refs ops-14, srv-4.
- [158 — Break the tts/index ↔ provider cycle (partial-mock release-gate flake)](158-tts-index-cycle-mock-flake.md) — `stable`. Root-causes the intermittent `[vitest] No "isTtsModelKey" export is defined on the "../tts/index.js" mock` that failed the v1.5.1 cross-OS gate (win+mac). `tts/index.ts` imported the provider classes while `gemini.ts`/`sidecar.ts` imported pure helpers back from `index.js` — a runtime cycle, so `importOriginal('../tts/index.js')` in the 7 route tests that mock it returned a partial namespace under the parallel pool. Fix: move the pure helpers (`isTtsModelKey`, `engineForModelKey`, `sidecarModelId`, `resolveGeminiModelId`, `TTS_MODEL_LABELS`, `TtsModelKey`/`TtsEngine`) to a new leaf `tts/model-keys.ts`; providers import the leaf; `index.ts` re-exports it (`export *`) so the public surface is unchanged. Identity guard in `index.test.ts` locks the re-export so the cycle can't re-form.
- [157 — Default-bind the HTTP server to loopback (srv-19)](157-srv19-loopback-bind.md) — `draft`. Closes 2026-05-31 security-review findings #1+#2: plain-HTTP dev mode bound `0.0.0.0`, so every unauthenticated route + the `/workspace` static mount (all manuscripts/audio/`state.json`/`cast.json`) was reachable by any LAN peer. New pure `selectBindHost(lanHttps, env)` helper (`server/src/bind-host.ts`) → default bind `127.0.0.1`; the opt-in `LAN_HTTPS` mobile flow keeps binding all interfaces (deliberately reachable); `BIND_HOST`/`HOST` env override restores all-interface HTTP for power users. One host arg threaded through the existing `index.ts` listen wiring; `export-lan` LAN/port/CORS tests stay green. Cheapest hardening from the review; the rest (srv-20/21/22, side-12, ops-7) deferred.
- [156 — Pre-commit scope filter + GPU-contention test throttle](156-precommit-scope-contention.md) — `active`. Pre-commit (`verify:fast:scoped`) now skips any leg whose scope the **staged diff** never touched (`scripts/verify-cache.mjs --scope-staged`, mirroring `verify.yml`'s scope matcher), so a sidecar-only/docs-only commit runs none of the frontend/server suites — closing the green-only-cache hole where a contention flake forced out-of-scope re-runs. Plus a soft GPU-contention guard: `verify-cache.mjs` probes `nvidia-smi` and sets `LOW_CONCURRENCY=1` (frontend pool → half-cores, server `maxForks` 2→1; otherwise plan-45 defaults) when a generation run is hammering the box. Pre-push still runs the full battery.
- [118 — CI cost reduction round 2 (run-count + test-impact)](118-ci-cost-round-2.md) — Stacks on plans 103/106. Four levers against Linux runner-minute spend: (1) explicit `timeout-minutes` on every job (`verify` 15→20, `pr-title-lint` 5, `release` verify 20 / publish 15) to kill the timeout double-charge + 360-min runaway; (2) **draft-by-default** convention — `verify.yml` already skips drafts + re-fires on `ready_for_review`, so develop-on-draft → local verify → `gh pr ready` bills ONE run per PR; (3) **one integration PR per parallel-agent round**, verified once, instead of N PRs; (4) **test-impact selection** — the frontend + server vitest legs run `vitest --changed <base>` (diff-affected tests only), with `forceRerunTriggers` on the runner-injected `src/test/setup.ts` closing the only module-graph gap. e2e stays scope-gated (Playwright `--only-changed` is unsafe for app-source changes). Full suite preserved on local pre-push + weekly cross-os.
- [132 — Code-statistics tooling](132-code-stats.md) — `scripts/code-stats.mjs` (`npm run stats` / `… -- --write`) wraps `tokei` into a SLOC-per-language table with a production-vs-test split, rewritten idempotently into a `<!-- CODE-STATS -->` block in `brand/project-narrative.md` (local-only) Appendix B. `bump-version.mjs` runs `--write` best-effort on every release so the numbers self-refresh. tokei stays a local-only maintainer dependency (never added to CI); pure helpers (`classifyFile`/`summarize`/`renderMarkdown`/`replaceBlock`) are unit-tested without the binary, and a missing tokei never blocks a release cut.

### M. Deferred / future work

- [188 — Android (Flutter) companion app](188-android-companion-app.md) — `active` (epic; backlog prefix `app` + MVP server prereqs `srv-32`/`srv-34`/`srv-35`, follow-up `srv-33`). Umbrella spec for a native, Android-first listening companion that **delta-syncs a constantly-regenerated library** (per-chapter audio pulls, NOT whole-book M4B re-exports) with **two-way resume sync**, offline playback, and Bluetooth/lock-screen controls — the differentiator vs. the export→sideload→third-party-player flow. Flutter (`just_audio`/`audio_service`) so iOS is incremental; four iOS-readiness principles (the key one: **app-managed, auto-verified TLS trust** by pinning the fetched `/cert/root.crt` against a QR-embedded fingerprint, no OS cert install). MVP server contracts: `srv-32` (two-level gzip'd sync-manifest, `?since` + active-ID set + per-chapter `uuid`/`urlSuffix`), `srv-34` (listen-progress `listenedAt` + guarded compare-and-set), `srv-35` (stable per-chapter `uuid` — also repairs the web player). Holds a v1 definition-of-done, a 7-wave delivery roadmap, and an **"External dependencies (D1–D11)"** map of every cross-area change. Four 2026-06-06 reviews folded in (secondary + cross-item coherence + edge-case). **Build started 2026-06-06: foundation shipped** — `srv-34`, `srv-20`, `app-1` (scaffold + CI), `app-2` (pairing + TLS pinning + API client); the app runs on a Pixel 10 Pro emulator. **MVP shipped + live-validated on a real device 2026-06-07** — server prereqs `srv-32`/`srv-34`/`srv-35` + `app-1`…`app-9`/`11`/`13`/`14` + `srv-33` (PRs #600/#601/#602 and the per-item PRs). Follow-ups remain (now in the backlog under Should): `app-10` (blocked on a loopback proxy), `app-12` (iOS), `app-15` (legacy icons). See the plan's **"Build progress & dev setup"** section.
- [129 — Qwen decode CUDA-graph / static-cache spike](129-qwen-decode-cuda-graph-spike.md) — `deferred` (backlog `side-7`). The only path past the dispatch-bound RTF ceiling to sub-1, but blocked: `qwen_tts` has no static cache + a nested per-step `code_predictor.generate()`. **Probe-gated**: profile the batch-16 decode loop FIRST (batch-16 may already be ~compute-bound, leaving graphs nothing to capture); only then audit the nested loop; only then consider the 2–5-day static-cache fork. Post-1.5.0, after length-bucketing (plan 128).
- [178 — CPU-only analyzer device](178-cpu-only-analyzer.md) — `deferred` (backlog `srv-30`, #507). Run the local Ollama analyzer **CPU-only** (`num_gpu:0`, system RAM) per-model so a large model (e.g. **Gemma 4 12B**, which won't fit the 8 GB GPU) can be used without touching the GPU. The load-bearing trick: a CPU model **skips the GPU semaphore** (`server-side resolveAnalyzerDevice` → `ollama.ts` branch), so CPU analysis and GPU TTS run **concurrently** instead of evicting each other — Phase 0 (small GPU model) + Phase 1 (big CPU model) side-by-side. New `model-device.ts` + `ANALYZER_CPU_*` knobs; required wiring so `/api/ollama/load` matches the device and the TTS auto-evict skips CPU models; Gemma 4 entry env-gated until validated (brand-new). From the 2026-06-04 Gemma-12B hardware evaluation (12B Q4 spills the 8 GB GPU → CPU/RAM is the right deployment). Design-only; owes live-box validation.

## Status legend

In-body banner (legacy — still valid for plans that haven't been ported to frontmatter):

- **stable** — feature is end-to-end functional; assert real behavior.
- **KNOWN: scaffolded** — UI/contract is in place but parts are mocked or partial; assert only the documented behavior.
- **KNOWN: backend-pending** — frontend done, backend stub returns empty/canned data; mock mode exercises the UI, real mode is intentionally a no-op.
- **KNOWN: operational dependency** — works but requires a sibling process the user must start (TTS sidecar).

Frontmatter (canonical going forward — see "Plan lifecycle" above for the full set).

## Shipped (archive)

Plans that shipped and are no longer load-bearing for in-flight work live in
[`archive/`](archive/README.md). The git log is the changelog; this section is a
complete breadcrumb (numerically sorted) so cross-references still resolve.

> **Plan-number collisions (historical).** Four numbers were assigned twice during parallel sessions and are intentionally left as-is (renumbering would break inbound links): **127** — release-cross-os-gate (archived) vs generation-rtf-telemetry (active); **137** — reopen-never-auto-enqueues (archived) vs sidecar-fetch-timeout (active); **167** — coqui-in-app-installer (archived) vs fe-react-cluster-upgrade (archived); **32** — audiobook-export vs sticky-analysis (both archived). Disambiguate by slug, not number.

- [00 — Stage machine](archive/00-stage-machine.md) — Stage machine.
- [01 — Hash router](archive/01-hash-router.md) — Hash router.
- [02 — Upload (paste or file)](archive/02-upload-paste-or-file.md) — Upload (paste or file).
- [03 — Import & confirm metadata](archive/03-import-confirm-metadata.md) — Import & confirm metadata.
- [04 — Analysing view & SSE progress](archive/04-analysing-view-progress.md) — Analysing view & SSE progress.
- [05 — Manual handoff analyzer (retired)](archive/05-analyzer-manual-handoff.md) — the file-drop cowork analyzer; engine removed in 71b35a8, residue cleaned up. `server/handoff/` now serves Gemini forensic traceability only.
- [06 — Gemini analyzer (ANALYZER=gemini)](archive/06-analyzer-gemini.md) — Gemini analyzer (ANALYZER=gemini).
- [07 — Audio tag vocabulary (UI ↔ parser sync)](archive/07-audio-tag-vocabulary.md) — Audio tag vocabulary (UI ↔ parser sync).
- [08 — Audio tag auto-detection (server-side)](archive/08-audio-tag-auto-detection.md) — Audio tag auto-detection (server-side).
- [09 — Voice match pipeline](archive/09-voice-match-pipeline.md) — Voice match pipeline.
- [10 — Profile drawer](archive/10-profile-drawer.md) — Profile drawer.
- [11 — Batch character regenerate](archive/11-batch-character-regenerate.md) — Batch character regenerate. — **Removed** (superseded by [114](archive/114-profile-regen-preview.md)).
- [12 — Manuscript view](archive/12-manuscript-view.md) — Manuscript view.
- [12a — Fix — sentence reassignment scoped by (chapterId, id)](archive/12a-fix-reassign-cross-chapter-id.md) — Fix — sentence reassignment scoped by (chapterId, id).
- [13 — TTS engine picker](archive/13-tts-engine-picker.md) — TTS engine picker.
- [14 — Coqui XTTS sidecar](archive/14-tts-sidecar-coqui.md) — Local sidecar TTS alternate (zero-shot voice cloning); bounded-retry with provider-side classification of transient (network blip / 5xx / 408) vs non-transient (4xx / CUDA-poisoned 503) failures; full failure-path table. Shipped 2026-05-18.
- [14a — Kokoro v1 TTS engine](archive/14a-tts-sidecar-kokoro.md) — Kokoro v1 TTS engine.
- [15 — Gemini cloud TTS](archive/15-tts-gemini-cloud.md) — Gemini cloud TTS.
- [16 — Generation stream](archive/16-generation-stream.md) — Generation stream.
- [17 — Regenerate (this or forward)](archive/17-regenerate-this-or-forward.md) — Regenerate (this or forward).
- [18 — Listen view](archive/18-listen-view.md) — Cover, chapter list, mini-player, metadata editor, listener-app tiles (5 of 7 live via export modal), export queue, cover Replace/Regenerate. Plan 18a slice + 18b correction shipped 2026-05-18; remaining gaps tracked as BACKLOG Could #31/#32/#33/#34/#35.
- [19 — Listener preview (POV)](archive/19-preview-listener.md) — Listener preview (POV).
- [20 — Revisions & drift](archive/20-revisions-and-drift.md) — Pending drafts + drift events + a/b audio audition (rollback-preserved previous audio) + stale-audio banner on voice edits. Close-out adds startup fsck for half-preserved rollback pairs + mid-flight Reject toast. Shipped 2026-05-18.
- [21 — Book library](archive/21-book-library.md) — Book library.
- [22 — Voice library](archive/22-voice-library.md) — Voice library.
- [22a — Voice library compare](archive/22a-voice-library-compare.md) — Two-cast-member compare entry point on the Voices tab; reuses `CompareCastModal`; same-/different-base-voice badge on the selection pill. Shipped 2026-05-17.
- [23 — Mock toggle (VITE_USE_MOCKS)](archive/23-mock-toggle.md) — Mock toggle (VITE_USE_MOCKS).
- [24 — OpenAPI source of truth](archive/24-openapi-source-of-truth.md) — OpenAPI source of truth.
- [25 — Design tokens (CSS variables)](archive/25-design-tokens.md) — Design tokens (CSS variables).
- [26 — RTK Immer drafts](archive/26-rtk-immer.md) — RTK Immer drafts.
- [27 — Book state persistence](archive/27-book-state-persistence.md) — Book state persistence.
- [28 — Audio output format (MP3 VBR V2)](archive/28-chapter-audio-format.md) — Audio output format (MP3 VBR V2).
- [29 — Local Ollama analyzer + Gemini fallback (ANALYZER=local)](archive/29-analyzer-ollama-local.md) — Local Ollama analyzer + Gemini fallback (ANALYZER=local).
- [30 — Global model-control affordance](archive/30-global-model-control.md) — Global model-control affordance.
- [31 — Sticky generation across navigation](archive/31-sticky-generation.md) — Sticky generation across navigation.
- [32 — Audiobook export — sideload to PocketBook Reader (MP3.ZIP + M4B)](archive/32-audiobook-export.md) — Audiobook export — sideload to PocketBook Reader (MP3.ZIP + M4B).
- [32 — Sticky analysis across navigation](archive/32-sticky-analysis.md) — Sticky analysis across navigation.
- [33 — Voice export](archive/33-voice-export.md) — Live Voice (Android audiobook player) tile on the Listen tab; M4B-standards conformance (`stik = 2` + `desc` / `ldes`) regression-guarded; defaults to M4B + sync-folder. Long-form description field shipped alongside. Shipped 2026-05-18.
- [34 — MP3-folder export (folder-scanning audiobook apps)](archive/34-mp3-folder-export.md) — MP3-folder export (folder-scanning audiobook apps).
- [35 — Per-chapter engine drift detection](archive/35-engine-drift-detection.md) — Per-chapter engine drift detection.
- [36 — Book cover imagery (OpenLibrary)](archive/36-book-covers.md) — Book cover imagery (OpenLibrary).
- [37 — Playwright e2e harness](archive/37-e2e-playwright.md) — Browser-level smoke + on-ramp for visual regression; 14 specs / 30 tests at ship, covering library, upload, analysing, confirm, ready, listen, voices, cast/drawer, theme, toast, manual continuity, revision diff, bulk sync, cover framing, plus per-surface visual baselines (light + dark). Sibling `.github/workflows/regen-visual-baselines.yml` (added 2026-05-22 for BACKLOG Should #1) regenerates the Linux baseline tree on ubuntu-latest via `workflow_dispatch` and opens an auto-PR with the resulting PNGs — removes the WSL/Docker dependency on the dev side. Shipped 2026-05-18.
- [38 — Branching model & commit convention](archive/38-branching-and-commit-convention.md) — Branching model & commit convention.
- [39 — Purge WAV (MP3 is the only format)](archive/39-purge-wav.md) — Dropped the legacy-WAV fallback once documented in plan 28; locator + routes + types now MP3-only. Shipped 2026-05-17.
- [40 — Cover framing + local-disk upload](archive/40-cover-framing-and-upload.md) — Three-tab CoverPicker (Search / Upload / Frame), PNG → JPEG transcode server-side, render-time pan + zoom via `object-position` + `transform`, account-level default for the initial tab. Shipped 2026-05-17.
- [41 — Bulk-apply library sync on confirm-cast](archive/41-bulk-library-sync.md) — Top-of-view pill that flips Reuse for every eligible row and auto-ticks the "Sync profile" checkbox for low-confidence (< 0.9) matches only; high-confidence rows keep the sync as a per-card opt-in. Existing `handleConfirm` batch fans out the per-character library-cast-override POSTs unchanged. Shipped 2026-05-18; Bug C amend 2026-05-19; Bug D (confidence gate) amend 2026-05-22.
- [42 — Dark mode + theme management](archive/42-dark-mode.md) — `[data-theme="dark"]` token override block; top-bar 3-state toggle (Light / Dark / System) as device-local override; Account page sets the first-visit default. Multi-commit ship arc (initial feat 2026-05-17, contrast passes + pre-ship bug bundle through 2026-05-19) — final dark utility-override block covers white / amber / red / rose ladders with their `/N` alpha + hover variants. Plus the `floating-pill-inverse` bespoke utility (cast-view selection bar), match-detail z-index bump, and book-card always-visible metadata strip. Shipped 2026-05-19.
- [43 — Auto-start TTS sidecar](archive/43-auto-start-sidecar.md) — Per-user `autoStartSidecar` preference (default ON); Node owns the sidecar child-process lifecycle (port-9000 probe → spawn → `.run/tts.pid` → `win32` `taskkill /T /F` tree-kill on SIGINT/SIGTERM). `start-app.bat` brings up frontend + server + sidecar in one shot. `PRELOAD_COQUI` env propagation gated on `defaultTtsModelKey === 'coqui-xtts-v2'` (Kokoro's eager-load is unconditional inside the sidecar). 6-case Vitest covers the spawn/probe/tree-kill contract. Shipped 2026-05-17.
- [44 — Pull request hygiene](archive/44-pr-hygiene.md) — Pull request hygiene.
- [45 — Vitest pool tuning + one-retry policy + hot-file hoist](archive/45-vitest-pool-tuning.md) — Vitest pool tuning + one-retry policy + hot-file hoist.
- [46 — Lint, format, a11y baseline](archive/46-lint-format-a11y.md) — ESLint 8 + Prettier 3 + axe-core on the four core views; `npm run lint` (max-warnings 0) prepended to `verify`. Shipped 2026-05-18.
- [47 — Listening progress / resume bookmarks](archive/47-listen-progress.md) — Per-book sibling `listen-progress.json`; mini-player seeks to the saved position on chapter mount via debounced PUT + onLoadedMetadata; Listen view "Resume at MM:SS" pill. Shipped 2026-05-18.
- [48 — Global toast surface](archive/48-toast-surface.md) — `notifications` slice + `<ToastStack/>`; stream-middleware halts + export 5xx dispatch through it; dedupe-by-key collapses repeats; auto-dismiss 6 s. Shipped 2026-05-18.
- [49 — Release packages on git-tag push](archive/49-release-package.md) — `scripts/bump-version.mjs` + `.github/workflows/release.yml` produce `audiobook-generator-vX.Y.Z.zip` + `.sha256` on GitHub Releases whenever a `v*.*.*` tag is pushed. Cross-OS verify matrix (Ubuntu / macOS / Windows), Node-ESM production launch path (`npm run start:prod` → `express.static(dist)` on :8080), in-app Gemini API key field. v1.2.0 and v1.2.1 retracted as dry-runs; v1.2.2 the first live ship. Shipped 2026-05-18.
- [50 — Verify-cache for cheap retries after flake](archive/50-verify-cache.md) — Per-step input-hash cache on `npm run verify`; skips green steps when inputs (filtered from `git ls-files` + lockfiles + Pester/pytest tool fingerprints) match the last green hash. Cold ~120s → warm ~1s; flake recovery drops from the full pipeline to one re-run. Shipped 2026-05-18.
- [51 — Chapter restructure panel](archive/51-restructure-chapters.md) — Three-way structural edit surface (merge / split / drag-reorder) at ready-stage `view: 'restructure'`. Pure-remap semantics preserve sentence text + characterId + voice assignment; content-changed chapters' audio is deleted, renumbered-only chapters' audio is renamed in place (two-pass via temp + segments.json metadata rewrite). Shipped 2026-05-18.
- [52 — MOBI / AZW3 parsing](archive/52-mobi-parsing.md) — `.mobi` + `.azw3` upload via `@lingo-reader/mobi-parser`; KF8 routes through `initKf8File`, legacy MOBI through `initMobiFile`; DRM-protected files rejected up-front with HTTP 415 by reading the PalmDOC encryption byte; original `.azw3` extension preserved on persist; `stripHtml` / `extractFirstHeading` / `GENERIC_NCX_RE` hoisted from `epub.ts` into shared `html-utils.ts`. Shipped 2026-05-18.
- [53 — Mini-player feature pack](archive/53-mini-player-feature-pack.md) — Playback speed picker (0.75×–2×) persisted per book; user-placed markers (note / rerecord) with a listen-view sidebar; sleep timer with countdown presets + end-of-chapter mode. Shipped 2026-05-19.
- [54 — verify-cache `--steps` filter](archive/54-verify-cache-fast.md) — `--steps=<csv>` flag selects a subset of the verify pipeline; `verify:fast` (pre-commit) now caches via the plan-50 runner, dropping warm pre-commits to under 1 s on no-source-change commits. Shipped 2026-05-19.
- [55 — Revision history timeline](archive/55-revision-history-timeline.md) — Per-chapter chronological log of accept/reject events surfaced via a modal off the existing A/B player; read-side consumer for plan 20's `acceptedSelections` map. Read-only in v1.3.0; multi-step rollback (snapshot-per-entry) parked for v1.4.0. Shipped 2026-05-19.
- [56 — Real chapter-audio peaks at encode time](archive/56-real-chapter-peaks.md) — 240-bin RMS envelope written alongside the MP3 encode, persisted as `<bookDir>/audio/<slug>.peaks.json`, surfaced from the chapter-audio meta endpoint. Missing-file fallback returns `peaks: []` so legacy chapters keep working. Shipped 2026-05-19.
- [57 — Listen-view download tiles](archive/57-download-tiles.md) — Wires the M4B chaptered + new MP3 ZIP tiles on the listen view to open `ExportAudiobookModal` with the format pre-filled; streaming-link tile remains "Coming soon" pending a slugged URL endpoint. Shipped 2026-05-19.
- [58 — E2E coverage refresh](archive/58-e2e-coverage-refresh.md) — Un-quarantines `listen-playback` + `new-book-flow`; adds `binary-upload.spec.ts` covering EPUB / PDF / MOBI / AZW3 routing; applies file-level serial mode to five contention-flaky spec files. From 3-5 hard failures per `verify` run to 0. Shipped 2026-05-19.
- [59 — Parallel Claude Code sessions](archive/59-parallel-claude-sessions.md) — `scripts/wt-new.mjs` spawns a git worktree on a new branch with non-colliding dev-server ports so multiple top-level `claude` sessions can run in parallel without fighting over `:5173` / `:8080` / `:9000` / `:5174`. Slot N → ports offset by `N*10`. `scripts/wt-list.mjs` lists active worktrees + their assignments. `vite.config.ts` + `playwright.config.ts` now env-driven; stock defaults preserved for the main worktree. Shipped 2026-05-19.
- [60 — Listen view decomposition](archive/60-listen-view-decompose.md) — Behaviour-neutral lift of `src/views/listen.tsx` (1136 → 319 lines) into a slice-wiring orchestrator that composes three region sub-components under `src/components/listen/` (header, player+markers, downloads+queue). Pure-deletion diff (52 ins / 869 del); zero spec modifications. Gates Wave 3 of the v1.4.0 slate. Shipped 2026-05-19.
- [61 — In-app multi-model management UX](archive/61-in-app-model-management.md) — Account → Models card: detect-Ollama + install (platform-aware), pull-model (NDJSON-streamed progress), refresh-available-models, plus `install-coqui.{sh,ps1}` parallels to `install-kokoro.*`. Two state machines (install: idle → detecting → downloading → installing → installed; pull: idle → pulling → pulled). Dependency-injectable bootstraps so tests run offline. Closes plan 49's deployer-first gap for non-Kokoro engines. Shipped 2026-05-19.
- [62 — CI verify-on-PR](archive/62-ci-verify-on-pr.md) — GitHub Actions workflow runs `npm run verify` on every PR targeting `main`; ffmpeg + Node + Playwright chromium provisioned, npm + browser caches keyed on lockfile hash; concurrency `cancel-in-progress` supersedes redundant runs on rapid pushes; pairs with the visual baselines (`e2e/responsive/visual.spec.ts` since 2026-05-22) so the same gate the pre-push hook runs locally is the gate every PR must clear before merge. Sibling `.github/workflows/e2e-mobile.yml` (added 2026-05-22 for BACKLOG Should #3) runs `npm run test:e2e:mobile` non-blocking on every PR so mobile/tablet layout regressions show up as a visible status check. Shipped 2026-05-19.
- [63 — Cross-tab `BroadcastChannel` state sync](archive/63-cross-tab-broadcast-sync.md) — `broadcastMiddleware` sends post-mutation snapshots of analysis + chapters `activeStream` on `BroadcastChannel('audiobook-state')` so a sibling tab on the same workspace updates its top-bar pill without a `/api/library/active-analyses` round-trip. Two-layer echo suppression (per-tab `instanceId` tag + inbound-action allowlist); narrow scope (only `activeStream` slots — never per-chapter rows / cast / manuscript) preserves the single-user-per-workspace contract and keeps backlog `fe-11` parked. Shipped 2026-05-19.
- [64 — Voice preview while editing the character](archive/64-voice-preview-while-editing.md) — Per-candidate "Play sample" affordance in the profile-drawer override picker; user-editable sample line persisted to `localStorage` (key `voice-preview-sample-text`); auditions are read-only (no cast commit until Save). Reuses existing `playBaseVoiceSampleWithAutoLoad` orchestrator + `useTtsLifecycle()` (no new slice). Shipped 2026-05-19.
- [65 — Voices global-tab same-book compare](archive/65-voices-global-tab-compare.md) — Lifts plan 22a's "Open a book to compare" gate for global-tab pairs that share a `bookId`: `api.getBookState(bookId)` resolves the foreign cast on demand, cached for the modal session via a component-local `Map<bookId, Character[]>` (no new redux slice). Fetch-fails surface a deduped error toast and retroactively disable the Compare button for that bookId. Cross-book pairs remain disabled (BACKLOG #17). Shipped 2026-05-19.
- [66 — Real-binary MOBI / AZW3 parser fixtures](archive/66-real-binary-parser-fixtures.md) — `scripts/gen-parser-fixtures.mjs` probes for Calibre's `ebook-convert` and derives gitignored `sample.mobi` + `sample.azw3` from the existing EPUB fixture; new server Vitest suite + the binary-upload e2e MOBI/AZW3 cases run the real `@lingo-reader/mobi-parser` path against those binaries, skipping cleanly via `describe.skipIf` + `test.skip` when Calibre isn't installed. Shipped 2026-05-19.
- [67 — Per-book editorial notes](archive/67-editorial-notes.md) — `notes?: string | null` on `BookStateJson` round-trips via the existing `slice='state'` PUT; collapsible Notes card mounted in `ListenHeader` (plan-60 sub-component, not the orchestrator); plain textarea in the metadata editor; line breaks via `whitespace-pre-wrap` (no markdown parser dep). Persistence middleware's `bookMeta/commitDraft` rule simultaneously gained `description` (previously missing). Shipped 2026-05-19.
- [68 — Streaming-link download tile](archive/68-streaming-link-tile.md) — `POST /api/books/:bookId/share` mints a 12-char Crockford-style base32 slug; `GET /share/:slug` resolves the slug → bookId via `<workspace>/.audiobook/share-links.json` and proxies the book's most-recent successful M4B export off disk. Idempotent mint (re-POST returns the same slug). Closes BACKLOG #33 and the last "Coming soon" tile on the Listen view's "Or download a file" section. Shipped 2026-05-19.
- [69 — Share 30-second chapter clip](archive/69-share-chapter-clip.md) — New `GET /api/books/{bookId}/chapters/{chapterId}/clip` route slices the source MP3 via `ffmpeg -ss <start> -i <mp3> -t <duration> -c copy` (no re-encode, fast-seek path). Per-chapter Share-clip button in the listen-view player region opens a modal with default ±15 s window around the current playhead, range / step / typed-input handles, server-enforced 60 s cap. Shipped 2026-05-19.
- [70a — Chapter restructure bug fix](archive/70a-restructure-bugfix.md) — Orphan-sentence recovery (preserves original `oldChapterId` in the response remap so the frontend reducer lookup still resolves), auto-prune of empty chapter rows, and auto-renumber of generic "Chapter N" titles against new positions on every merge / reorder. Resolves the stale-numbers + zero-sentence symptoms visible on long manuscripts. Server-only; opens a `warnings: string[]` channel on the restructure response. Shipped 2026-05-19.
- [70b — Chapter restructure feature extensions](archive/70b-restructure-extensions.md) — Five-part feature bundle: shared `<RestructureChaptersButton>` mounted in both Listen and Manuscript view headers (pre-generation entry); sticky Merge-selected toolbar (`top-16 z-30` to clear the global TopBar); per-row Exclude / Include toggle reusing `Chapter.excluded` soft-hide semantics (audio preserved, merge checkbox disabled on excluded rows); Refresh chapter names button (re-parse manuscript + first-line promotion with dialogue / length / title-case heuristics, user-custom titles preserved); frontend toast consumer surfaces plan 70a's `warnings` advisories inline. Shipped 2026-05-19.
- [71 — Audio loudness normalization (EBU R128)](archive/71-audio-loudness-normalization.md) — Two-pass `loudnorm` via ffmpeg in the chapter encoder; default ON via `AUDIO_LOUDNORM_ENABLED`; per-chapter `<slug>.lufs.json` sidecar carries the post-normalisation integrated loudness / LRA / true-peak (2026-05-22 post-ship correction: was previously persisting the pre-filter input loudness; second-pass filter now emits `print_format=json` and `mp3.ts` parses `output_i` out of the encoder's stderr). Backfill via `scripts/relufs-existing.mjs` for chapters rendered before the correction. Shipped 2026-05-20; corrected 2026-05-22.
- [72 — AAC/M4A and Opus chapter audio output](archive/72-audio-codec-aac-opus.md) — AAC/M4A and Opus chapter audio output.
- [73 — Library search + per-book tag filter](archive/73-library-search-tags.md) — Library search + per-book tag filter.
- [74 — Manuscript diff viewer on re-upload](archive/74-manuscript-diff-on-reupload.md) — Manuscript diff viewer on re-upload.
- [75 — Portable book bundle (export + import)](archive/75-portable-book-export.md) — Portable book bundle (export + import).
- [76 — Library card↔table view](archive/76-library-table-view.md) — Toggle pill in the library chrome flips between the existing card grid and a dense, series-grouped table view; standalones collected into a synthetic pseudo-section; `library.viewMode` persisted in localStorage. Behaviour parity via reuse of the grid's callbacks; shared `STATUS_UI` + empty-state modules extracted for both. Shipped 2026-05-20.
- [77 — Per-chapter loudness report card (EBU R128 drift surfacing)](archive/77-loudness-report-card.md) — Per-chapter loudness report card (EBU R128 drift surfacing).
- [78 — Chapter rename (manual title override)](archive/78-chapter-rename.md) — Chapter rename (manual title override).
- [79 — Exports in the book folder + Voice sync fixes](archive/79-exports-in-book-folder-and-voice-fixes.md) — Finished artifacts move out of the hidden `.audiobook/exports/<id>/` jail into a visible `<bookDir>/exports/<slug>.<ext>` sibling to `audio/` (clobber-newest-wins; manifests stay under `.audiobook/export-manifests/`). Export modal auto-saves the sync folder on blur, surfaces save errors as a red banner, and gains a "Test" probe button backed by `POST /api/user/settings/sync-folder/test`. Widens `renameWithRetry` to cover EACCES / EIO for Drive for Desktop's virtual-FS hiccups; prepends Drive/OneDrive-specific hints on terminal failures. Portable bundle now also stages a local copy at `<bookDir>/exports/<slug>.portable.zip`. Shipped 2026-05-20.
- [80 — Regenerate applies manuscript-edits overlay before synth](archive/80-regenerate-applies-manuscript-edits.md) — Regenerate applies manuscript-edits overlay before synth.
- [81 — Mobile + tablet support (LAN over HTTPS)](archive/81-mobile-tablet-support.md) — Six-wave round: mkcert local-CA HTTPS for LAN access (`npm run dev:lan` / `start:lan` / `install:cert-mobile`); Playwright mobile + tablet projects (chromium-engine override on Pixel 7 + iPad Pro 11 viewports) gated to `e2e/responsive/` specs; responsive shell (top-bar overflow + sticky mini-player + 44px touch targets); per-view responsive (6 parallel agents on isolated worktrees: books / confirm-cast / manuscript / listen / generation+upload / cast); touch affordances additive to drag-and-drop (tap-to-assign voice card pill + sticky banner; PointerEvents instead of MouseEvents on manuscript paragraph boundaries); `coarse-pointer:` Tailwind variant for hover-only affordances; e2e script split (`test:e2e` chromium-only for pre-push, `test:e2e:mobile` opt-in). Shipped 2026-05-21.
- [82 — Export queue Retry + Download row actions](archive/82-export-queue-retry-download.md) — Export queue Retry + Download row actions.
- [83 — Background drift polling across non-active books](archive/83-drift-poll-multibook.md) — Background drift polling across non-active books.
- [84 — Manuscript-diff rename guard for re-upload](archive/84-manuscript-diff-rename-guard.md) — Manuscript-diff rename guard for re-upload.
- [85 — wt-merge.mjs reconciliation helper](archive/85-wt-merge-helper.md) — wt-merge.mjs reconciliation helper.
- [86 — Live worktree dashboard](archive/86-worktree-dashboard.md) — Live worktree dashboard.
- [87 — Parallel chapter synthesis](archive/87-parallel-chapter-synth.md) — Bounded worker pool over chapters (width env renamed `GEN_CHAPTER_CONCURRENCY` → `GEN_WORKERS` in plan 111, now driven by the `generationWorkers` setting). `processOneChapter` extracted from `runMainGenerationJob`; cascade-fatal aborts the shared signal so in-flight siblings exit cleanly; per-chapter SSE tracks route through `src/store/chapters-slice.ts` so interleaved events for chapter B never touch chapter A's row. Shipped 2026-05-21.
- [88 — Pipelined two-model analyzer (Gemma cast + Gemini attribution, 10-chapter lag)](archive/88-analyzer-per-phase-model.md) — Phase 0 (Gemma) and Phase 1 (Gemini) wrapped in sibling `Promise.all` worker pools; `awaitPhase1Dispatch` enforces a minimum 10-chapter lag via the new `phase-watermark.ts` back-pressure semaphore. Independent rate-limit buckets (gemma 1,500 RPD + gemini 500 RPD). Rolling roster snapshot per dispatch, final consolidation when Gemma's done. `ANALYZER_PHASE{0,1}_MODEL` + `ANALYZER_PHASE1_MIN_LAG_CHAPTERS` env knobs; non-pipelined runs stay sequential. Shipped 2026-05-21.
- [89 — Frontend perf pass](archive/89-frontend-perf-pass.md) — Broadcast-middleware shallow diffing + phaseProgress debounce (C2); `useAppSelectorShallow` applied at five large-slice sites including `src/views/listen.tsx:122` (C3); `React.lazy` route splitting with a delayed Suspense fallback in the layout shell + per-area icon split (C5). Main bundle 410 kB → 345 kB (gzip 108 kB → 91 kB). Preserves plan 63 narrow-scope BroadcastChannel rule. Shipped 2026-05-21.
- [90 — Low-confidence triage polish: fast-nav + series-roster pickers + typeahead search](archive/90-low-confidence-triage-polish.md) — Three behaviours shipped together as one workflow round (BACKLOG Could #32 + #33 + #34). New `<CharacterSearchPicker>` extracted under `src/components/`, consumed by all three manuscript-view picker sites. New `POST /api/books/:bookId/cast/add-from-roster` route lets the user materialise a new local character from a prior series-mate's cast in one click — closes the "analyzer missed a recurring series character entirely" gap that the older `cast/link-prior` couldn't fix. Header's "X low-confidence" stat becomes an active ▲/▼ navigator with J/K shortcuts that scroll the next misattribution into view and auto-open the inspector. `priorRoster` now lives on `LayoutContext` so the ProfileDrawer and the manuscript view share one `/series-roster` fetch per book. Post-ship polish (2026-05-22, PR #148): picker now portals to `document.body` so the inspector's `overflow-y-auto` no longer clips the roster group; row-level dismissal switched from `onMouseLeave` to click-outside + Esc; dark-mode `.picker-surface` override lifts the popover one elevation step above the bg-white redirect. Shipped 2026-05-22.
- [91 — Cast Drift modal: consolidation + book-title + per-character scope](archive/91-cast-drift-consolidation.md) — Three-PR ship. Consolidation by `(book × character × snapshot)` (PR #119): one card per group instead of one per event; ~300-event modal collapses to ~6–18 cards via `createSelector`-memoised `selectDriftGroupsByBook` + `React.memo`-wrapped rows; DOM-node count 7,200 → 200; detailed `ProfileCompareCard` preserved at top of every card. Book-title fallback (PR #141): per-section "BOOK" header resolves through `bookMeta.saved → library.books → bookId` so cross-book cards stop leaking workspace slugs. Per-character scope (PR #145): clicking the amber drift pill on a cast row scopes the modal to that one character via new `ui.driftReportCharacterFilter` field + `openDriftReportForCharacter` / `clearDriftReportCharacterFilter` actions; in-modal "Showing X · Show all characters" banner is the escape hatch; top banner stays unscoped. Shipped 2026-05-22.
- [94 — Series-prior roster dedup](archive/94-series-prior-dedup.md) — `dedupSeriesPrior` (union-find over normalised name + alias tokens; transitive alias-chain merge) collapses per-book cast.json rows into one entry per unique character before they reach the analysing-view pill and the Phase 0a per-chapter prompt. The "Carried in from prior books in this series · N characters" count now reports unique characters (KOTLC #4 dropped from 136 raw rows). Producer (`scanSeriesCharacters`) + Profile Drawer's manual continuity-link picker route stay raw — they legitimately need per-book provenance. `SeriesPriorCharacter.fromBookTitle` → `fromBookTitles: string[]`; SSE payload shape unchanged. Shipped 2026-05-22.
- [95 — Editable cast aliases](archive/95-alias-edit.md) — Per-chip `X` on the Profile Drawer's "Also known as" row splits a misplaced alias back into its own standalone cast member; sibling `+ Add alias` button appends a typed name. Reattribute Lines modal (new) opens on `X`-click and lists candidate sentences in the chapters where the alias originally appeared (derived from the preserved Phase-0a `chapterCast`), reusing `manuscriptActions.setSentenceCharacter` for per-sentence picks. Two new contract-internal POST routes (`cast/unlink-alias`, `cast/add-alias`); delta-shaped responses driving new `applyUnlinkAlias` / `applyAddAlias` cast-slice reducers. Shipped 2026-05-22.
- [95 — Analysing-stage multi-model UI + sticky status bar](archive/95-analysing-multi-model-ui.md) — Analysing-stage multi-model UI + sticky status bar.
- [96 — Cross-book voice Compare with series-propagating saves](archive/96-cross-book-voice-compare.md) — Cross-book voice Compare with series-propagating saves.
- [96 — Recover missing character — manual cast script](archive/96-recover-missing-character.md) — Recover missing character — manual cast script.
- [97 — Narrator-only named characters survive Phase 0a + minor-cast fold](archive/97-narrator-only-named-characters.md) — Narrator-only named characters survive Phase 0a + minor-cast fold.
- [98 — Sticky low-conf nav + per-chapter low-conf chapter-list badge](archive/98-low-confidence-sticky-and-chapter-badge.md) — Manuscript-view header stats row (`N segments · M speakers · L low-confidence ▲▼ · Prev/Next`) lifted into a `sticky top-16 z-30` bar so the ▲/▼ navigator + chapter Prev/Next stay reachable from any scroll position on large chapters; sidebar chapter rows gain an amber count badge when the chapter contains low-confidence sentences so users can scan-and-pick which chapters need triage without opening them. New `src/components/manuscript/sticky-stats-bar.tsx` mirrors the `sticky-analysis-bar.tsx` CSS-only sticky pattern. Shipped 2026-05-22 via PR #159.
- [99 — Merge cast duplicates from the Voices selection pill](archive/99-voices-pill-merge.md) — Top-level Voices selection pill grows a `Merge into <longer-name>` button when 2 same-base-voice, same-book, non-narrator cast cards are selected. Reuses `api.mergeCharacters` + `castActions.applyMerge`; survivor picked by substring-containment → longer-name → stable order. Bundles a dark-mode contrast fix on `.floating-pill-inverse` descendants (Compare + Merge + Clear were painting dark-on-dark). Shipped 2026-05-22 via PR #167 (originally drafted as plan 98; renumbered on archive because the existing plan-98 already occupied that slot — in-code `styles.css` / `voices.test.tsx` references still say "plan 98" for historical accuracy at ship-time).
- [100 — GPU-arbitration semaphore for parallel sessions](archive/100-gpu-arbitration-semaphore.md) — GPU-arbitration semaphore for parallel sessions.
- [101 — Cross-book duplicate cleanup — review surface for escaped continuity-links](archive/101-cross-book-duplicate-review.md) — Cross-book duplicate cleanup — review surface for escaped continuity-links.
- [101 — Doc-only PR fast-path (skip verify + e2e-mobile)](archive/101-docs-only-ci-skip.md) — `paths-ignore: [docs/**, *.md, .github/*.md]` added to both `verify.yml` and `e2e-mobile.yml` so PRs whose entire changed-file set lives in those globs skip the 10–15 min full battery. `pr-title-lint.yml` and GitHub's native `mergeable` status still gate every PR; the moment a non-doc file is touched the full verify runs as before. CONTRIBUTING.md + CLAUDE.md updated with one-paragraph pointers. Shipped 2026-05-22 via PR #173 (originally drafted as plan 100; renumbered on archive because PR #171's `100-gpu-arbitration-semaphore.md` had merged via ship-notes PR #174 while PR #173's CI was running).
- [102 — Global queue modal for chapter generation](archive/102-global-queue-modal.md) — Cross-book, reorderable, regen-safe FIFO queue persisted to `<workspace>/.queue.json`. Replaces the implicit per-book `state ∈ {queued, in_progress}` queue and the hard-interrupt regen path. Modal mounted globally in Layout hosts the relocated Resume/Pause control + per-row reorder/cancel actions. All chapter-level + character-level regen sites (10 entry points) funnel through `queueActions.enqueue` → `queue-dispatcher-middleware` drains serially. SSE survival + idempotent server-side `resume_from` ack absorbs former BACKLOG Should #1. Eight-PR ship (#181-#185, #187, #188) across seven waves; cross-book dispatcher + chapters-slice cleanup remain on BACKLOG as a follow-up. Shipped 2026-05-23.
- [103 — CI cost reduction (path-filtered verify + cross-OS consolidation)](archive/103-ci-cost-reduction.md) — `verify.yml` collapsed to one `verify` job (name preserved) that detects changed scopes via a pure-bash `git diff` against the PR base (no third-party action) and gates each leg behind an `if:` — frontend-only PRs skip server tests, server-only PRs skip Playwright e2e + the frontend unit suite, a root lockfile change runs everything; the local pre-push hook still runs the full battery so nothing is permanently un-covered. Adds a `node_modules` cache, skips drafts (`+ ready_for_review` trigger). `release.yml` dropped to Ubuntu-only verify; new `cross-os.yml` carries macOS+Windows verify + the former per-PR `e2e-mobile.yml` on `workflow_dispatch` + a weekly Sunday cron; `pr-title-lint.yml` dropped its `edited` re-fire. Cuts per-PR Linux burn ~30%. Shipped 2026-05-23 via PR #186 (`da5c695`) — merged with red checks because the account had hit its monthly Actions quota, so the canary verification lands on the first PR after top-up.
- [104 — ESLint 9 flat-config migration + jsdom/archiver bumps](archive/104-eslint-flat-config.md) — `.eslintrc.cjs` → `eslint.config.js` (flat) on ESLint 9; `typescript-eslint@8`, react-hooks v7, `eslint-config-prettier@10`. Same plan-46 lint ratchet preserved verbatim (`--max-warnings 0`). Bundles jsdom 25 → 29 + archiver 7 → 8 so a fresh `npm install` prints ZERO deprecation warnings. Shipped 2026-05-23.
- [105 — Multer 1.x → 2.x security upgrade](archive/105-multer-2-upgrade.md) — Bumps `multer` `^1.4.5-lts.1` → `^2.1.1` across the four multipart upload routes; API-compatible (`memoryStorage` / `upload.single` / `req.file` / `MulterError` codes preserved). Closes the `multer@1.4.5-lts.x` deprecation warning + the EOL-1.x CVE string; `cover.ts` hardened with an explicit `LIMIT_UNEXPECTED_FILE` → 400. Shipped 2026-05-23.
- [106 — CI: composite setup action + cross-os mobile-e2e path filter](archive/106-ci-setup-action-mobile-filter.md) — New local `.github/actions/setup` composite (setup-node + `node_modules` cache + conditional `npm ci`) DRYs the preamble byte-duplicated across `verify.yml` and `cross-os.yml`; a Node-major bump is now a one-file edit. Plus a `changes` probe that path-filters `cross-os.yml`'s weekly `mobile-e2e` on whether `main` saw a frontend/e2e change in the prior 7 days. Shipped 2026-05-23.
- [109 — MP3 Xing/Info VBR header (correct chapter duration in players)](archive/109-mp3-xing-vbr-header.md) — Chapter MP3s were piped to ffmpeg's non-seekable `pipe:1`, so libmp3lame could not stamp the Xing VBR header; players/ffprobe then estimated duration ~7× too long (a 10:34 chapter showed as ~76:18 in the mini-player, with broken seeking). Affected every MP3 the tool had ever produced. Fix: the MP3 path encodes to a seekable temp file (+ explicit `-write_xing 1`) and reads it back, preserving the `encodePcmToAudio => Promise<Buffer>` contract and the plan-71 output `-ar` pin; `mini-player.tsx` stops clobbering the authoritative server `durationSec` with the browser estimate; `scripts/rexing-existing.mjs` losslessly back-fills the existing library (`ffmpeg -c:a copy -write_xing 1`, idempotent, no re-encode). Distinct from plan 71's loudnorm sample-rate stretch. Shipped 2026-05-24 via `fix/server-mp3-xing-header-duration`.
- [111 — Persisted queue as single source of truth + N generation workers](archive/111-queue-worker-pool.md) — Removes the `hasWork` "generating override" so the persisted `.queue.json` drives all generation; parallelism is N configurable queue workers (`generationWorkers` setting, default 2, coalesce-per-book — the dispatcher claims N entries across books and groups same-book claims into one stream via the plan-87 pool). `activeStream` → per-book map; runner → `Map<bookId, handle>`; `GEN_CHAPTER_CONCURRENCY` retired (→ `GEN_WORKERS`). In-memory mock-queue makes generation run in mock mode. Shipped 2026-05-25 across PRs #223–#225 + wave 4 (plan 110 / #219 shipped the read-side overlay + GPU-badge rename).
- [112 — Qwen3-TTS synthesis quick wins](archive/112-qwen-synth-quick-wins.md) — Quality-preserving Qwen speedups on top of the inherent autoregressive-vs-feed-forward gap: SDPA attention by default (`QWEN_ATTN_IMPL`, ships `side-1`, kwarg-rejection fallback), an in-memory clone-prompt cache (evict-on-redesign / clear-on-unload), per-call RTF logs on Qwen + Kokoro, and a stdlib `bench-tts.py` with a `--concurrency` sweep. Voices bit-identical. Real throughput levers deferred to `side-3` (batching, in flight) / `side-4` (x-vector-only). Shipped 2026-05-26 via PR #247.
- [114 — Profile-change regen + A/B preview](archive/114-profile-regen-preview.md) — Changing a character's voice regenerates every chapter they speak in; an opt-in one-chapter A/B preview gates the rest (Approve fans out, Reject reverts). Repurposes the plan-20 revisions A/B as the preview gate; removes the per-character queue scope + `regenerateCharacter`/`batchRegenerateCharacters` reducers + the cast-view batch modal; drift-report regen now applies immediately. Shipped 2026-05-26.
- [115 — Opt-in FlashAttention-2 install (Windows)](archive/115-flash-attn-opt-in.md) — `install-qwen3.mjs --flash-attn` / `QWEN_INSTALL_FLASH_ATTN=1` pip-installs a pinned community prebuilt wheel (`flash_attn 2.7.4` / `cu124` / `torch2.6.0` / `cp311` / `win_amd64`); platform/version-gated + non-fatal via the pure `resolveFlashAttnInstall()`. SDPA stays the default — benchmarked 2026-05-26: FA2 ≈ SDPA (modest, noisy edge; TTS is decode-bound), so `side-5` resolved "SDPA stays". Full data in [tts-performance.md](../tts-performance.md). Shipped 2026-05-26 (PR #260).
- [116 — EPUB parsing](archive/116-epub-parsing.md) — `epub2` primary path + a `yauzl` raw-zip fallback that tolerates namespace-prefixed OPFs (publisher EPUBs the `epub2` lib can't walk); classified `UnusableEpubError` (HTTP 415) for DRM / image-only / no-spine instead of a generic 500. Shipped 2026-05-26.
- [117 — Qwen voice presentation](archive/117-qwen-voice-presentation.md) — Status-first Qwen sections in the cross-book Voices view (`Qwen · Needs a voice` / `Qwen · Designed voices` + per-card Designed/Generated badge) replacing the degenerate 1-member voice families; new optional `Voice.generated` flag (per-voiceId, scanned from rendered segments via shared `server/src/audio/segments-io.ts`) drives the badge + an engine-aware cast Status pill (Needs voice / Designed / Generated for Qwen rows; preset rows keep `voiceState` pills). Preset voice-family grouping untouched. Shipped 2026-05-27 via PRs #272 + #274.
- [118 — Per-phase analyzer split wiring fix + chip honesty](archive/118-analyzer-per-phase-wiring-fix.md) — Makes the opt-in two-model split (plan 88) actually function: the route resolves Phase 0 via `selectAnalyzerForPhase('phase0')` and the frontend only sends a per-request `model` when it should, so the saved per-phase models are reachable. Chips show the truly-effective model (no fabricated "Gemma 4 31B" when single-model); "warms up after ch. N" only shows when a split is engaged. Account card retitled + live OFF/ON status line. Shipped 2026-05-27 (PR #280).
- [119 — Generate-view enqueue gate + Clear-queue control](archive/119-generate-view-enqueue-gate-clear-queue.md) — Fixes "book queues at analysis time": `enqueueOnWork` now gates on `stage.kind === 'ready' && stage.view === 'generate'`, so a freshly-analysed book only enters the queue when the user reaches the Generate view ("Approve cast & start generating") — not at analysis/confirm. Adds a queue-modal **Clear queue** button + confirm dialog (default clears queued+failed; opt-in "Also stop generation in progress" tears down live streams via `requestStreamHalt` + force-clears), backed by a new pure `clearQueue` mutator and `POST /api/queue/clear` route. Cross-links plans 102/110/111. Shipped 2026-05-27 (PR #281).
- [120 — Status pill + hover popover (top-bar consolidation)](archive/120-status-pill-popover.md) — Collapses the top-bar TTS (Kokoro/Coqui/Qwen Load/Stop + GPU-busy badge) / analysis / generation / revisions pill cluster into ONE compact, color-coded Status pill that reveals an anchored **hover/tap popover** (no modal, no backdrop) carrying the full detail, so the cluster stops crowding the nav menu. Pure `summarizeStatus` priority ladder (halted › stalled › generating › analysing › loading › paused › revisions › idle); pill hidden on idle global views; `ModelControlPill`/`AnalysisPill`/`GenerationPill` reused verbatim inside the popover; queue chip / theme / avatar stay inline. The cast drawer tucks under the top bar (`profile-drawer.tsx` `top-16`) and the portaled, propagation-stopping popover means clicking its buttons never dismisses an open cast drawer. Shipped 2026-05-27 (PR #283 modal; hover-popover follow-up + renumber from 119).
- [120 — TTS load-error surfacing + orphaned-sidecar log resilience](archive/120-tts-load-error-surface-and-sidecar-pipe.md) — Fixes "Qwen Load model reverts to idle with no error." Layer A: a shared `TtsNoticeBanner` rendered once globally under the top bar (was Generate-view only), so a Load failure / analyzer-eviction triggered from any stage is visible instead of silently reverting. Layer B: `spawn-sidecar.ts` hands the child inherited log-file descriptors instead of Node pipes, so an orphaned sidecar keeps logging — the broken pipe was raising `[Errno 22] Invalid argument` from `from_pretrained` mid-`/load`. Shipped 2026-05-27 (PR #285).
- [121 — Cast table sort — by line count, unknown buckets last](archive/121-cast-table-sort.md) — Cast table sort — by line count, unknown buckets last.
- [122 — Rebaseline collapses recurring cast by name/alias](archive/122-rebaseline-name-alias-collapse.md) — Durable fix for "two Sophies" in the Rebaseline modal: it collapses recurring characters by normalised name/alias (not just `voiceId ?? id`), respecting the `notLinkedTo` escape hatch. The keystone is a new `voice-override-linked` route that unifies `voiceId` across the name/alias group and propagates the Qwen voice to every member book, so approving a collapsed row can't silently skip a book on a divergent key. Also hardens the two source seams: `cast-link-prior` now shares `voiceId` (not just aliases) and the fold step canonicalises any bucket-id row back to `Unknown male/female` so a named character never wears a fold-bucket id. Shipped 2026-05-27 (PR #295).
- [122 — Shared user-settings location](archive/122-shared-user-settings.md) — User account defaults move from the per-checkout `<SERVER_ROOT>/user-settings.json` to one shared `~/.audiobook-generator/user-settings.json`, so a save honours regardless of which git checkout the app launches from (fixes the recurring "default reverts to Kokoro / eager-load turns back on"). Pure `resolveUserSettingsPath` (`USER_SETTINGS_FILE`-overridable) + a one-time copy-migration of the legacy file; a new `server/src/test-setup.ts` redirects settings to a temp file so suites can't touch real settings. Settings now also survive extract-over reinstalls. Shipped 2026-05-27 (PR #294).
- [123 — Reused-voice drawer parity + Reused/lifecycle status split](archive/123-reused-voice-drawer-parity.md) — Fixes the profile drawer reading "Qwen · No voice designed yet" (+ a blocked Play button + a hardcoded "Default (Kokoro)" label) for a character whose Qwen voice was reused/matched from a prior book — the cast row resolved it correctly (via `resolveDisplayTtsVoice`) but the drawer re-derived from the project engine + the character's empty override. New shared `resolveVoiceStatus` (`src/lib/voice-status.ts`) backs both surfaces and splits provenance (a small `ReusedBadge`, keyed off `matchedFrom`) from the lifecycle pill (Designed/Generated/Tuned/Locked), so a reused Qwen voice reads "Generated · Reused" instead of a lone "Reused"; the Qwen lifecycle now also fires when the matched voice is Qwen even if the character isn't engine-pinned. Also: `VoiceEnginePicker` default-option label is project-engine-driven (not hardcoded Kokoro), and the preset Model-voice picker is gated on the effective engine so it hides for an effective-Qwen character. Frontend-only; extends 117 + 10 + 108. Shipped 2026-05-28.
- [124 — Build version footer](archive/124-build-version-footer.md) — In-flow footer in the app shell showing the running build on every stage. Provenance (version + git SHA/branch/dirty + build time) captured once at vite-start and injected via Vite `define`, read through a `typeof`-guarded `buildInfo` + pure `formatBuildStamp`. Verbose in dev (`v1.4.0 · a1b2c3d* · fix/foo · 14:32`), minimal in prod (`v1.4.0 (a1b2c3d)`). Single labelled `contentinfo` landmark; design-token colour only. Shipped 2026-05-28 (PR #303).
- [125 — Listen-view chapter row mirrors the live playhead](archive/125-listen-row-live-progress.md) — Listen-view chapter row mirrors the live playhead.
- [126 — Analysis-time cross-book reuse linking](archive/126-analysis-time-reuse-linking.md) — Server-side "reused" continuity at analysis (Facet A, `series-reuse-link.ts`) + durable preservation across reparse & re-analysis (Facet B / `srv-13`): reparse carryover snapshot, Facet-A `notLinkedTo` pre-seed, `notLinkedTo`/aliases in the merge, dropped voiced-char rescue, chapter-retry link pass, frontend cast-slice preservation. Facet A 2026-05-30, Facet B 2026-06-05.
- [127 — Cross-OS verify gate on the version-cut process](archive/127-release-cross-os-gate.md) — Cross-OS verify gate on the version-cut process.
- [128 — Qwen batch length-bucketing](archive/128-qwen-batch-length-bucketing.md) — Sorts batchable Qwen groups by length before slicing so each batched forward decodes to a tight max-length (less padding waste); output-preserving, the foundation of the token-budget packer. Shipped 2026-05-30 at the 32/3600 default (closes `side-6`).
- [134 — Engine-aware eager-load toggle + Qwen-aware TTS-sidecar settings copy](archive/134-qwen-eager-load-setting.md) — Engine-aware eager-load toggle + Qwen-aware TTS-sidecar settings copy.
- [135 — Qwen→Kokoro fallback is loud, never silent + stale-sidecar replacement](archive/135-qwen-loud-fallback.md) — Qwen→Kokoro fallback is loud, never silent + stale-sidecar replacement.
- [136 — Qwen token-budget batching](archive/136-qwen-token-budget-batching.md) — Variable batch width bounded by a per-batch token budget (`width×maxLen ≤ budget`) so short/dialogue batches pack wide and long sentences stay narrow; `QWEN_BATCH_SIZE` is the hard cap. Shipped 2026-05-30 as the default — cap 32 / budget 3600 (closes `side-9`).
- [137 — Opening a book never auto-starts generation (explicit-start enqueue)](archive/137-reopen-never-auto-enqueues.md) — Opening a book never auto-starts generation (explicit-start enqueue).
- [138 — Plan 138 — Reused Qwen voice resolution](archive/138-reused-qwen-voice-resolution.md) — Plan 138 — Reused Qwen voice resolution.
- [153 — Variable-shape host leak: candidate-1 (MKLDNN-disable) probe + leak-slope harness](archive/153-sidecar-variable-shape-host-leak.md) — Diagnostic toggle (`SIDECAR_DISABLE_MKLDNN`, default OFF) + `committed_mb` readout + `bench-tts.py --mem-sample` leak-slope instrument. 2026-06-01 live A/B **rejected** candidate 1 on throughput (disabling MKLDNN cripples Qwen synth ~15×); flag stays OFF. The leak itself stays open under `side-11`, bounded by the plan-143 recycle. Shipped 2026-06-01.
- [158 — Break the tts/index ↔ provider cycle (partial-mock release-gate flake)](archive/158-tts-index-cycle-mock-flake.md) — Fixes the `isTtsModelKey` partial-mock flake the cross-OS release gate caught: a circular import between `tts/index` and the provider module. Shipped 2026-06-01 (PR #381).
- [167 — Coqui XTTS v2 in-app installer (engine install parity with Qwen)](archive/167-coqui-in-app-installer.md) — One-click Coqui XTTS v2 install from Account → Models with streamed progress + value/difference copy, mirroring the Qwen installer; node-side on-disk detection replicates the Coqui lib's default `get_user_data_dir("tts")` so it never reports a wrong state. Shipped 2026-06-02 (PR #450).
- [167 — Frontend React-cluster + TypeScript major upgrade](archive/167-fe-react-cluster-upgrade.md) — Round 1 of the deferred framework majors: `fe-19` (Vite 8 + Vitest 4 + plugin-react 5), `fe-18` (React 19), `fe-21` (react-router 7), `ops-10` (TypeScript 6). Zero behaviour change; the battery is the regression net. Closes #403/#404/#407/#408. Shipped 2026-06-02 (PR #454).
- [168 — fe-2 power-user tuning panel](archive/168-fe-2-power-user-tuning.md) — Account → Advanced card (device-local `settings` slice): rebindable play/pause shortcut, high-contrast + larger-text accessibility toggles, and a tunable autosave debounce. Closes `fe-2`/#400. Shipped 2026-06-02 (PR #450).
- [170 — Dependency major upgrades round 2 (Zod 4 · Express 5 · pdfjs 5 · Tailwind 4)](archive/170-deps-majors-zod-express-pdfjs-tailwind.md) — Round 2 of the deferred framework majors: `srv-25` Zod 4 (drops `zod-to-json-schema`), `srv-24` Express 5, `srv-26` pdfjs-dist 5, `fe-20` Tailwind 4. Zero behaviour change plus three seam tests. Closes #405/#406/#409/#410. Shipped 2026-06-02 (PR #489).
- [177 — Per-quote expressive / emotion synthesis (fs-25)](archive/177-fs25-per-quote-emotion.md) — Per-quote emotion tags (`neutral`/`whisper`/`angry`/`excited`/`sad`) that select a pre-designed Qwen variant voiceId at synth time (no `/synthesize` schema change; strict no-op on Kokoro/XTTS). Additive `Sentence.emotion` + `overrideTtsVoices.qwen.variants`; variants minted through the existing `design-voice` flow with an emotion-augmented instruct. Core (waves 1–3, 4a, 5a–5d, 6a) shipped 2026-06-04 (PR #505). Deferred sub-waves re-homed: 4b→`fs-33` (#510), 5e remainder→`fs-34` (#511), 6b→`fe-32` (#512); 5e manuscript hint shipped via `fe-31`. **fs-33 (Detect-emotions backfill pass) + fs-34 (remove-variant, Voices badge, staleness, cast-row count) shipped 2026-06-07** (closes #510 + #511) — detected emotion reaches synth via the manual-tag path (`applyDetectedEmotions` → `manuscript-edits.json` → `rebuildCacheFromEdits`). Live-GPU acceptance owed. Closes #479.
- [179 — Pre-assembly per-sentence QA gate + auto re-record](archive/179-pre-assembly-segment-qa.md) — Signal-based per-sentence QA (dead-RMS / silence-run / duration-drift) on the in-memory PCM before concat, re-records bad sentences in place (keep-best-of-N); `POST /chapters/:id/audio-qa-repair` re-scans rendered chapters. Shipped 2026-06-06 (PR #513, closes #509).
- [181 — Stage-2 attribution coverage guard + audit](archive/181-stage2-coverage-guard.md) — Detects stage-2 loop-and-truncate (duplicated + missing tail) against the exact input prose, re-analyses, and audits library-wide (`npm run audit:stage2-coverage`). Shipped 2026-06-06 (PR #516, closes #515).
- [182 — Missing-speaker roster-coverage guard (Layer 2b)](archive/182-missing-speaker-roster-guard.md) — Stage-1 binding rule + `roster-coverage.ts` retry/auto-add for a quoted speaker dropped from the roster; chapter-drift WARN; audit/repair scripts. Shipped 2026-06-06 (PR #520, closes #519).
- [183 — Re-analysis preserves designed voices + per-chapter Reanalyse](archive/183-reanalyse-preserve-voices.md) — `mergeAnalysisResultWithExistingCast` overlays preserved voice fields at all five cast-write sites; per-chapter Re-analyse button; relink recovery script. Shipped 2026-06-06 (PR #521, closes #518).
- [185 — Golden-audio regression harness (ops-11)](archive/185-golden-audio-regression.md) — Opt-in deterministic audio gate: Suite A (GPU Kokoro length-baseline) + Suite B (GPU-free ffmpeg assembly); never in `verify`. Shipped 2026-06-06 (PR #527, closes #467).
- [186 — ASR content verification for per-sentence audio QA (srv-31)](archive/186-asr-content-qa.md) — 4th sidecar `WhisperEngine` + `POST /transcribe`; `classifyTranscript` WER policy (ok / drift / inconclusive); OFF by default (`SEG_ASR_ENABLED`), CPU-first. Shipped 2026-06-06 (PR #526, closes #508).
- [187 — Large-chapter stage-2 truncation fix + attribution-coverage audit](archive/187-large-chapter-stage2-and-attribution-coverage.md) — `AnalyzerTruncatedError` + paragraph-boundary chunking with adaptive re-split; shared `attributeChapterStage2` runner; `validateAttributionCoverage` 0-line half-state check. Shipped 2026-06-06 (PR #532, closes #528 + #529).
- [189 — Recover dialogue lines stranded on narrator (+ fold keeps tagged speakers)](archive/189-recover-tagged-narrator-lines.md) — `recoverTaggedNarratorLines` flips a narrator quote before a `Name said` tag onto the resolved speaker; fold keeps 0-line prose-tagged speakers. Shipped 2026-06-06 (PR #557, closes #537).
- [190 — Stable per-chapter `uuid` (srv-35)](archive/190-srv-35-stable-chapter-uuid.md) — Immutable optional chapter `uuid` minted lazily by `ensureChapterUuids` at the persisting read seams, carried through restructure by identity, protected from wholesale-state strip by `reconcileChapterUuids`; listen-progress `chapterUuid` makes resume follow a chapter across a restructure (repairs a latent web-player bug). First MVP server prereq for the Android companion (188). Shipped 2026-06-06 (PR #569, closes #540, srv-35).
- [191 — Per-chapter sync-manifest endpoint (srv-32)](archive/191-srv-32-sync-manifest.md) — `GET /api/library/sync-manifest`: two-level, gzip'd, `?since` delta; index carries audio-aware `updatedAt` + `activeBookIds`, detail keys chapters off the srv-35 `uuid` with a finalize-converged fingerprint + actual `urlSuffix`/`durationSec`/`lufs`. Feeds `app-3` delta sync + `fs-15`. Shipped 2026-06-06 (PR #570, closes #538, srv-32; durations PR #601 2026-06-07).
- [192 — Cross-book character link: collapse duplicates + carry profile](archive/192-cross-book-link-profile-merge.md) — `filterLinkablePriorCandidates` collapses prior-book copies by `voiceId`; `cast/link-prior` merges profile (evidence / attributes / etc.); backfill script tops up thin multi-book rows. Shipped 2026-06-06 (PR #574).
- [196 — Single voice design — background-survivable with live progress](archive/196-single-voice-design-background.md) — Single-character Qwen voice design promoted to a detached, SSE-streamed, reattachable background job reusing the bulk `castDesign` slice/middleware/pill (`kind: 'bulk' | 'single'`); branded in-drawer `DesignProgress` (ragged-waveform + soft-fill ETA + `designing`/`rendering` phases); first designs auto-persist, re-designs hold a `-preview` for A/B compare; symmetric single↔bulk 409. Shipped 2026-06-07 (PR #639).
- [197 — ASR "Verifying speech" phase in the Generate view](archive/197-asr-verifying-phase.md) — `chapter_verifying` SSE tick + a UI-only `'verifying'` member of `Chapter.phase` (mirrors `assembling`) so the per-chapter Generate row reads "Verifying speech…" during the ASR content-QA pass; per-sampled-group `asr.onProgress` fires even for `ok` verdicts, feeding the no-progress watchdog (closes a latent false-stall). Shipped 2026-06-08 (PR #646, closes #640, fs-40).
