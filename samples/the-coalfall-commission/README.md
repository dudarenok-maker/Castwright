# The Coalfall Commission (bundled sample)

An original Castwright work, all rights reserved. Bundled as the fs-22 generate-able demo book. No audio ships — the demo runs the real pipeline locally (the analysis cache rebuilds from manuscript-edits.json on the first generate).
